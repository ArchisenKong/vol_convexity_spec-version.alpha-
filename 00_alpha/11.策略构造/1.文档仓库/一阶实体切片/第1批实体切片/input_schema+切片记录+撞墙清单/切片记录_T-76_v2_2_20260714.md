# 切片记录 · T-76 option surface node（重打）· v2.2 · 20260714

> **v2.2修订（W-7扫描H2/H3/H4裁定落地）**：①〔H2〕容差口径注记——本记录各处"(b)≤1e-15"为裁决13当时定值，**现行有效值＝1e-12（裁决19，20260712修订，任务6 v1.7）**；"KD登记项"末段"锚定测量后续直接适用"前瞻声明失效（锚定权已由裁决19重定），原文留档不删，判定结论不受影响（实测上界3.464e-16≪1e-12）；②〔H3〕新增"本根裁定登记"固定字段（W-1，汇总散落于input_schema §1/§4之裁定语句）；③〔H4〕step0段补追溯性敞口注记。schema同步升v2.2（H1，bucket阈值三分定性）。A/B产物与全部数值零变动，合格判定维持通过。
> **v2.1修订**：依裁决13（容差重锚：连续解析类相对diff≤1e-15）对 D段 step1-2 重断言（仅改判据参数，A/B产物零变动）；依裁决14（实体边界）微调对象语义并将 BS 算法降格为占位桩（entity/input_schema.md v2.1）；撞墙清单更新至 v2.1（TPL2闭合＋新增TPL1-T76-01）。v2.0 判定（不合格）系依当时有效口径如实执行，非执行错误，留档不删。

## 对象语义（必填，B1）

T-76 = option surface node：组合级 Greeks 管理的最低观察单位实体。以 leg_type × side × option_type × tenor_bucket × moneyness_bucket × scenario 六维坐标为身份键，将一笔具体期权持仓唯一归属到一个 node；node 级 raw Greeks 是归属于该坐标的全部持仓 per-lot Greeks 之和——**per-lot raw Greeks 为上游输入（裁决14），T-76 实体算法要件＝bucket 派生＋坐标归属＋node 级聚合**，node 级 raw Greeks 为上游输入之聚合事实，只回答"当前事实是什么"（坐标归属为记录性映射，无判断成分）。lot_id、原始 expiry、原始 strike、liquidity_bucket 作为保留字段随持仓记录携带，不参与坐标身份键（本根工程裁定，见 entity/input_schema.md §1，不外推为跨根命名规范）。本根 per-lot Greeks 由占位桩（BS 无股息解析解）供给，真实供给方为待建的品种感知自算 Greeks 引擎（TPL1-T76-01，open）。

实跑印证：3 笔人造持仓唯一映射到 2 个 node（LOT-0001、LOT-0002 同坐标归入 N1，聚合生效；LOT-0003 归入 N2，区分生效），语义与实跑行为一致。

## 合格判定（必带，B2）

**通过（合格）**。

判定依据：D段三步全过——step0 前置合规通过；step1-2 数值比对通过（裁决13 口径：连续解析类相对 diff ≤ 1e-15；实测 10 项中 7 项 diff=0、3 项非零且相对 diff ≤ 3.464e-16，全部限内）；step3 附加断言通过。判定口径出处＝裁决13（任务6 v1.6 D段①）。

## 一阶实体文件清单

| 文件 | 说明 |
|---|---|
| entity/input_schema.md | **v2.1**：字段取用裁定＋schema＋bucket算法（实体算法要件）＋per-lot Greeks 占位算法（桩，裁决14降格标注） |
| entity/input_positions.json | A段人造input：3笔期权持仓，`_data_source: synthetic_hand_constructed`（v2.0原样） |
| entity/harness.py | B段一次性harness：坐标归属＋node级聚合＋占位Greeks（v2.0原样） |
| entity/harness_output.json | C段实跑真数：2个node，坐标＋raw Greeks＋contributing_lots（v2.0原样） |

## 撞墙清单条目

非空。模板一 1 条（TPL1-T76-01，open：品种感知自算 Greeks 引擎，缺模块，外审阶段暴露）；模板二 1 条（TPL2-T76-01，已闭合，裁决13）。详见 `撞墙清单_T-76_v2_1_20260710.md`。

## 检验通道记录

**step 0 结论**：通过（同 v2.0，三项复核 True；本次重断言复跑再证）。

〔v2.2注记，W-7扫描H4裁定〕本记录未载step0(ii)扫描模式、扫描集内容与剥离口径——本根构造早于W-5（bare import模式，自T3-03起）与W-8（统一扫描集＋剥离后判定，自T-24起）生效时点，属追溯性记录证据敞口；终判依据＝外审源码核查（三层外审第二层，已完成），非夹具扫描。不重扫不重跑。

**独立复算实现方式说明（何以路径独立）**：同 v2.0——数据源独立（复算直读 clean 人造 input，不 import harness、不取中间量）；计算逻辑独立编写（bucket 分配 if/elif 链 vs 阈值列表遍历；CDF 走 erf vs erfc 不同 libm 入口）；共享仅语言级数学原语。外审补充佐证：非零 ULP 级差异本身为两路径真独立之物证。

**逐项比对结果**（裁决13 口径：相对 diff ≤ 1e-15）：

| Node | 分量 | diff | 相对diff | 判定 |
|---|---|---|---|---|
| N1 | delta / vega / theta / rho | 0 | — | 限内（精确相等） |
| N1 | gamma | -1.421e-14 | 1.789e-16 | 限内 |
| N2 | delta / gamma / rho | 0 | — | 限内（精确相等） |
| N2 | vega | 1.819e-12 | 1.637e-16 | 限内 |
| N2 | theta | -1.819e-12 | 3.464e-16 | 限内 |

坐标集合一致：True。10/10 项限内，0 项越限。断言脚本 v2.1 变更：判定逻辑改按 REL_TOL=1e-15；全部非零 diff 无论限内外均逐条如实记录；删除 v2.0 硬编码"不合格原因定位"叙述（外审观察项订正），原因描述以数据日志为准。

**step 3 附加断言结论**：通过（①唯一归属 ②坐标与明细一致 ③形态齐备，均 True）。

## KD 登记项：容差实测值

- **适用口径**：裁决13——确定性·连续解析计算类，相对 diff ≤ 1e-15（锚＝本根实测上界 3.5e-16 × ~3倍裕度，测量派生）。
- **实测值**：10 项中 7 项 diff=0；3 项非零，相对 diff ∈ [1.637e-16, 3.464e-16]（1-2 ULP），全部限内。占用裕度约 1/3，余量充足。
- **本根为该子口径的锚定测量**：后续连续解析计算类对象直接适用，不逐根重定；离散/精确有理计算类维持 0（bit-exact）；迭代类开放项状态不变（任务6 v1.6）。
  〔v2.2注记，W-7扫描H2裁定〕上句前瞻声明**失效**：T-24实测（复合BSM＋差值型输出，cancellation放大）证伪单一来源锚定的可转移性，(b)锚定值经裁决19重定为**1e-12**（任务6 v1.7），本根1e-15及其"直接适用"声明仅为历史记录。本根实测值（上界3.464e-16）在新旧锚定下均限内，合格判定不受影响。

## 本根裁定登记〔W-1字段，v2.2依W-7扫描H3裁定补落〕

| # | 裁定语句 | 指针 |
|---|---|---|
| 1 | 实体边界：T-76实体算法要件＝bucket派生＋坐标归属＋node级聚合；per-lot raw Greeks＝上游输入，BS解析解降格占位桩，缺口TPL1-T76-01 | 裁决14；input_schema 头部/§4；本记录B1 |
| 2 | 坐标轴取用：六维身份键取T3§8.2结构、expiry/strike用bucket形态——本根工程裁定，不外推为跨根命名规范 | input_schema §1 |
| 3 | scenario轴本根固定"base"，未消耗多值维度，范围限定非scenario语义裁决 | input_schema §1 |
| 4 | 保留字段（lot_id/expiry/strike/liquidity_bucket）不入身份键——本根工程裁定，不外推 | input_schema §1 |
| 5 | bucket阈值取值＝类三阶段B校准候选，声明值不外推；函数结构＝要件已定 | input_schema §3〔v2.2注记〕；W-7扫描H1裁定 |
| 6 | raw口径：Greeks均未做行业惯用缩放（vega按σ变化1.0计、theta按年计、rho按r变化1.0计），与Q4"raw Greeks"一致，不引入缩放判断 | input_schema §4 末段 |
| 7 | 容差子口径(b)分派；本根1e-15锚定声明经裁决19失效，现行值1e-12 | 本记录KD登记项〔v2.2注记〕；裁决13/19 |
