# 期权波动率工程笔记

> **v1.2（20260730）**：QT-002录入（raw Greeks口径读法说明，KD指示收录20260730，裁决45随行）。模板、字段集、路由规则零变动。

> v1.1（20260726）：首条目录入——QT-001（容差分派的成分级应用，T-33外审P-4，KD裁定收录＝裁决34-F2）。模板、字段集、路由规则零变动。

> **v1.0**：模板确立，字段与路由规则定稿（尚无条目）
>
> **定位**：记录波动率凸性/期权工程领域的技术性经验——Greeks计算、vol surface构造、数值实现坑、容差与精度处理。条目应自包含（不依赖跳转项目文档即可复用），过程性/治理性教训归 EXP- 系列（见《工程构造经验笔记》）。
>
> **登记门槛**：同 EXP- 系列——满足以下任一条方登记——(1) 原则可迁移到本项目外场景；(2) 纠正此前隐含错误假设；(3) 揭示此前未命名的失败模式。

---

## 索引

| ID | 日期 | 类别 | 一句话结论 | 适用范围 | 状态 |
|---|---|---|---|---|---|
| QT-001 | 20260726 | NUM | 容差子口径分派作用于数值成分本身而非计算上下文：含超越函数路径中的离散派生量仍按bit-exact比对 | 量化工程通用 | 假设待验证 |
| QT-002 | 20260730 | NUM | 全链Greeks唯一raw口径（裸偏导零缩放），行业惯例换算仅在展示层做一次；本条为读法辅助，权威载体＝T-76 schema原文 | 项目全链＋量化工程通用 | 定案约定（读法辅助） |

---

## 条目

### QT-001 · 容差分派的成分级（非族级）应用

| 字段 | 内容 |
|---|---|
| 日期/来源 | 20260726，T-33切片构造＋外审复核（外审报告§1第二层第10项、P-4）；KD裁定收录＝裁决34-F2；容差实测值入档＝裁决34-F1 |
| 类别 | NUM |
| 适用范围 | 量化工程通用 |
| 触发情境 | T-33五族人造路径中，族Q价格生成含超越函数sin（C3分派判据命中子口径(b)相对diff≤1e-12），但族Q的crossing_count（穿越计数）仍按子口径(a) bit-exact（diff=0）比对，未因所在计算路径含超越函数而放宽 |
| 教训/问题 | 容差子口径的分派单位若取"族"或"计算路径"（上下文级），会把离散派生量错误地纳入放宽口径——整数计数结果在两条独立实现路径上若不逐位相等，即为实现分歧，1e-12容差会掩盖它 |
| 根因 | C3分派判据（超越函数在场性）的作用对象是**被比对的数值本身**：价格序列（浮点，超越函数直接产物）与穿越计数（整数，价格序列的离散函数）是不同成分。计数不引入额外舍入，比对对象是整数本身 |
| 可迁移原则 | 逐成分分派：对每个进入数值比对的量单独问"该量本身的取值过程是否引入连续解析舍入"，而非问"它所在的计算路径是否含超越函数"。同一根内允许(a)(b)两口径并存于同一族 |
| 技术内容 | T-33实测入档值（裁决34-F1）：族P/O/Z/N（整数/Fraction精确有理）全项diff=0；族Q逐点价格相对diff=0.000e+00、rv_proxy相对diff=1.735e-16（≤1e-12上限，子口径b）、crossing_count diff=0（bit-exact不放宽）。锚定值情境依赖纪律同步确认：各族本根首次实测，未跨复合度搬运（C3命题，X4实证链） |
| 项目实例指针 | T-33 input_schema §5（crossing_count恒按bit-exact之注记）；切片记录§3-6；外审报告§1第二层第10项；裁决13/19（两子口径设立与锚定值修订） |
| 适用边界/反例 | (a) 离散量若定义本身依赖浮点比较（如"价格>阈值"计数且阈值为浮点计算产物），边界点归属可能受上游舍入影响，此时计数的bit-exact比对可能因合法舍入差异失败——须先核对边界点是否落在舍入敏感区（T-33族Q边界点103.0/97.0为精确可表示值，未触发）；(b) 反例待寻 |
| 状态 | **假设待验证**（单根实证；跨根第二例待后续切片） |
| 关联条目 | EXP-003（不同族：本条属数值口径，EXP-003属验证方法论） |


---

### QT-002 · raw Greeks口径读法说明（人读辅助件）

| 字段 | 内容 |
|---|---|
| 日期/来源 | 20260730，六桩接口扫描JP-1裁定（raw量纲口径入1级契约）随行产出；KD指示收录（"要有个读法的说明文档"） |
| 类别 | NUM |
| 适用范围 | 项目全链账本与引擎输出读数；原则可迁移任何多模块Greeks系统 |
| 权威声明 | **本条＝读法辅助，非规范载体**。口径权威＝一阶实体_T-76_input_schema原文（1级契约以指针引原文，不以本条为据）；本条若与schema原文不一致，以原文为准并订正本条——防双载体漂移（T3-17翻译漂移同型风险） |
| 技术内容·口径定义 | raw值＝定价函数V(S,K,σ,r,τ)偏导数的**裸输出**，任何行业缩放不过链路边界。已言明三项：**vega per 1.0σ**——σ以小数计（20%波动率＝0.20），vega＝σ变动1.0（即100个vol点）的价值变化；**rho per 1.0r**——r变动1.0（100个百分点）的价值变化；**theta年计**——对τ（年单位）求导的裸值 |
| 技术内容·读数换算（仅展示层执行一次） | 行业惯常"每1 vol点"vega＝raw_vega÷100（raw_vega=500 ⇔ 每vol点$5）；"每1bp"rho＝raw_rho÷10000；日theta＝raw_theta÷年day count。**年day count（365/252等）系另一约定维度，T-76 schema未言明，换算时须自选并就地标注，不得默认** |
| 教训/问题 | 缩放因子是纯表示层约定，但混用＝静默错：同账本内per 1.0σ与per 1%并存差100倍，数值均"合理"，断言测试不FAIL，错误至消费端才显形且难归因——"系统正常运行、数值全错"形态。事前唯一便宜拦截点＝口径契约（JP-1入1级之依据） |
| 可迁移原则 | 全链单一raw口径＋缩放只在最终展示层做一次＋换算责任在展示端；任何"每单位变动"读数先问单位（1.0还是1%还是1bp），单位差即10²/10⁴量级差 |
| 项目实例指针 | T-76 schema量纲段（P1-3转抄，扫描记录§1桩1）；裁决45（JP-1入1级）；44-C9密度下限条款 |
| 适用边界/反例 | delta/gamma及未言明维度（day count、gamma报价惯例per 1%move等）不在已言明面内，按44-C8沉默不补白——需用时查schema原文或留引擎切片撞出，本条不代拟 |
| 状态 | **定案约定（读法辅助）**——非假设，系对既裁契约的人读转写；漂移风险由权威声明行对冲 |
| 关联条目 | QT-001（同类NUM；彼管比对容差，此管计量口径） |

---

## 类别标签说明

| 标签 | 含义 |
|---|---|
| VOL | 期权/波动率领域技术细节（Greeks语义、surface构造等） |
| NUM | 纯数值计算通用坑（不含期权语义，如浮点误差、容差机制设计） |

---

## 路由规则（与《工程构造经验笔记》共用，规则原文见该文档）

| 情形 | 去向 |
|---|---|
| 教训关于期权/Greeks语义，或不含期权语义的纯数值计算坑 | 本文档（QT-） |
| 教训关于治理/AI协作/验证方法/流程/文档纪律 | 《工程构造经验笔记》（EXP-） |
| 一条教训同时含两层 | 拆两条，各自登记，互加"关联条目"指针 |
