# 切片记录 · TC-33 path P/L / worst-path · v1.2 · 20260714

> **v1.2修订（W-7扫描H5/H6裁定落地）**：①〔H5〕"本根裁定登记"由KD登记项内补录形态升格为独立固定字段（W-1格式），并经完备性复核补齐至5条（原3条＋子口径分派＋无前视约定归属）；②〔H6〕step0段补W-5生效时点注记（bare import模式源于本根M6实证、生效自T3-03、本根不回补扫描）。A/B产物与全部数值零变动，合格判定维持通过。
> **v1.1修订**：依裁决16（实体边界：对冲政策/hedge仓位序列与估值约定＝上游输入；TC-33实体算法要件＝逐路径P/L聚合＋worst确界）微调对象语义并将阈值对冲规则降格为占位桩（entity/input_schema.md v1.1）；撞墙清单更新至 v1.1（撤"无条目"判定，新增 TPL1-TC33-01，外审阶段订正）。A/B产物与全部数值零变动，合格判定维持通过。

## 对象语义（必填，B1）

TC-33 = path P/L / worst-path：对给定/声明的路径集，逐路径计算一个动态对冲持仓的实现 P/L（路径凸性 P/L，Gamma 路径实现语义）。**对冲政策/hedge仓位序列与估值约定为上游输入（裁决16），TC-33 实体算法要件＝逐路径P/L聚合＋worst确界**；本根以阈值制占位桩实例化路径相关性（对冲仓位随路径穿越执行价的时点而变化，非终点单一决定），真实供给方为待建对冲政策对象（TPL1-TC33-01，open）；worst-path 取路径集合上 P/L 的确界（最差取值），为非连续对冲者的风险上限口径。逐路径 P/L 为对声明路径的重估，worst 为集合上确界运算，两者均无概率/方向判断成分（与 §1 语义锚一致）。

实跑印证：5 条人造路径中，P2（单调上行至110）与 P3（先深跌后急拉、同终点110）验证"相同终点不代表动态对冲损益相同"——P2 P/L=+300，P3 P/L=−1200，路径形状差异直接决定损益符号翻转；P4（压力路径：OTM盘整后末端跳空至160）worst=−6200，量级远超期初权利金300，验证"权利金不是动态对冲者完整风险上限"。语义与实跑行为一致。

## 合格判定（必带，B2）

**通过（合格）**。

判定依据：D段三步全过——step0 前置合规通过（input 人造标记确认、harness 静态扫描无数据源接入、独立复算未耦合harness）；数值比对 6 项（5路径+worst）全部 diff=0（裁决13 子口径(a)：离散/精确有理计算，bit-exact）；附加断言 3 项全部通过（路径数=输出数、worst∈集合且为极值、形态齐备）。判定口径出处＝裁决13（任务6 v1.6 D段①）。

## 一阶实体文件清单

| 文件 | 说明 |
|---|---|
| entity/input_schema.md | B段路线裁定＋持仓构造＋对冲规则声明＋P/L公式＋路径集设计理由 |
| entity/input_paths.json | A段人造input：1个短call持仓（K=100,M=100,premium=3/股）＋5条路径，`_data_source: synthetic_hand_constructed` |
| entity/harness.py | B段一次性harness：Python原生循环实现，逐路径hedge P&L累加＋期末intrinsic payoff＋worst取值 |
| entity/harness_output.json | C段实跑真数：5路径P/L＋worst（值−6200，路径P4） |

## 撞墙清单条目

非空。模板一 1 条（TPL1-TC33-01，open：动态对冲政策/规则权威定义，缺定义，外审阶段订正登记）；模板二 0 条；§6.3 回传 0 条。详见 `撞墙清单_TC-33_v1_1_20260711.md`（含核验脚本假阳性排查说明）。

## 检验通道记录

**step0 结论**：通过。(i) input `_data_source=synthetic_hand_constructed` 确认；(ii) harness.py 静态扫描（剥离注释/docstring后关键词匹配，另以人工grep交叉核验）无 IBKR/QuantConnect/网络请求/外部文件读取——〔v1.2注记，W-7扫描H6裁定〕本扫描集不含W-5 bare import模式：该模式即由本根外审变异M6（`import requests`无调用逃逸）实证产生（KD裁定D4），生效自T3-03任务包起，对本根为追溯性夹具口径敞口，不回补扫描不重跑；终判依据＝外审源码核查，已覆盖；(iii) independent_recompute.py 未 import harness 模块、未读取 harness_output.json，人工grep交叉核验确认所有"harness"字样命中均落在独立性声明docstring内，非实际耦合。

**独立复算实现方式说明（何以路径独立，何以核心分叉）**：
- 数据源独立：`verify/independent_recompute.py` 直读 `entity/input_paths.json`，不触碰 harness 产物。
- 计算核心分叉（非取字段/循环外壳分叉）：hedge 阈值判定——harness 用 Python `if prices[t]>=K` 逐步判断，复算用 `np.greater_equal` 向量化布尔比较；区间差分——harness 逐步相减，复算用 `np.diff`；hedge P&L 累加——harness 用 Python for 循环累加，复算用 `np.dot(h*M, diffs)` 点积；期末 payoff——harness 用内建 `max()`，复算用 `np.maximum()`；worst 取值——复算对**自身独立算出**的 P/L 数组用 `np.min()`，不读 harness 的 worst 字段。四处核心表达式均分叉，非共用同一行。
- 手工第三方三角化（本根全整数运算，人工逐路径笔算作为第三方常量核验，见下）：

  ```
  P1=[100,102,98,103,99]: h=[1,1,0,1]
    hedge=1·100·2 + 1·100·(-4) + 0 + 1·100·(-4) = 200-400+0-400 = -600
    payoff=max(99-100,0)·100=0 ；P/L=-600+300-0=-300
  P2=[100,101,103,106,110]: h=[1,1,1,1]
    hedge=100+200+300+400=1000 ；payoff=max(110-100,0)·100=1000
    P/L=1000+300-1000=300
  P3=[100,95,90,98,110]: h=[1,0,0,0]
    hedge=100·(-5)+0+0+0=-500 ；payoff=max(110-100,0)·100=1000
    P/L=-500+300-1000=-1200
  P4=[100,95,92,95,160]: h=[1,0,0,0]
    hedge=100·(-5)+0+0+0=-500 ；payoff=max(160-100,0)·100=6000
    P/L=-500+300-6000=-6200
  P5=[99,97,94,96,93]: h=[0,0,0,0]
    hedge=0 ；payoff=max(93-100,0)·100=0 ；P/L=0+300-0=300
  worst=min(-300,300,-1200,-6200,300)=-6200（P4）
  ```

  三方（harness / 独立复算脚本 / 人工笔算）结果一致，无分歧。

**逐项比对结果表**（裁决13 子口径(a)：0，bit-exact；全部非零diff无论限内外均如实记录——本根全部为0，无非零项）：

| path_id | harness P/L | 独立复算 P/L | 人工笔算 P/L | diff | 判定 |
|---|---|---|---|---|---|
| P1 | -300 | -300 | -300 | 0 | 限内（精确相等） |
| P2 | 300 | 300 | 300 | 0 | 限内（精确相等） |
| P3 | -1200 | -1200 | -1200 | 0 | 限内（精确相等） |
| P4 | -6200 | -6200 | -6200 | 0 | 限内（精确相等） |
| P5 | 300 | 300 | 300 | 0 | 限内（精确相等） |
| WORST | -6200 | -6200 | -6200 | 0 | 限内（精确相等） |

6/6 项限内，0 项越限。

**附加断言结论**：通过。①路径数(5)与P/L输出数(5)一致；②worst(-6200)∈输出P/L集合且为该集合极值（min）；③形态齐备（逐路径标量 + worst标量，输出类型均为int，无缺失）。

## KD 登记项

- **适用容差子口径**：裁决13子口径(a)——离散/精确有理计算，0（bit-exact）。理由：本根全部数值（strike/multiplier/premium/价格序列）设计为整数，全部运算为 `+ - × max 比较`，无超越函数、无浮点舍入风险，整数运算在 float64/int64 下精确，天然满足bit-exact，非另需宽限的连续解析类。实测值：6项全部diff=0。
- **B段计算路线选择**：离散算术路线（价格序列直接声明持仓工具价格 + 期末intrinsic payoff + 阈值声明对冲规则）。理由：Q4判据"喂路径集+持仓，出各路径P/L与worst值"未要求连续重估；期权到期payoff取内在价值公式（纯算术），对冲比例取持仓自身声明的阈值规则（非Greeks/BS重估），全程不触发定价引擎依赖，不撞TPL1-T76-01。任务包§3 B段"允许的最简路线"条款明确许可此选择。
- ~~本根裁定登记（裁决16落地补录）~~〔v1.2：升格为下方独立固定字段，W-7扫描H5裁定，原三条并入不删义〕

## 本根裁定登记〔W-1字段，v1.2依W-7扫描H5裁定升格补齐〕

| # | 裁定语句 | 指针 |
|---|---|---|
| 1 | 实体边界：TC-33实体算法要件＝逐路径P/L聚合＋worst确界；对冲政策/hedge仓位序列与估值约定＝上游输入 | 裁决16；本记录B1；input_schema v1.1头部 |
| 2 | 对冲规则h(t)＝阈值制占位桩，非实体算法要件，缺定义登记TPL1-TC33-01 | input_schema §2 v1.1；撞墙清单TPL1-TC33-01 |
| 3 | 估值约定"期初/到期两点"＝上游输入之占位形态（含无前视时点约定：t时刻已知价格决策，随h(t)桩归属上游） | input_schema §2/§3 v1.1 |
| 4 | 离散算术路线选择＝本根工程裁定，不外推为TC-33唯一路线（重估路线留待TPL1-T76-01/TPL1-TC33-01闭合后）；全程不触发定价引擎依赖 | 本记录KD登记项；input_schema §1 |
| 5 | 容差子口径(a)分派（全整数、无超越函数、bit-exact），实测6项diff=0 | 本记录KD登记项；input_schema §3末段 |
