# 撞墙清单 · T-24 · v1.0 · 20260712

模板一登记：0 条新登（引用 TPL1-T76-01，缺实现型，定价引擎/重估实现供给方，裁决14模式，引用不重登）。
模板二登记：0 条。
§6.3 缺陷回传：1 条（本会话施工阶段主动自查，非外审订正——依裁决14/16/17/18同构教训，逐成分三分判别后如实登记）。

---

## §6.3 缺陷回传 · REG-T24-01

| 字段 | 内容 |
|---|---|
| 条目ID | REG-T24-01 |
| 缺陷类型 | 既有权威产物（裁决13子口径(b)锚定值）在新对象实测下覆盖范围存疑，非本根实现缺陷 |
| 缺陷描述 | 裁决13子口径(b)锚定值＝相对diff≤1e-15，锚定测量单一来源＝T-76（node级raw Greeks，各Greek解析式单次CDF求值）实测上界3.5e-16×~3倍裕度。本根（BSM期权全价公式，每腿两次复合CDF求值＋差值型输出bleed=V(t0)-V(t1)）逐项比对7项中5项相对diff超出1e-15既有锚定值，量级区间[1.21e-15, 6.68e-15]，末端bleed项（两个相近量级操作数相减，cancellation放大相对误差）达6.68e-15，约为既有锚定值6.7倍、T-76原始实测上界19倍 |
| 触发切片 | T-24（本根） |
| 触发场景 | verify/assert_check.py step1-2数值比对，harness.py（erf CDF入口）vs verify/independent_recompute.py（erfc CDF入口）逐项比对 |
| 出处 | 任务6 v1.6 §3 D段①（裁决13子口径(b)定案文本）；执行期裁决记录 v1.3 裁决13正文；本根 verify/comparison_table.tsv（实测数据） |
| 独立性核验（排除实现缺陷） | 以 mpmath 50位高精度基准交叉核验：harness路径（erf）偏离高精度真值相对量级≈1.68e-15；独立复算路径（erfc）偏离高精度真值相对量级≈8.37e-15。两路径均为合理浮点精度量级，无算法/编码错误；差异纯属两条独立浮点实现路径在多步复合运算（log/sqrt/exp/两次CDF复合）下的累积舍入误差，经bleed端操作数相减（V(t0)≈2998, V(t1)≈2454, bleed≈544）进一步放大 |
| 当前占位处理 | 数值比对逐项如实记录（全部非零diff无论限内外），不做未经授权的锚定值放宽处理；判定依裁决13(b)现锚定值字面适用，见切片记录B2 |
| 阻断性 | 不阻断 A→D 跑完（本根已完整跑通step0/数值比对/附加断言全流程，仅数值比对结论受该疑问影响） |
| 建议归属（仅指针） | KD复议：(a) 维持现锚定值不变，本根判不合格，返回条件＝定价桩改用误差累积更低的实现路径重打；或 (b) 依本根实测扩展/修订裁决13(b)锚定值（"复合定价公式+差值型输出"子情形另设锚定或统一上调），比照裁决13自身"测量派生非惯例值"方法论产生 |
| 状态 | open |

## 说明：其余机制成分三分判别结论（不登记，逐项如实记录理由）

- **公式结构 `bleed=V(t0)-V(t1)`**：类一测试——Q4判据原文"时间推移**定价**"要求经定价得出bleed，与重估差相容；线性均摊类候选（premium支出÷剩余期限）不经定价、与"定价"语义矛盾，当场裁掉。00A行86"时间价值**衰减**成本"佐证。裁A，定案写入实现，非桩，落档见 entity/input_schema.md §4①。
- **重估所用定价模型**：类二缺实现，供给方＝**TPL1-T76-01**（裁决14登记，缺模块），本根占位＝BSM无股息解析解，引用不重登。
- **模型参数（t0/t1、S/K/σ/r、持仓明细）**：类三/数据层，人造input构造自由度，不登记。S/σ/r在t0→t1间保持不变属该层构造裁定（孤立呈现纯theta衰减），非机制系数，见 input_schema §3。
- **分账约束成分**：income/cost分账规则本身由 T2/00A行86＋00B§4行221-242 明确定义（非缺定义），非机制缺口；income侧数值＝人造input承担（依赖披露§1处置），不登记。
- **t0→t1推移幅度**：τ0/τ1均为input直接给定值，非经固定响应系数/衰减规则从τ0派生τ1，不触发"固定系数须单独测试"条款（任务包§3末段自查项已过）。

## 核验脚本自查记录

`verify/assert_check.py` step0(ii)/(iii) 静态扫描剥离注释/docstring前后均0命中forbidden模式（本根harness.py/independent_recompute.py未出现"harness"字样于代码可执行部分，人工grep交叉核验剥离前raw文本0处命中，无需假阳性排查，与TC-33/GK-14先例的假阳性场景不同型，如实记录）。
