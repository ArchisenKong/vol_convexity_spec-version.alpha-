# F-12 简式重验一致性核证留痕件 · TPL-FW03A-02 · 引擎 v1.3 微修窗 · v1.0 · 20260808

> **会话身份**＝四3收口裁决台会话（微修窗，KD授权同session执行例外，裁决78）。**形态**＝简式（裁决76-5 收敛后口径）。

## §1 触发事实
- 触发原因：微修窗触碰 `entity/engine.py`（E-1α：`_crr_lattice` 返回 `n_steps_used`、diagnostics 改取实际执行路径返回值，含两处注释）。
- engine.py 指纹：`f9b5bf8e3aa1b6969b325da1684bed539098e0c9b78b2b495eb559ae9f439a10` → `9e85c76823803982b2c9d714a1e9ec8f3bef44245659feeb9c3a95e66fcdcbbf`

## §2 重验一致性核证
- **token 流对照**（剥离 COMMENT/NL/NEWLINE/INDENT/DEDENT/ENCODING）：2133 → 2140 token；差异自 index 1475 起，恰＝E-1α 之两处代码 token 增量（`"n_steps_used": n_steps` 返回项＋7 token 净增；diagnostics 取值 `CRR_STEPS`→`am["n_steps_used"]`）。**本窗为代码面变更（非纯注释），如实标注**——行为面核证如下：
- **数值产出零漂移**：`harness_output.json` 与修复前**逐字节一致**（`crr_steps` 回显值同为 500，取值路径变、值不变）；S-2 之 252 项 bit-exact 维持；probe 产物逐字节一致。
- **声明值零变更**：模块级 12 项常量 AST 对照零变动（新增常量＝0；`n_steps_used` 为返回 dict 键非模块常量）。
- **下游桩依赖面**：六根消费面零可观测行为变更，不触发下游重验。

## §3 内嵌段与脱版注记
内嵌段维持 v1.2 快照不动（76-5）；脱版差＝内嵌 v1.2 vs M1 现版 v1.3（微修后），仍一版；DOC-31 承载注记兑现。TPL-FW03A-03 本窗不触发排查。

## §4 能力边界
本件只核证指纹变更之行为面；内嵌段内容实质正确性归 FW-03-甲 本根。
