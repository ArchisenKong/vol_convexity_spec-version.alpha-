# P2' 摩擦记录 · 曳光弹第二发（QC）· 进行中 · 20260818

> **格式**：B10 四栏。**「未撞到」须显式填写，空白 ≠ 未撞到**（85-9 F0 修订）。
> **三态**：F0 链路贯通／F1 摩擦命中／F2 硬阻断。**F1／F2 均为有效产出，非不合格。**
> **本位纪律**：只记「撞到／未撞到」事实。**现象与原因分开写**——原因一律入「推测成因」栏并标明是推测。不定性、不总结、不给建议、不作总体判定。
> **状态**：本件随执行推进增量填写，非终稿。截至本版覆盖 §4 与 §5-1。

---

## 第一段 · §4 工作树与装载

| 项 | 撞到/未撞到 | 摩擦形态描述 | 反推指向之条款 | 是否需KD裁定 |
|---|---|---|---|---|
| M6 引擎装载核证形态 | **未撞到（F0）** | SHA256 对照可直接执行：`sha256sum` 逐件比对锚值，5/5 逐字节一致（engine v1.3＋GL-06 四件）。前值落 `verify/load_fingerprint_baseline.json` 作 A6' 零写入比对基线。**显式记录：无摩擦。** | 69扩项(i)／X-3 | 否 |
| A5' 引擎逐字复用（首轮） | **未撞到（F0）** | `entity/engine.py` ＝ `d88a955e…dca682cf`，与规格件 §0.3 锚值一致 | 85-1a | 否 |
| A9' 载体根逐字复用（首轮） | **未撞到（F0）** | GL-06 四件全部命中《切片记录_GL-06 v1.1》交付件指纹表 | 丁-3／F-12 同型 | 否 |
| GL-06 包内 `__pycache__` 落位 | **撞到（F1，已自解）** | 交付包内含 `entity/__pycache__/harness.cpython-312.pyc`（CPython 3.12 编译副产物），不在规格件四件枚举内。本位一度上呈为口径缺口，后按规格件 §3.1 α-6／§5 A9' 之**四件明确枚举**自行消解：只搬四件，pyc 不搬、不入 manifest、不入 A9' 射程 | 规格件 §3.1／§5 A9'；§10.5 缺陷-2 同型 | 否（已消解，KD 一词可推翻） |

---

## 第二段 · §5-1 基线快照（只读，零写入零删除）

### 2.1 通道面

| 项 | 撞到/未撞到 | 摩擦形态描述 | 反推指向之条款 | 是否需KD裁定 |
|---|---|---|---|---|
| `files.get_all(35174460)` | **未撞到（F0）** | A 类端点，一次成功。返回 2 件：`research.ipynb`（19,626 chars）／`main.py`（880 chars），均 `modified=2026-08-17 15:34:56` | §5-1；E1-17 遗留物基线 | 否 |
| `projects.get_all(ORG_ID)` | **未撞到（F0）** | A 类端点。返回 1 项：`id=35174460 TR2-ENV-PROBE`，`created=2026-08-14 15:00:56`，`modified=2026-08-17 15:34:58`，`lang=Py` | §5-1 | 否 |
| `object_store.list('/', ORG_ID)` | **未撞到（F0）** | A 类端点。根下 1 件：`/tr2probe-e14-marker.txt`，252 B，`modified=2026-08-17T13:25:59Z`。`objectStorageUsed=252`。与 E1-12「list 通」一致 | §5-1；E1-12 | 否 |
| `nodes.get_all(ORG_ID)` | **未撞到（F0）** | A 类端点，两轮均成功 | §5-1；E1-13 核对位 | 否 |
| `research/read` | **撞到（F2，见 2.2）** | 5 轮全 FAIL | §5-1；E1-13；A'-10 | **是**（FR-P2-03） |

### 2.2 FR-P2-02 · `research/read` 五轮失败，且错误原文三段式演化

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F2 硬阻断，该端点面）** |
| **形态描述** | 同一调用 `api.get("research/read", {"projectId": 35174460})`、同一参数、连续 5 轮（间隔 20 s），返回**三种不同错误**：<br>①轮 1–3：`RequestFailedError: Timeout waiting for a node allocation, please retry. R-a3697c1266afadefa71cbe10f5604b45`，耗时 **120.9／121.0／120.9 s**（服务端分配超时约 120 s）<br>②轮 4：`RequestFailedError: Connection not found.`，耗时 **0.9 s**<br>③轮 5：`RequestFailedError: The requested project does not exist or you do not have the necessary permissions to access it - 35174460`，耗时 **0.8 s**<br>**轮 5 之错误原文与已证事实矛盾**：同一 `projectId=35174460` 于本轮之前经 `files.get_all()` 与 `projects.get_all()` **两次读取成功**，项目确证存在且权限在场。故该错误文案**不指向真实失败面**。 |
| **推测成因（推测，非结论）** | 错误文案随服务端状态变化而变，且末轮文案与事实矛盾——形态类同第一发摩擦-06 之「错误文案不指明失败面」（IBKR Flex `1020: Invalid request or unable to validate request`）。是否为同型，归 P3' 与裁决台，本位不作跨发归纳。 |
| **反推指向之条款** | §5-1；E1-13；A'-10（B 类端点仅作旁证）；E1-19（HTTP 200＋`success:false` 之成败判定形态） |
| **是否需KD裁定** | 是（随 FR-P2-03） |

### 2.3 FR-P2-03 · research 节点 `busy` 状态于本位重试期间由 true 转 false —— **本位可能触碰遗留物，自陈**

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1，且含本位纪律自陈）** |
| **形态描述（事实，无推测）** | 同一节点 `RN-cfd071182d13803f49be6fa37a5d8b5c`（R-MICRO，free 档唯一 research 节点）：<br>· **A 轮 20260818T134301Z**：`busy=true`，`usedBy="Archisen Kong"`，`projectName="TR2-ENV-PROBE"`<br>· **B 轮 20260818T135234Z**（5 轮 `research/read` 重试之后）：`busy=false`，`usedBy="-"`，`projectName=""`<br>两轮之间本位**唯一**执行之动作＝5 轮只读 `research/read` 调用。<br>该节点 ID 与规格件 E1-13 所载 `RN-cfd0711…` 为**同一节点**；E1-13 载其自探测期（2026-08-17）起 `busy:true` 未释放，且 `research/stop` 端点不存在。 |
| **本位自陈（纪律面）** | A'-12 代定＝**遗留物不清，以基线快照隔离**；作业指令 §2.3＝「探测期遗留物一律不清理、不删除、不覆盖」。本位之 `research/read` 调用**是否构成对遗留 research 会话之触碰／置换／终止，本位无法确定**。三种可能并列，本位**不选**：<br>(i) 遗留会话自然过期（距探测期已逾 22 小时）；<br>(ii) 本位之节点分配请求置换了遗留会话；<br>(iii) 二者无因果，时间上巧合。<br>**本位如实上报该不确定性，不作有利于自身之推定。** |
| **反证材料（已采，支持"项目面未被改动"）** | · 云端项目 `modified` ＝ `2026-08-17 15:34:58`，A/B 两轮一致，**未变动**<br>· `research.ipynb`／`main.py` 之 `modified` ＝ `2026-08-17 15:34:56`，**未变动**<br>· ObjectStore `/tr2probe-e14-marker.txt` 252 B，`modified=2026-08-17T13:25:59Z`，**未变动**<br>→ **文件面与对象存储面零改动可证**；不确定性仅限 research 会话／节点占用状态面。 |
| **推测成因（推测，非结论）** | 无法在现有材料内分辨 (i)(ii)(iii)。QC 未暴露 research 会话之创建时刻与生命周期字段，`research/stop` 端点不存在（E1-13），故无直接观测位。 |
| **派生影响** | research 节点现为 `busy=false` ＝ **可分配态**。作业指令 KD 补充裁定②（X-1 取候选丙＝回测运行时＋research 内核**并跑**）与③（α-6 随丙两位各跑一次）之 research 位，其可达性由「节点被占」转为「端点调用失败但节点空闲」——**两者不是同一件事**，本位不合并判定。 |
| **反推指向之条款** | A'-12（遗留物不清）；作业指令 §2.3、§9⑤；KD 补充裁定②③；E1-13 |
| **是否需KD裁定** | **是** —— ①本位是否已越 A'-12 射程；②research 位后续是否继续探（继续即再次分配节点，不确定性同型复现）。**本位停在此处待裁，不自行推进 research 面。** |

### 2.4 本位工程 bug 分列（非环境摩擦，如实分开记）

| # | 现象 | 成因 | 处置 |
|---|---|---|---|
| BUG-P2-01 | `projects.get_all()` → `TypeError: missing 1 required positional argument: 'organization_id'` | **本位调用签名错**，漏传 `organization_id`。**非环境摩擦** | B 轮修正后 F0 通过 |
| BUG-P2-02 | `object_store.list()` → `TypeError: missing 1 required positional argument: 'organization_id'` | **本位传参位错**，真实签名＝`list(path, organization_id)`。**非环境摩擦** | B 轮修正后 F0 通过 |

**登记理由**：AGENTS.md 第5条允许适配层工程 bug 自由调试；但 A 轮日志已落盘且含该两条 FAIL，若不显式分列成因，将被后续消费方误读为环境阻断。**故显式分列，不修饰、不删除 A 轮日志。**

### 2.5 FR-P2-01 · 乙-5「先落原件」与 A8'「凭据零落盘」条款冲突

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1，条款面冲突）** |
| **形态描述** | 乙-5 条款：「下载成功即落**原件**」「载荷写入带序号/时间戳不覆盖」——原件＝bytes 直写、不改原件。<br>A8' 断言：全工作树 token／session id／支付卡字段字面命中数 ==0。<br>E1-18 事实：`research/read` 返回可用 session token；`accounts.get_organization()` 返回 `card{brand,expiration,last4}`——**普通响应夹带凭据**。<br>→ 对夹带凭据之响应，**落逐字节原件即违 A8'；遮蔽后落盘即非原件**。两条款在该类响应上**直接冲突**，规格件与作业指令均未给消解口径。 |
| **本位处置（保守，非裁定）** | 按 AGENTS.md 第6条（凭据面）优先：落**遮蔽后件**，并在每件内显式写入 `_probe_is_byte_exact_original: false`、`_probe_redaction_operator`（算子描述）、`_probe_original_fidelity_note`（冲突指针）。遮蔽后执行正则复扫（模式＝`[0-9a-f]{40,}`），**截至本版全部件 `leak-scan hits=0`**。 |
| **推测成因（推测，非结论）** | 乙-5 之归纳基础＝第一发摩擦-07/-08（下载保真面），其一阶实例不含「响应夹带凭据」形态；A8' 之立法目的＝凭据不落盘。两条款立法时点不同、射程未相交，故未预见该交叉。 |
| **反推指向之条款** | 乙-5；A8'；E1-18（F1-14／F1-15） |
| **是否需KD裁定** | **是** —— 消解口径归 KD。本位不自裁，仅登记并采保守处置。 |

### 2.6 FR-P2-04 · lean CLI 1.0.228 `object-store` 子命令 help 文案缺陷

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1，工具面）** |
| **形态描述** | `lean object-store --help` 之五个子命令 `delete`／`get`／`list`／`properties`／`set` 之说明文案**全部相同且全部错误**，均为 `Opens the local storage directory in the file explorer`。云端 Object Store 之正确入口为 `lean cloud object-store`。 |
| **推测成因（推测，非结论）** | lean CLI 自身 help 文本复制错误。 |
| **反推指向之条款** | M7 文件交换开销／工具面；E1-14 端点来源分级 |
| **是否需KD裁定** | 否（现象登记） |

---

## 第二段之二 · §5-1 C 轮（KD 删除云端项目后之新基线，只读）

**触发事实**：KD 于 20260818 **手动删除**云端项目 `TR2-ENV-PROBE`（id 35174460）。该项目为 E1-17 遗留物清单首项，A'-12 代定＝「不清，以基线快照隔离」。**本次清理系 KD 亲为，非本位动作**；A'-12 对该项之射程随 KD 动作落定，本位不评价。A/B 轮快照转为**删除前历史留痕**，C 轮为新基线。

**本轮不调用 `research/read`** —— 该面待 KD 裁（KD-P2-03），本位不裁不碰。

| 项 | 撞到/未撞到 | 摩擦形态描述 | 反推指向之条款 | 是否需KD裁定 |
|---|---|---|---|---|
| `projects.get_all(ORG_ID)` | **未撞到（F0）** | 返回 **0 项**。旧项目 35174460 已不在清单。账户下云端项目数 ＝ 0 | §5-1 新基线 | 否 |
| `object_store.list('/')` | **未撞到（F0）** | `/tr2probe-e14-marker.txt` 252 B，`modified=2026-08-17T13:25:59Z`，**仍在场、未随项目删除而消失**。`objectStorageUsed=252` | E1-17 遗留物；A'-12 | 否（登记） |
| `nodes.get_all(ORG_ID)` | **未撞到（F0）** | backtest／research 两节点均 `busy=false`，`usedBy='-'`，`projectName=''` | §5-1 新基线 | 否 |
| `files.get_all(35174460)` 对**已删除**项目 | **撞到（F1，见 FR-P2-05）** | 返回 **`[OK] list, 0 项`**——成功且空，**非错误** | E1-19；FR-P2-02 联动 | **是**（并入 KD-P2-04） |

### FR-P2-05 · 项目存在性之 API 语义双向不可靠

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1）** |
| **形态描述（两向并置，均为实测）** | **方向一（项目存在，报不存在）**：20260818T135234Z 前后，`research/read` 对 `projectId=35174460` 返回 `The requested project does not exist or you do not have the necessary permissions to access it - 35174460`；而同一 id 于同段时间内经 `files.get_all()`、`projects.get_all()` **两次读取成功**，项目确证存在。<br>**方向二（项目不存在，报成功）**：20260818T140756Z，该项目经 KD 删除后，`files.get_all(35174460)` 返回 **`[OK] list, 0 项`**——成功响应、空清单，**不报 not found、不报 error**。<br>→ 「项目是否存在」这一事实，在两个不同端点上分别得到**相反方向的误导性回答**。 |
| **推测成因（推测，非结论）** | 与 E1-19（QC API 以 HTTP 200＋`success:false` 返回业务错误）同一族：成败与存在性之语义不由状态码/异常承载。是否同族归 P3'，本位不作跨条归纳。 |
| **对 P2' 断言形态之影响（事实登记，非建议）** | 「以调用成功推断对象存在」与「以错误文案推断对象不存在」**两条推断路径在本环境内均已被实测推翻**。本位后续一切存在性判定改以 `projects.get_all()` 清单**枚举比对**承载，并逐次留痕。 |
| **反推指向之条款** | E1-19；FR-P2-02；P2' 断言脚本之成败判定形态 |
| **是否需KD裁定** | 是（并入 KD-P2-04） |

### FR-P2-03 之补充事实（时序，收窄不确定性但未消解）

- research 节点释放（`busy: true → false`）发生于 **A 轮 13:43:01Z 与 B 轮 13:52:34Z 之间**。
- KD 删除项目发生于 **B 轮之后、C 轮 14:07:56Z 之前**。
- **故：节点释放在先，项目删除在后；删除不是释放之成因。**
- FR-P2-03 之三种可能 (i) 自然过期／(ii) 本位调用置换／(iii) 巧合，**仍无法分辨**。KD-P2-02 未消解。

---

## 第四段 · §4/§5-2 云端承载与 α-6 载体面

### FR-P2-06 · QC file API 单件上限 32,000 字符 —— **规格件 E1 映射表内无此项，本发新发现**

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1）** |
| **形态描述** | `files.create(project_id, file_name, content)` 上传 43,416 字符之 `.py` 件被拒，错误原文逐字：<br>`RequestFailedError: File exceeds the maximum size of 32,000 characters by using 43,416.`<br>→ **单件上限 ＝ 32,000 字符**。该约束**不在规格件 E1-01～E1-24 任一行内**，亦不在探测三件之摩擦清单内。 |
| **触发路径** | E1-08（白名单拒 `.md`/`.json`）迫使改载体格式 → 采 base64 包装 → base64 膨胀约 +33% → 越 32,000 上限。**两个约束叠乘方才显形**，单独任一不触发。 |
| **本位处置（工程面，非条款面）** | 载体变换升 v2：`raw → zlib.compress(9) → base64 → .py 字面量`。载体件由 43,417/36,328 B 降至 **17,374 / 16,320 字符**，均在线内。变换仍可逆、仍带 SHA256 自核；本地无损性自证 5/5 逐字节相等。生成器内置上限守卫（超限即停，不上传）。 |
| **反推指向之条款** | E1-08；§3.2 工程面第1点「载体格式须改」；M7 文件交换开销 |
| **是否需KD裁定** | 否（现象登记）；但**建议登记为 E1 映射表增补候选**——归 KD |

### FR-P2-07 · `files/create` 失败非原子：报错但仍留下 1 字符文件条目

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1）** |
| **形态描述** | 首轮上传 `cloud_payload_engine.py`（43,416 字符）时 `files.create` **抛出** `RequestFailedError`（超 32,000 上限）。次轮上传前盘点云端项目，发现同名文件**已在场，内容长度 ＝ 1 字符**。<br>→ 调用以异常形态返回失败，但服务端**已创建文件条目**，内容为 1 字符。**失败非原子。** |
| **本位自陈（更正）** | 本位曾于报告中称首轮"上传 0/3，没有部分写入"——**该陈述错误**，实际留下 1 字符条目。已于本条更正。成因＝本位以"调用抛异常"推断"服务端无副作用"，该推断与 FR-P2-05 所载同族（成败语义不可由异常/状态码承载），本位未记取。 |
| **后果面** | 该 1 字符件已由次轮 `files.update` 覆盖为完整内容，现无残留。**若当时未复用同一项目而另建新项目，该 1 字符件将成为不可清理之遗留物**（`files/delete` 被 A'-14 全禁）。 |
| **反推指向之条款** | E1-19；FR-P2-05；A'-14（delete 全禁之下，非原子写入之残留不可清理） |
| **是否需KD裁定** | **是**（并入 KD-P2-04；另请 KD 注意 A'-14 与非原子写入之交互面） |

### FR-P2-08 · α-6 逐字节比对被平台行尾约定污染 —— **本地对照组实测**

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1，方法论面）** |
| **形态描述（定量，非推测）** | 于**本地**（Windows，CPython 3.10.11）以 GL-06 自带 15 期人造账本 input 实跑 `harness.py`，产出与锚件 `harness_output.json` 比对：<br>· 锚件（expected）：**4,159 B，CR=0，LF=149**（纯 LF）<br>· 本机产出（produced）：**4,308 B，CR=149，LF=149，CRLF=149**<br>· 字节差 **+149** ＝ 149 个换行各多一个 `\r`<br>· `byte_exact` ＝ **False**<br>· produced 之 CRLF 归一化为 LF 后：大小 4,159 B，与锚件**逐字节相等**，`sha256` ＝ `722f00ee0c4dbceb43ec4d72eb2c86cfbfddb0b5f879aa4f709124589cab7e44` ＝ **精确命中锚值**<br>· JSON 语义相等：**True** |
| **机制（自源码读出，非推测）** | GL-06 `harness.py` 以 `open(OUTPUT_PATH, "w", encoding="utf-8")` **文本模式**写盘，行尾由平台约定介入；锚件生成于 LF 平台。计算面（整数 cross-multiplication，零浮点、零超越函数）无差异。 |
| **对 α-6 判据之影响（事实登记，非建议）** | 规格件 §3.1 α-6 之「撞到/未撞到」判据为**逐字节相等**单值。该单值**无法分辨「计算不同」与「行尾不同」**。<br>**推论风险已实证**：云端运行位为 Linux（LF），逐字节比对**很可能 PASS**；但该 PASS 之一部分由「平台行尾约定恰与锚件一致」贡献，**非纯计算等价之证据**。若无本地对照组，该 PASS 将被误读为「GL-06 在 QC 环境内可运行且输出一致」。 |
| **本位处置** | runner 改为**双值并列**输出（A'-16 代定形态 (iii)「双值并列不单判」之同型适用）：`byte_exact` 与 `byte_exact_normalized` 并列，附 `newline_profile`（CR/LF/CRLF 计数）与归一化算子声明 `bytes.replace(b'\r\n', b'\n')`。**本位不单判、不定性。** |
| **与 E1-09／A'-16 之关系（区分，勿合并）** | E1-09／A'-16 讲的是 **`lean cloud pull` 通道**之 LF→CRLF 转换；本条讲的是 **harness 自身写盘**之平台行尾介入。**两者机制不同、通道不同**，仅同属行尾污染族。本位不合并登记。 |
| **反推指向之条款** | §3.1 α-6；A'-16；E1-09；子口径(a) ＝ 0 bit-exact |
| **是否需KD裁定** | **已裁** —— **KD 20260818 裁定：采纳双值口径**（KD-P2-06 落定，见下） |

**KD-P2-06 裁定留痕（20260818）**：

> KD 原话：「KD-P2-06，采纳你的双值口径。」

**落定内容**：α-6 之「撞到／未撞到」判据自本裁定起改采**双值并列**——`byte_exact`（原始逐字节）与 `byte_exact_normalized`（行尾归一化后逐字节）**并列记录、不单判**，附 `newline_profile`（CR／LF／CRLF 计数）与归一化算子显式声明 `bytes.replace(b'\r\n', b'\n')`。

**射程限定（本位自守）**：本裁定落**判据记录口径**，**未落**「GL-06 于 QC 环境内是否可运行」之任何结论，亦**未落**任何层级之合格主张（规格件 §3.1 禁作面不因本裁定松动）。本件不得被引作该结论之判例。

**适用位**：α-6-甲（回测运行时）与 α-6-乙（research 内核）**两位同口径**，两位结果分行记录、不合并、不取其一（作业指令 §5-2/§5-3）。

### FR-P2-09 · file API 往返逐字节保真（与 `lean cloud pull` 之 CRLF 转换对照）

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **未撞到（F0）** |
| **形态描述** | 经 `files.create`/`files.update` 上传、`files.get_all` 回读之三件（17,605 / 16,551 / 6,229 B，均纯 LF），回读后：<br>`byte_exact_raw` ＝ **True**（三件全部），`CR: 0 → 0`，大小逐件相等。<br>→ **file API 往返通道逐字节保真，无行尾转换。** |
| **对照（并置，不作推荐）** | E1-09 载 `lean cloud pull` 在 Windows 上做 **LF→CRLF 转换**。**两条通道在保真面行为不同**：file API 往返保真；pull 落盘转换。**通道选择直接决定指纹链是否成立。** |
| **反推指向之条款** | E1-07 回传路径矩阵；E1-09；A'-16；D-2 指纹链射程 |
| **是否需KD裁定** | 否（现象登记） |

### FR-P2-10 · α-6-乙（research 内核位）会话不可分配；`research` 端点语义厘清；E1-13 成因回溯

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F2，该运行位）** |
| **本发实测** | 20260818T143500Z，`projectId=35315786`：<br>· 执行前节点状态：backtest `busy=false`／research `busy=false`（**节点空闲，非配额面阻断**）<br>· `research/read` **4/4 全失败**，错误逐字 `RequestFailedError: Connection not found.`，耗时 0.8／0.9／0.8／0.9 s（**快速失败，非分配超时**）<br>· 按作业指令 §9⑤ 记 **F2 并停该位**，另一位继续。α-6-乙 与 X-1-乙 于本轮**零结果产出**。 |
| **端点语义厘清（自探测期日志读出，非推测）** | 《probe/logs/E26_research_create.log》与《E27_research_create_cold.log》载：<br>· `research/read` ＝ **读取既存会话**；无会话时返回 `Connection not found.`（E27 第2步逐字同文）<br>· `research/create` ＝ **创建会话**（POST，参数 `{projectId}`），返回 `sessionId／uri／token／nodeId`<br>→ 本发 4 轮失败之文案与 E27「无活动会话」态**逐字一致**，指向**会话不存在**，非项目不存在、非权限不足。 |
| **E1-13 成因回溯（重要，事实链完整）** | E27 日志时序（探测期 2026-08-17 15:06:53）：<br>①调用前节点 `busy=false`（执行人已于网页端停止会话）<br>②`research/read` → FAIL `Connection not found.`<br>③`POST research/create` → **FAIL，耗时 190.1s**，`Timeout waiting for a node allocation, please retry.`<br>④调用后节点 → **`busy=true`**<br>→ **E1-13 所载「research 节点 busy:true 未释放」之状态，成因＝探测期自身之 `research/create` 失败调用**：创建请求超时未产出可用会话，却把节点置于 busy 态。free 档单节点 ＋ `research/stop` 端点不存在（E1-13）⇒ 该状态无端点可解。 |
| **对 FR-P2-03 之修正（收窄，仍未全解）** | 原三选一之不确定性得到**部分收窄**：<br>· **卡死状态之来源已确证＝探测期之 `research/create` 失败调用，非本位。** 本位于 08-18 首次接触时该状态已在场逾 22 小时。<br>· **仍未解**：该状态于 A 轮（13:43:01Z）与 B 轮（13:52:34Z）之间转为 `false`，本位 5 轮 `research/read` 是否为其终结成因，现有材料仍不可分辨（E27 显示 `research/read` 在卡死态下亦耗时 121s，说明该调用确会介入节点分配面）。<br>· **净结论（限于事实面）**：本位**未造成**该遗留状态；是否**终结**该状态，未定。KD-P2-02 射程随之收窄，未消解。 |
| **推进路径与其已知代价（事实并置，不作建议）** | 通向该运行位之唯一已知端点＝`POST research/create`。**探测期先例（E27）**：该调用在 free 档实测**失败**（190.1s 超时），且**失败后将节点置于 busy 态**——即本条所述卡死状态之制造动作本身。<br>→ 调用该端点存在**按先例复现卡死状态**之可能；而卡死态无端点可解（`research/stop` 不存在），亦不得以破坏性动作清理（A'-14 全禁）。 |
| **本位处置** | **停在此处，不自行调用 `research/create`**，呈 KD（KD-P2-07）。理由：KD 之「research 位放行」裁定作出时，E27 先例尚未进入呈报面；该先例构成**授权作出后新增之实质信息**（调用有制造不可解遗留状态之实测先例），本位不以既有授权覆盖之。 |
| **反推指向之条款** | 作业指令 §9⑤（记 F2 停该位，另一位继续）；§9②；A'-12；A'-14；E1-13；KD 补充裁定②③（X-1 取候选丙＝两位并跑） |
| **是否需KD裁定** | **是（KD-P2-07）** |

### FR-P2-10 之落定：α-6-乙 经「网页端起会话」路径**走通**（KD 20260818 选路径乙）

**KD 裁定留痕**：KD-P2-07 ＝ **选路径乙**（由 KD 在 QC 网页端打开 research notebook 起会话），**不调用 `research/create` 冷启动**。本位未调用该端点，E27 型卡死风险未触发。

**会话到场时序（只读轮询实测）**：网页端操作后 `node.busy` 由 `False` 翻 `True`（`usedBy='Archisen Kong'`），`research/read` 同刻由 `Connection not found.` 转 OK（耗时 1.9 s）。**「节点 busy 状态」即「会话是否在场」之可靠观测位**，本发确证。

**中间阻断（F1，已处置）**：会话在场后 `/api/kernels` 仍为 **0 项**，`/api/status` 载 `connections=0, kernels=0`——即 E27 所记 `Select Kernel` 态：**Jupyter 服务在场 ≠ 内核在场**。本位经 Jupyter C 类标准接口 `POST /api/kernels`（`name=python3`，kernelspec default ＝ `Foundation-Py-Default`）起内核，返回 **201**。<br>**自守与副作用登记**：该动作不在 A'-14 禁用面（禁用面＝`push`／`files.delete`／`object-store delete`）内；本位**未调** `DELETE /api/kernels`，不碰删除类动作。**副作用＝会话内留下一个由本位创建之运行中内核**，随会话闲置自回收。

**可用 kernelspec（实测）**：`python3`（Foundation-Py-Default，默认）／`foundation-legacy`／`csharp`（Foundation-C#-Default）／`foundation-autogluon`。

### α-6-乙 · research 内核运行位 · 结果（双值并列，KD-P2-06 口径）

| 项 | 值 |
|---|---|
| 运行位 | research 内核，`nodeId=RN-cfd071182d13803f49be6fa37a5d8b5c`（R-MICRO，free） |
| **python** | **3.11.14** CPython |
| **platform** | **Linux-5.15.0-187-generic-x86_64-with-glibc2.35** |
| **libc_ver** | **('glibc', '2.35')** |
| 执行通道 | 内核 WebSocket（`execute_reply status=ok`，`kernel_errors=[]`） |
| harness | exit code **0**；stdout `15 periods, 11 evaluated` |
| 四件还原（内核内） | `harness.py` `5702aae3…`／`harness_output.json` `722f00ee…`／`input_schema.md` `33fe363c…`／`ledger_input.json` `31605728…` —— **4/4 命中锚值** |
| **byte_exact** | **True** |
| **byte_exact_normalized** | **True** |
| newline_profile | expected `{CR:0, LF:149, CRLF:0}`／produced `{CR:0, LF:149, CRLF:0}` |
| produced sha256 | `722f00ee0c4dbceb43ec4d72eb2c86cfbfddb0b5f879aa4f709124589cab7e44` |
| expected sha256 | `722f00ee0c4dbceb43ec4d72eb2c86cfbfddb0b5f879aa4f709124589cab7e44` |
| 三态 | **F0（该运行位链路贯通）** |

**环境指纹与既载事实之对表**：与规格件 **E1-02** 所载（Python 3.11.14／`Linux-5.15.0-187-generic-x86_64-with-glibc2.35`／libc `('glibc','2.35')`）**逐项一致**。E1-02 明载该指纹「非 E8 所指本地镜像，不可替代」——本发不改该定性。

**FR-P2-08 之预判被实证（重要，须显式）**：本位于本地对照组曾登记「云端为 Linux（LF），逐字节比对**很可能 PASS**，且该 PASS 之一部分由平台行尾约定恰与锚件一致贡献，非纯计算等价之证据」。本轮实测：produced `CR=0`，与锚件行尾约定**一致**，`byte_exact` 直接 True。<br>→ **该 True 与本地 Windows 之 False，差异源已定量归因于行尾（+149 B ＝ 149 个 CR），非计算面。** 双值口径（KD-P2-06）在两位上均已产出可分辨记录。

**禁作面自守**：本结果为环境可运行性材料。**不得**表述为 GL-06 之任何层级合格主张，**不得**表述为「环境可用」之总体判定（规格件 §3.1 禁作面；85-9 反面条款）。

### α-1 材料 · 自造载荷注入 research 内核之通道（候选 (i) `.py` 内嵌，实测贯通）

| 项 | 实测值 |
|---|---|
| 内核 cwd | `/QuantConnect/research-cloud/airlock`（与探测期 F2-09 所载一致） |
| cwd 是否在 `sys.path` | **是**（`sys.path[0]` 即 cwd） |
| `listdir(cwd)` | `__pycache__`／`cloud_payload_engine.py`／`cloud_payload_gl06.py`／`cloud_runner_alpha6.py`／`config.json`／`log.txt`／`main.py`／`research.ipynb` |
| 载体件全盘扫描命中 | 仅 `/QuantConnect/research-cloud/airlock/` 下；`/home`／`/root`／`/tmp` 均 **0 命中** |
| 三态 | **F0** |

**通道形态（事实链完整）**：本地 `entity/` 件 → `files.create`（file API，A 类）上云 → **云端项目文件流入 research 内核 cwd** → cwd 在 `sys.path` 上 → 直接 `import` → 载体件 `restore()` 还原并自核 SHA256 → 4/4 命中锚值。<br>**入向贯通。** 出向另计：F2-09 载 airlock 写入**不回流**（本发未复测该面）。

**α-1 三候选之覆盖状态**：(i) `.py` 内嵌常量 ＝ **实测贯通**；(ii) `object-store set` 后算法侧读取 ＝ 本发未测（E1-12 载 `get` 被 Institutional 闸拒）；(iii) 自定义数据类型 `AddData` ＝ 本发未测（属回测运行位面）。**未测项显式登记，不以已测项覆盖未测项。**

### FR-P2-11 · 本位遮蔽算子过度触发，吞掉 SHA-256 证据（本位仪器缺陷，已修）

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1，本位仪器面，非环境摩擦）** |
| **形态描述** | 本位 `scrub()` 内含兜底正则 `\b[0-9a-f]{40,}\b → <REDACTED-HEX>`，本意防凭据残留。首轮 α-6-乙 成功执行后，`produced_sha256`／`normalized_sha256`／`expected_sha256` **三项全被该正则遮蔽**——而 SHA-256 正是本步之核心证据，遮蔽后 `byte_exact=True` 沦为不可独立复核之断言。 |
| **成因** | 遮蔽算子未区分「来源未知之文本」与「本位自产、schema 已知、零凭据字段之结构化结果块」，一律施加兜底正则。 |
| **处置** | 遮蔽分两级：`scrub_secrets()`（仅替换实际 token／sessionId 值 ＋ `R-<32hex>` 模式）用于本位 runner 自产结果块；`scrub()`（前者 ＋ 兜底正则）用于未知来源文本。复跑后三项 SHA-256 完整留痕，且凭据面复扫仍零残留。 |
| **本位自陈** | 与 BUG-P2-01/02 同类，属本位工程缺陷，**非环境摩擦**，显式分列以防后续消费方误读。首轮日志（S3e）原样保留不删。 |
| **反推指向之条款** | A8'（凭据零落盘）与证据完备性之张力；乙-5 原件纪律；FR-P2-01 同族 |
| **是否需KD裁定** | 否（本位自修，登记备查） |

---

## 第五段 · X-1-乙（research 内核运行位）· DOC-25 前置素材

> **射程自守**：本段只产**素材**。**不判「是否一致」之结论、不作合格主张、不作环境可用/不可用判定**（85-9 反面条款）。**DOC-25 兑现点不变更**（85-6 A11）。总体判定归 KD。

### 5.1 执行面（F0）

| 项 | 值 |
|---|---|
| 运行位 | QC research 内核（α-6-乙 同一位） |
| fixture | `X1_SYNTH_ES_001`（第一发已钉定，**不新造**） |
| 引擎 | v1.3（续3 美式 CRR），`engine_sha256` 两位**同值** `d88a955e…dca682cf` |
| `x1_synth.py` 执行 | exit **0**，report 6,170 B |
| 执行形态 | 与第一发**同一份字节**之 `x1_synth.py`（逐字节搬运，零改写、零重实现），故两发口径无漂移 |
| 三态 | **F0** |

### 5.2 环境面对表

| 项 | 甲＝第一发本地基准 | 乙＝本发 QC research |
|---|---|---|
| implementation | CPython | CPython |
| python_version | **3.10.11** | **3.11.14** |
| python_compiler | **MSC v.1929 64 bit (AMD64)** | **GCC 11.2.0** |
| platform | Windows-10-10.0.26200-SP0 | Linux-5.15.0-187-generic-x86_64-with-glibc2.35 |
| machine | AMD64 | x86_64 |
| libc_ver | `['', '']` | `['glibc', '2.35']` |
| **libm digest** | `587fbd8f98c8ccdde681f0e0a869c80fc9efd206f5ed3d5c4be423d074f16319` | `6494f0068d8f5f1da5da22964d9f842d8672358436eca3b72ea9e1f6580bf789` |
| digest 比对 | **异** | |

### 5.3 libm 探针逐条（IEEE754 hex）· 同 5 / 异 2

| 探针 | 甲 | 乙 | 比对 |
|---|---|---|---|
| `exp(1)` | `0x1.5bf0a8b145769p+1` | 同 | 同 |
| `exp(-0.5)` | `0x1.368b2fc6f960ap-1` | 同 | 同 |
| `log(2)` | `0x1.62e42fefa39efp-1` | 同 | 同 |
| `sqrt(2)` | `0x1.6a09e667f3bcdp+0` | 同 | 同 |
| `pow(1.0000001,500)` | `0x1.000346e1b915ap+0` | 同 | 同 |
| **`erf(0.7)`** | `0x1.5b08c21171645p-1` | `0x1.5b08c21171646p-1` | **异，ULP 距离 = 1** |
| **`expm1(1e-8)`** | `0x1.5798ee3fdb763p-27` | `0x1.5798ee3fdb764p-27` | **异，ULP 距离 = 1** |

### 5.4 引擎逐位输出 `per_unit` · 同 5 / 异 1

| 量 | 甲（repr / hex） | 乙（repr / hex） | 比对 |
|---|---|---|---|
| `value` | `27.247943493957006` / `0x1.b3f79398dcd23p+4` | 同 | **同** |
| `delta` | `-0.49081376310230534` / `-0x1.f697e213cdeb5p-2` | 同 | **同** |
| `gamma` | `0.00573201514840477` / `0x1.77a7419a425c3p-8` | 同 | **同** |
| `theta` | `-2424.9461527073745` / `-0x1.2f1e46e20ae64p+11` | 同 | **同** |
| `rho` | `-18.04847182317104` / `-0x1.20c68a63f4c10p+4` | 同 | **同** |
| **`vega`** | `229.2405982811605` / `0x1.ca7b2fb2aa1dcp+7` | `229.24059828114807` / `0x1.ca7b2fb2aa027p+7` | **异，ULP 距离 = 437** |

**非浮点面**：`route_id`（`american_crr_binomial`）、`computability_status`（`computed_with_open_dimensions`）、`unstated_dimensions_hit`（`['day_count_convention']`）、`lot_id`、`instrument_class`、`exercise_style` —— **逐项同**。

### 5.5 求法形态（自 `entity/engine.py` 源码读出，**事实非推测**）

| 量 | 求法 | 源码位 |
|---|---|---|
| `delta`／`gamma`／`theta` | **格点原生**（spot-bump 差分经 KD 裁定 D-1 **禁用**） | `engine.py:72-73, 401, 437-438` |
| **`vega`** | **声明步长中心差分**（`_val(vol=sig+BUMP) - _val(vol=sig-BUMP)`） | `engine.py:451` |
| **`rho`** | **声明步长中心差分**（`_val(rate=r+BUMP) - _val(rate=r-BUMP)`） | `engine.py:453` |

**据此可排除之解释（重要，防误读）**：「差分求法放大 libm 差异，故差分量不一致」——**该解释与观测不符**。`rho` **同样走中心差分**，两位却**逐位相同**；差异**只落在 `vega` 一量**。故「差分 vs 格点原生」**不足以解释本观测**。

**成因未定（如实登记，不推测）**：本发**未**探明 `vega` 单量差异之成因。判明所需之动作＝对 vol-bump 路径上实际调用之 libm 函数与其具体实参逐点取位比对（本发 7 探针未覆盖该组实参）。该动作**不在作业指令射程内**，本位**不自行扩张**。

### 5.6 呈 KD 登记

| # | 事项 | 归属 |
|---|---|---|
| KD-P2-08 | X-1 素材已产出（研究位）：libm digest 异、7 探针中 2 项各差 1 ULP、引擎 6 量中 `vega` 差 437 ULP，余 5 量逐位相同。**素材之解读与 DOC-25 后续处置归 KD**，本位不判 | KD |
| KD-P2-09 | `vega` 单量差异之成因判明是否立项（需 vol-bump 路径 libm 实参逐点取位比对，超本发射程） | KD |

**比对件落位**：`verify/x1_compare_report.txt`（全量逐字段）；素材原件 `probe/raw/x1_research_20260818T151232Z.json`。

---

## 第五段之二 · KD-P2-10 · A'-6 与 A'-9 冲突之裁定（20260818）

### 冲突形态（事实）

`A'-6`（规格件 §4.5 授权代定）＝「QC 接口面代码（算法/notebook 侧）**全部落 `probe/`**」。
`A'-9`（规格件 §4.1 授权代定）＝「云端项目**仅承载 `entity/` 面**；`probe/` 面一律不上云」。

发回测必须云端在场 `main.py` 且含 `QCAlgorithm` 子类 ＝ 接口面代码 → 按 A'-6 落 `probe/` → 按 A'-9 不上云。**直接对撞。**

### 冲突之来源（本位反推，非新裁）

A'-9 之推得依据原文将 `probe/` 内容枚举为「**外部环境响应载荷**（API 返回、账户元数据、样本数据清单、错误原文）」，据此推出「此类内容无一需要上云」。而 **A'-6（同版同批代定）恰将接口面代码置入 `probe/`**。故 A'-9 之枚举**在 A'-6 落定时即已不成立**——二者对「`probe/` 装什么」采用了互不相容之模型。**此为两条代定间之既存不自洽，非本发新增冲突**；须至回测位方显形。

### KD 裁定（20260818）

> KD 原话：「采纳你的建议。」（对本位所呈候选 (甲)）

**落定内容**：A'-9 之云端承载面由「**仅 entity/ 面**」微调为「**entity/ 面 ＋ `probe/` 之接口面代码子集**」。**A'-6 不动**（接口面代码仍落 `probe/`，防护面不缩，85-7 不受影响）。

**保留之立法目的（未松动）**：A'-9 立法目的＝**真实/外部数据载荷零上云**。接口面代码为人造件、零真实数值、零外部数据载荷，其上云不触及该目的。API 返回、账户元数据、错误原文、`data_manifest` 等**照旧零上云**。

**检测位（机械，替代原 A'-9 检测位）**：

1. 云端文件清单 ∩ {本地 `probe/` 内**非接口面代码**文件名集} ＝ **∅**
2. 上云之接口面件须满足：**零真实数值 ∧ 零外部数据载荷 ∧ 零凭据字面**（机械扫）
3. `entity/` 面维持 A4' **零 `QuantConnect` 字面**（现已实测五件全 0）
4. 云端两类面须**机械可分**

**检测位 4 之实现调整（本位技术面，非条款面）**：本位原呈「上云接口面件加文件名前缀」。实测约束：QC 回测入口按 `main.py` 约定定位算法类，改名有致入口不可达之风险。故改以**件内显式声明标记**承载：接口面件首部置 `_P2P_FACE = "interface"`，`entity/` 面件置 `_P2P_FACE = "entity"`，并随 `data_manifest` 登记。机械可分性不降，仅承载位由文件名改为件内常量。**该调整为工程实现形态，不改裁定内容。**

### 出局候选留痕（保留供推翻用）

| 候选 | 出局理由 |
|---|---|
| (乙) 接口面代码改落 `entity/`，A'-6 退回 (ii)/(iii) | 撞 85-7「防护面不缩」；且 `entity/` 立现 `QuantConnect` 字面 → **A4' 断言当场 FAIL** |
| (丙) 另立第三类子树承载云端接口面 | 新立法（界线②）；与 D-1「目录物理三分」字面冲突（同 §4.1 候选 (b) 出局理由） |
| (丁) 不发回测，回测位记 F2 结案 | 零改动，但 α-6-甲／X-1-甲／α-1 三候选全部零产出；KD 补充裁定②（两位并跑）只剩一位；且规格件 §3.2 定 X-1 运行位之推得依据即「回测运行时＝Phase B 执行运行时，唯一直接对答 DOC-25 所问者」，research 位明载「所测非 Phase B 运行时」 |

### 程序面留痕

A'-6 与 A'-9 **均为授权代定，非 KD 裁决**，二者皆挂「一词推翻通道常开」。故本裁定系行使代定件自带之推翻通道，**非推翻既有 KD 裁决**。

A'-9 之即停条件（「若出现『必须上云方能推进』之 probe 面内容，即停呈 KD，**不得就地扩大射程**」）**按设计生效**：本位停于该点、呈 KD、未自行扩张射程。

---

## 第七段 · 回测运行位（两次回测，KD 20260818 放行）

**配额留痕**：本发共发回测 **2 次**（`researchGuide.backtestCount` 由 1 → 2）。第一次因日志通道受限致两项缺失，第二次经 KD 选路径 2 改回传通道后补齐。**配额消耗不可撤销**（E1-17）。

| # | backtestId | 目的 | 结果 |
|---|---|---|---|
| 1 | `64c64125a2ee96a7ea50dc14f38add25` | 六探测项，日志通道回传 | 4 项完整／1 项残片／1 项缺失（日志配额耗尽） |
| 2 | `a0013dfac99cda0307c6661f6bce5d88` | 补两缺口，`runtimeStatistics` 通道 | **全项到位** |

### FR-P2-12 · free 档日志配额 **10 KB/回测 且 10 KB/天** —— 规格件 E1 映射表内无此项

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F2，日志通道）** |
| **形态描述** | 回测 1 之日志末行截断告示逐字：<br>`You currently have a maximum of 10kb of log data per backtest, and 10kb total max per day. Have you tried step-by-step debugging? ... or upgrade your organization type for more logs`<br>实测：日志全量 **34 行／10,697 字节**；`X1_JIA` 期望 20 片／7,883 字符**仅到 5 片**；`A1_III_ADDDATA` **BEGIN 标未出现**（配额在 X1 中途耗尽）。<br>**「每日 10 KB」使当日重发回测亦无法取得日志**——重试不解此约束。 |
| **反推指向之条款** | α-4（断言结果之回传路径与体积）；E1-06／E1-07 回传矩阵；M7 |
| **是否需KD裁定** | 否（现象登记）；**建议登记为 E1 映射表增补候选**——归 KD |

### FR-P2-13 · `backtests/read/log` 参数面与误导性错误文案（第三例）

| 栏 | 内容 |
|---|---|
| **形态描述** | 端点必填参数经逐次报错挤出：`start` → `end` → `query`，三者缺一即拒。**且报错原文称 `end` 「It must be a valid timestamp」，实测 `start`/`end` 为行号**——追加 `end-start>200` 时返回 `Log lines can be read in pages up to 200 lines`，证其为**行号分页**而非时间戳。 |
| **归族** | 与 FR-P2-02（项目存在却报不存在）、FR-P2-05（项目已删却报成功）**同族第三例**：错误文案不指向真实约束面。 |
| **是否需KD裁定** | 并入 KD-P2-04 |

### 7.1 α-1 三候选 · 回测运行位（射程本体）

> **射程更正留痕**：规格件 §3.1 α-1 原文为「以人造确定性序列作为**回测器**输入源」。本位先前于 research 内核所测之候选 (i) 为**相邻证据**，非 α-1 本体。本节为本体结果。

| 候选 | 三态 | 实测 |
|---|---|---|
| **(i)** `.py` 内嵌常量 | **F0** | 三载体件 `cloud_payload_engine`／`gl06`／`x1` 均 `import: ok`；五件还原后 SHA256 **全部命中锚值** |
| **(ii)** `object-store set` 后算法侧读取 | **F2 硬阻断** | 错误原文逐字：`InvalidOperationException: LocalObjectStore.ContainsKey(): The current user does not have permission to read from the organization Object Store. Please contact your organization administrator to request permission.`<br>`enumerated_keys` 返回 **空数组**（不报错、不给内容）。 |
| **(iii)** 自定义数据类型 `AddData` | **F0** | `add_data.ok = true`，symbol ＝ `P2PSYNTH.P2PSyntheticData`；temp 文件写入 62 B 成功；数据源＝**自造确定性序列**（3 行人造常量），零市场数据、零 vendor 格式 |

**(ii) 之射程价值（重要）**：规格件 **E1-12 明载「回测 job 侧读权限未测」**——本发**已测，结果为拒**。<br>**文案差异并置（不作归因）**：CLI 层／库层导出面之拒绝文案为「data licensing restrictions... **Institutional** accounts」（E1-12／F2-08）；本次算法侧读取之拒绝文案为「does not have permission to read from the organization Object Store... contact your **organization administrator**」。**两处文案不同，效果同为拒。是否同一道闸，本位不判。**

**(ii) 零遗留物声明**：本项**零 ObjectStore 写入**（`write_attempted: false`）。读取对象＝探测期既有遗留物 `tr2probe-e14-marker.txt`（A'-12 不清理），故**未产生任何新遗留物**。

### 7.2 α-6-甲 · 回测运行位（双值并列，KD-P2-06 口径）

| 项 | 值 |
|---|---|
| `exec_mode` | **subprocess**（未走进程内回退——回测位 `sys.executable` 可用） |
| `harness_exit_code` | 0 |
| **`byte_exact`** | **True** |
| **`byte_exact_normalized`** | **True** |
| `newline_profile` | produced `{CR:0, LF:149, CRLF:0}` ＝ expected |
| sha256（三值同一） | `722f00ee0c4dbceb43ec4d72eb2c86cfbfddb0b5f879aa4f709124589cab7e44` ＝ 锚值 |
| 三态 | **F0** |

**与 α-6-乙 之关系**：两运行位结果**一致**，且两位 `exec_mode` 均为 `subprocess`（同形态，可比）。**禁作面重申**：不得表述为 GL-06 任何层级之合格主张。

### 7.3 X-1 三位比对素材（甲＝第一发本地 ／ 乙＝research 内核 ／ 丙＝回测运行时）

**环境面**

| 项 | 甲 | 乙 | 丙 | 乙丙同 |
|---|---|---|---|---|
| python | 3.10.11 | 3.11.14 | 3.11.14 | **是** |
| compiler | MSC v.1929 (AMD64) | GCC 11.2.0 | GCC 11.2.0 | **是** |
| platform | Windows-10-10.0.26200 | Linux-**5.15.0-187**-generic | Linux-**6.8.0-52**-generic | **否（kernel 版本不同）** |
| libc | `['','']` | glibc 2.35 | glibc 2.35 | **是** |
| **libm digest** | `587fbd8f…f16319` | `6494f006…0bf789` | `6494f006…0bf789` | **是** |
| engine_sha256 | `d88a955e…dca682cf` | 同左 | 同左 | **是（三位同值）** |

**libm 探针 7 条**

| 探针 | 甲 vs 乙 | 乙 vs 丙 |
|---|---|---|
| `erf(0.7)` | **异，1 ULP** | 同 |
| `expm1(1e-8)` | **异，1 ULP** | 同 |
| `exp(1)`／`exp(-0.5)`／`log(2)`／`sqrt(2)`／`pow(1.0000001,500)` | 同 | 同 |
| **digest** | **异** | **同** |

**引擎逐位输出 6 量**

| 量 | 甲乙 | 乙丙 | 甲丙 |
|---|---|---|---|
| `value`／`delta`／`gamma`／`theta`／`rho` | 同 | 同 | 同 |
| **`vega`** | **异，437 ULP** | **同** | **异，437 ULP** |

**路由与状态**：`route_id`／`computability_status`／`unstated_dimensions_hit` **三位全同**。

**KD 补充裁定②③（候选丙＝两位并跑）之兑现**：规格件 §3.2 曾判候选丙之增量问题（「回测运行时与 research 运行时是否同一 libm」）为射程扩张而不取，作业指令 KD 补充裁定②将其取回。**本发已产出该问题之直接素材**：乙丙 libm digest 同值、7 探针逐条同、引擎 6 量逐位全同——**两云端运行位于本 fixture 上浮点行为一致，尽管 kernel 版本不同**。差异全部落在「Windows/MSC ↔ Linux/glibc」一条边界上，不在两云端位之间。

**射程自守**：以上为**素材**。不判「是否一致」之结论、不作合格主张、不作环境可用性判定；**DOC-25 兑现点不变更**（85-6 A11）。`vega` 单量差异之成因仍未定（见 §5.5，KD-P2-09）。

### 7.4 α-4 材料 · 逐路径断言之工程形态开销

| 面 | 实测 |
|---|---|
| **断言脚本置于何处** | 三处并存：本地 `verify/`（比对与复算）／云端 `entity/` 面纯人造 harness（被测逻辑）／云端 `probe/` 接口面 `main.py`（调度与回传）。**三处分工可成立**。 |
| **回传路径 · 日志通道** | free 档 **10 KB/回测且 10 KB/天**（FR-P2-12）。单片 400 字符；实测 34 行／10,697 B 即截断。**当日耗尽后重发无效。** |
| **回传路径 · `runtimeStatistics` 通道** | 经 `SetRuntimeStatistic` 写入、`backtests/read` 取回（**A 类端点**）。实测：**P2P_\* 键 24 个／载荷 4,955 B／日志配额消耗 0**；单值长度探针 100／200／400／800／**1600** 字符写入后**全部完整回读，无截断**。 |
| **两通道对照** | `runtimeStatistics` 在三面优于日志：不吃日志配额、单值容量大一量级、落 A 类端点（日志端点为 B 类且参数面误导，FR-P2-13）。**事实并置，不作推荐。** |
| **单次往返墙钟** | 回测 1：编译 ~4 s ＋ 回测 **212 s**；回测 2：编译 ~4 s ＋ 回测 **39 s**。（两次算法体不同，耗时不可直接相比。） |
| **失败时之诊断可得性** | `hasInitializeError`／`error`／`stacktrace`／`status` 四字段在场且可读；编译警告全量回传（13 行）；逐条 try/except 隔离 ＋ `traceback` 截断入结果。**诊断面可得。** |
| 三态 | **F0（通道在场）＋F2（日志通道配额面）** |

### 7.5 α-5 材料 · 证伪器语义之可承载性

**问题（规格件 §3.1）**：「在不采集收益／夏普／回撤类指标的前提下，能否只取回约束违反计数与账本量」。

**产出面（QC 无条件生成，实测不可关闭）**：

| 载体 | 键数 | 构成 |
|---|---|---|
| `statistics` | **27** | **收益/风险类 22 键**（Sharpe／Sortino／Drawdown／Alpha／Beta／PSR／Treynor／Information Ratio／Win Rate／Expectancy 等）；**非收益类仅 5 键**（`Start Equity`／`End Equity`／`Total Fees`／`Total Orders`／`Portfolio Turnover`） |
| `runtimeStatistics`（QC 自带） | 8 | `Equity`／`Fees`／`Holdings`／`Net Profit`／`PSR`／`Return`／`Unrealized`／`Volume` |
| `charts` | 8 | `Strategy Equity`／`Benchmark`／`Drawdown`／`Capacity`／`Exposure`／`Portfolio Margin`／`Portfolio Turnover`／`Assets Sales Volume` |
| `rollingWindow` | 4 | M1／M3／M6／M12 |
| `totalPerformance` | 3 | `closedTrades`／`portfolioStatistics`／`tradeStatistics` |

**取回面（本发实测）**：`SetRuntimeStatistic` 可注入**自定义键**，与 QC 自带键同处 `runtimeStatistics` dict 内，**按键名前缀（`P2P_`）机械可分**；本发即以此取回 24 键／4,955 B 之自定义载荷，**零收益类指标进入本位产物**。

**判据两半之落定（事实，不作结论）**：

- α-5「撞到」判据前半＝「环境**强制产出**收益类指标」→ **成立**（27 键中 22 键为收益/风险类，无关闭开关）。
- α-5「撞到」判据后半＝「**且无法只取账本量**」→ **不成立**（自定义键机械可分，取回面可只取账本量与自定义计数）。
- 故 α-5 之两半**分裂**：**产出面撞到、取回面未撞到**。本位**不合并判定**，逐半分列。

**禁作面自守**：本位全程**零采集**收益／夏普／回撤类指标；上表所列 QC 自带键系环境无条件生成之事实登记，**非本位采集对象**，不入本位任何比对或判定。

### 7.7 α-2 · 路径系综之规模与遍历（回测 3／4）

**注入面（A2SET）**：同一回测项目内注册 **3 条指定路径**（自造确定性序列，各 4 行钉定数值，非随机、非市场历史）：

| 路径 | 注册 | 行数 | symbol |
|---|---|---|---|
| `P2PATH1` | ok | 4 | `P2PATH1.P2PSyntheticData` |
| `P2PATH2` | ok | 4 | `P2PATH2.P2PSyntheticData` |
| `P2PATH3` | ok | 4 | `P2PATH3.P2PSyntheticData` |

**遍历面（A2RUN）**：

```
per_path_points : {"P2PATH1.P2PSyntheticData": 4,
                   "P2PATH2.P2PSyntheticData": 4,
                   "P2PATH3.P2PSyntheticData": 4}
paths_seen      : 3
```

**三态 ＝ F0（未撞到）**。α-2「未撞到」判据原文＝「N 条路径于**一次或可承受次数内**完成且结果**可分辨**」：

- **N≥2 指定路径可在同一回测项目内驱动** —— 3/3 注册成功；
- **逐条结果可分辨取回** —— 按 symbol 分别计数，各 4 点，无混淆；
- **配额消耗 ＝ 1 次回测承载 3 条路径**（非 N 次独立回测）。

**「撞到」判据之三个分支均不成立**：非只能单路径；非须 N 次独立回测；路径可指定（非随机）。

### FR-P2-14 · `AddData` 隐式创建 QC 内部 universe 订阅，其 `GetSource` 走同一自定义数据类

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1）** |
| **形态描述（直接证据，非推测）** | `GetSource` 实际收到之 symbol 字面序列（本位埋点采集，`getsource_seen`）：<br>①`["QC-UNIVERSE-USERDEFINED-USA-BASE", "qc-universe-userdefined-usa-Base 2S"]` ← **首次调用即为 QC 内部 universe symbol**<br>②起 `["P2PATH1","P2PATH1.P2PSyntheticData"]`、`P2PATH2`、`P2PATH3` 方为本位注册之路径<br>→ **`AddData` 会隐式创建一个 QC 内部 userdefined-universe 订阅，且该订阅之 `GetSource` 调用落在同一个自定义数据类上**，其 symbol 不在使用者注册之键集内。 |
| **后果** | 凡以「symbol → 源文件」映射实现 `GetSource` 者，**必然遇到一个映射外的 symbol**。若该分支返回 `None`，即触发 FR-P2-15。 |
| **反推指向之条款** | α-1 候选(iii)／α-2；E1-14 端点与行为面之未文档化 |
| **是否需KD裁定** | 否（现象登记）；**建议登记为 E1 映射表增补候选** |

### FR-P2-15 · 空数据源之报错位置指向数据权限管理器（误导性诊断第四例）

| 栏 | 内容 |
|---|---|
| **形态描述** | `SubscriptionDataSource(None, LocalFile)` 触发之错误原文：<br>`During the algorithm initialization, the following exception has occurred: Value cannot be null. (Parameter 'input') in **CloudDataPermissionManager.cs:line 92**`<br>→ 报错位置指向**数据权限管理器**，而真实成因为**源路径为 null**。二者无关。 |
| **归族** | 与 FR-P2-02（项目存在却报不存在）、FR-P2-05（项目已删却报成功）、FR-P2-13（行号参数报「须为时间戳」）**同族第四例**：诊断信息不指向真实约束面。 |
| **代价留痕** | 该次失败消耗 **1 次回测配额**（回测 3，`backtestId=7f18c4e3087eb24da17e4da8abb75e8a`），**成因为本位缺陷 BUG-P2-06，非环境阻断**。如实分列，不计入环境摩擦。 |
| **是否需KD裁定** | 并入 KD-P2-04 |

### 7.8 α-3 · 账本算术量之表达（五量占位形态）

> **禁作面自守（规格件 §3.1）**：本节**零判据定义、零阈值、零分桶法、零路径生成器规格**。占位表达式之语义**不构成**任何量之定义。

**三类账本接口在场性（α-3 动作明列项）**

| 接口 | 在场 | 实测值 |
|---|---|---|
| `Portfolio.Values`（**持仓账本**） | **present** | 3 |
| `Portfolio.CashBook`（**现金流水**） | **present** | 1 |
| `Transactions.GetOrders()`（**成交明细**） | **present** | 0 |
| `Portfolio.TotalFees` | present | 0.0 |
| `Portfolio.Cash` | present | 100000.0 |
| `Portfolio.TotalPortfolioValue` | present | 100000.0 |

**五量占位表达（纯账本量；零定价模型量，L3 铁律2 未触碰）**

| 量（α 原文） | 占位表达式（**非定义**） | 可写出 | 产出数值 |
|---|---|---|---|
| coverage 关系 | 持仓量绝对值聚合 | ✔ | 0.0 |
| 供血净额分桶 | 现金 − 累计费用 | ✔ | 100000.0 |
| 左尾激活爬坡 | 负持仓量计数 | ✔ | 0 |
| 右尾参与缺口 | 持仓市值绝对值聚合 | ✔ | 0.0 |
| 身份在场性 | 证券标识集合基数 | ✔ | 3 |

**三态 ＝ F0（未撞到）**。α-3「未撞到」判据＝「五量之占位表达式均可在环境内写出并产出数值」→ **5/5 成立**。<br>「撞到」判据＝「任一量所需之账本接口不可得／须依赖定价模型量（违 L3 铁律2）」→ **两分支均不成立**：三类接口全在场；五式全部只用持仓量／现金流水／名义量，零 Greeks、零定价模型量。

**射程限定（须显式）**：本节只证「**接口在场且表达式可写出并产数**」。**未证、亦不主张**该五量之任何取值具有意义——数值为零持仓状态下之平凡值，其经济含义**不在本发射程内**，五量之口径归 Phase B（骨架「明文不得预写项」负面清单）。

### 7.6 本位工程缺陷分列（非环境摩擦）

| # | 现象 | 成因 | 处置 |
|---|---|---|---|
| BUG-P2-03 | 内核内联注入代码 `SyntaxError: unterminated string literal` | 本位补丁脚本转义漏一层，`\n` 写成真实换行截断字面量 | 已修，并加编译自检（`compile()`）前置 |
| BUG-P2-04 | 内核诊断字段（cwd／listdir／payload_scan）在成功路径上丢失 | `_res = R.run_alpha6()` 整体替换字典，覆盖先前诊断字段 | 已修（先拷 `_diag` 后合并） |
| BUG-P2-05 | 重组表 `LIMPROBE` 显示「重组失败」 | **解析器误判**：`P2P_LIMPROBE_100/200/400/800` 键名尾部恰合分片编号之 `\d{3}` 模式，被当作分片。**上限探针本身五档全 ok** | 登记，不影响结果 |
| **BUG-P2-06** | 回测 3 初始化失败，**消耗 1 次配额** | 本位 `GetSource` 在 `_SRCMAP` 查找落空时把 `None` 传给 `SubscriptionDataSource`。落空之直接成因见 FR-P2-14（QC 隐式 universe symbol 不在键集内）。**本位缺陷，非环境阻断** | 已修：多形候选匹配（`Symbol.Value` ／ `str(Symbol)`）＋ 兜底源文件，**永不传 None**；并埋点采集 `getsource_seen`，该埋点即 FR-P2-14 之直接证据来源 |
| BUG-P2-07 | heredoc 内 `\n` 转义被多处理一层，写坏源码（两次） | 本位经 shell heredoc 传递含反斜杠转义之补丁文本 | 改以 Edit 工具直接改文件，绕开 shell 转义；源码内需换行处改用 `chr(10)` |

---

## 第六段 · 云端承载现状（§4 云端侧）

| 项 | 值 |
|---|---|
| 云端项目 | `P2PRIME-SHOT2`，`projectId=35315786`，本 session 创建 |
| QC 自带件（非本位上传，如实登记） | `main.py` 406 chars／`research.ipynb` 1,286 chars |
| 本位上传件（仅 entity/ 面，A'-9） | `cloud_payload_engine.py` 17,374 chars／`cloud_payload_gl06.py` 16,320 chars／`cloud_runner_alpha6.py` 5,210 chars |
| 写动作实计 | `projects.create` ×1，`files.create` ×3（其中 1 件经 update 覆盖 FR-P2-07 之 1 字符残留） |
| 破坏性动作 | **零** `push`／**零** `files.delete`／**零** `object-store delete`／**零** `projects.delete` |
| A4' 自查（上传前） | 三件 `QuantConnect` 字面命中数 **全部 ==0** |

---

## 第三段 · 本发环境面并置事实

| # | 事实 | 三态 | 备注 |
|---|---|---|---|
| P-01 | `lean` CLI 1.0.228 在场 | F0 | — |
| P-02 | Docker 不在场（`command not found`，exit 127） | **F2** | 与 E1-01 一致。本发两运行位均在云端侧 |
| P-03 | 本机 Python 3.10.11，与第一发本地基准同版本 | F0 | 比对对端在场（E1-03） |
| P-05 | 探测期遗留物在场且本位零触碰（本地面） | 仅记录 | 见 FR-P2-03（云端会话面之不确定性另计） |
| P-06 | 回测节点 `BN-1b212dcdcde815afe8432ca9c9daf1f3`（B-MICRO）两轮均 `busy=false` | 仅记录 | 回测运行位可分配 |
| P-07 | `research.ipynb` 云端体积：E1-17 载 **21,875 字节**；本位 API 读回 **19,626 chars** | 仅记录 | char≠byte（UTF-8），**不足以断定文件有变动**。登记为待核项，不推测 |

---

## 第八段 · M1–M7 必答面（85-9 F0 修订：**「未撞到」须显式记录，空白 ≠ 未撞到**）

| 项 | 撞到/未撞到 | 摩擦形态描述 | 反推指向之条款 | 是否需KD裁定 |
|---|---|---|---|---|
| **M1 目录分区** | **未撞到（F0）** | 外部环境下**零平铺定位摩擦**。三分目录 `entity/`／`verify/`／`probe/` 本地建立即用，全程无路径歧义、无定位失败。**但云端侧无同构承载**——QC 云端项目为**扁平文件面**（无目录语义）＋扩展名白名单，D-1 之目录三分在云端不成立，改由 KD-P2-10 之「entity 面 ＋ 接口面子集」＋件内 `_P2P_FACE` 标记承载。**该差异属条款落地形态，非平铺定位摩擦。** 29-B① 现档＝不做，本发**不推翻**。 | 29-B① | 否 |
| **M2 七台账 symlink** | **未撞到（F0）** | 外部环境**无台账装载面之需求显形**。本发全程零台账读写：曳光弹按 85-8 **不自立撞墙条目**，缺口一律走呈 KD 登记通道，材料挂 W-25 明细；台账（待修登记／执行期裁决记录）本发**只读且未装载**。故 symlink 机制在本发**无消费方**。29-B② 之激活/废止**不由本发单独反推**（丙-4：解冻待两发合并）。 | 29-B② | 否 |
| **M3 manifest** | **撞到（F1）** | 机器可读输入清单在本发**被实际消费**，且形态被环境倒逼：①`probe/data_manifest.json` 生成并入交付（94 条目／359,968 B）；②载体件 `cloud_payload_*.py` 内嵌 `PAYLOAD` 字典即一份**机读清单**（逐件 sha256＋size），云端侧 `restore()` 以其自核；③**倒逼成因**＝E1-08 白名单 ＋ FR-P2-06 之 32,000 字符上限，迫使「原件直传」改为「清单化载体」。现档「只建 pkg manifest；build/audit 未覆盖」在本发**被突破**——manifest 在本发是**运行期强制消费物**，非仅交付附件。 | 29-B③ | 否（材料留给 29-B 反推） |
| **M4 git init** | **未撞到（F0）** | 版本留痕在外部环境**未成为实需**。本发全程零 `git` 调用；版本面由三条既有机制承载并已足够：①件名带版本号与日期；②cp-then-edit 之底版 SHA256 入版头（留痕件 v1.0→v1.1→v1.2 即此形态）；③指纹锚值对表（A5'／A9'）。**撞墙条件未出现**——未发生「需回溯某中间态而无留痕」之情形。29-B④ 与 git commit 义务顺延解除**不由本发反推**。 | 29-B④＋裁决29-B git commit 顺延 | 否 |
| **M5 三条不变量之可执行性** | **撞到（F1）** | 逐条：<br>**①会话身份纪律**——可执行且**已实证生效**：本位 ≠ P1'／P3'，全程零自审、零裁决、零总体判定；**可证形态**＝本位所有实质判断均以「呈 KD」形态登记（KD-P2-01～11），无一自裁。<br>**②fresh context 隔离**——**本发未取得可证形态**：本位为续跑会话，非 fresh；隔离性只能由「不承接既往对话摘要作为事实源」之自守声明承载，**无机械检测位**。此为摩擦：**该不变量在 agent 形态执行位上缺可证手段**。<br>**③能力档口径**——可执行且已实证：本位能力边界经 KD 20260818 明示（`00_alpha\` 以内只读自主，以外须授权），已落 `AGENTS.md` 第13条为硬自守条款；**可证形态**＝越界事实自陈（留痕件 §5.3）＋触边即报实例（KD-P2-07／KD-P2-10 两次实际停工呈裁）。 | 55-1c | **是**（②之可证手段缺失，呈 KD） |
| **M6 引擎装载核证形态** | **未撞到（F0）** | SHA256 对照**怎么做**：逐件 `sha256sum` 与《切片记录 v1.1》交付件指纹表／规格件 §0.3 锚值机械对表，5/5 逐字节一致（engine v1.3 ＋ GL-06 四件）。<br>「零写入」**怎么证**：前值落 `verify/load_fingerprint_baseline.json`（链路运行前实测），后值于封包前复测，两次实测相等即证（断言 A6'，双值入日志）。<br>**跨环境扩展形态（本发新增）**：载体件内嵌 `sha256` 并于**运行位内**自核（`restore()` 不一致即抛），使核证点由「本地对表」延伸至「远端还原点」——回测位与 research 位各自 4/4、5/5 命中锚值。**显式记录：无摩擦。** | 69扩项(i)／X-3 | 否 |
| **M7 文件交换开销** | **撞到（F1）** | 三跳＝chat↔本地↔QC 云端。逐跳实测：<br>**跳1 chat↔本地**：本发零文件经 chat 传递（材料全部本地既存）；**开销 0**。<br>**跳2 本地↔云端（上行）**：file API `files.create/update`，5 件／45,822 chars，回读 `raw=True` 逐字节保真（CR 0→0，FR-P2-09）。**但受两道闸**：扩展名白名单 `cs/py/ipynb/html/css`（E1-08）＋单件 **32,000 字符**（FR-P2-06）——二者叠乘迫使载体变换升 `zlib+base64`（v2）。<br>**跳3 云端↔本地（下行）**：日志通道 **10 KB/回测且 10 KB/天**（FR-P2-12，硬阻断，当日耗尽即零）；`runtimeStatistics` 通道 24 键／4,955 B／单值 ≥1600 字符无截断／**零日志配额消耗**（A 类端点）。<br>**墙钟**：编译 ~4 s；回测 212 s／39 s／36 s；research 内核往返 <60 s。<br>**同步面**：项目文件流入 research 内核为**一次性**（会话/内核启动时），会话起后上传之件不流入——须内核内联注入绕行。 | §7 三方分工（本发＝chat↔本地↔QC 云端，含 file API 往返） | 否 |

**M1–M7 与 29-B 之关系**：本发材料与第一发合并后方构成 29-B①–④ 激活/废止之反推依据（丙-4：解冻待两发合并）。**本发不预判 29-B 任一项之激活或废止**（负空间申报第 5 项）。

---

## 第九段 · 条款开工核对项检测位落位（裁决87 落地去向 #5，五句逐条）

| 句 | 检测位 | 执行结果 |
|---|---|---|
| **乙-4**（凭据值不经聊天/口述/截图传递；已发生即作废重生成） | ①凭据唯一路径＝运行时既存登录态，禁写入任何文件/交付件/日志（断言 A8'）；②开工前置＝已传递之凭据须先作废重生成 | ①**PASS**——全工作树凭据**值**面命中 0，遮蔽后残留复扫 0（基面声明见断言日志 BASIS，呈 KD-P2-11）；本位全程未读取 `~/.lean/credentials` 内容、未回显、未转述。②**KD 20260818 授权豁免**——两笔（乙-4-①/②）均**未执行且不再要求执行**；豁免非满足，留痕见《开工前置核对留痕 v1.2》§2.0。**续跑期零新增明文传递事件**（§9④未触发）。 |
| **乙-5-a**（下载成功即落原件再解析） | 凡回传所得载荷先落原件（bytes 直写）并入 manifest，解析在原件之后且不改原件 | **PASS（附条款冲突留痕）**——`probe/raw/` 94 件均先落盘后解析；`backtest_logs_paged_*.txt`／`x1_research_stdout_*.txt` 为原件、其解析产物另存。**冲突（FR-P2-01）**：夹带凭据之响应上「落原件」与 A8'「凭据零落盘」直接对撞，本位按 AGENTS.md 第6条采**遮蔽后落盘**，并于每件显式写 `_probe_is_byte_exact_original: false` ＋ 遮蔽算子 ＋ 冲突指针。**消解口径归 KD（KD-P2-01）。** |
| **乙-5-b**（载荷写入带序号/时间戳不覆盖） | 输出文件名带序号或时间戳，同名覆盖命中数 ==0 | **PASS**——`probe/raw/` 全部件名带 UTC 时间戳（`YYYYMMDDTHHMMSSZ`），基线快照另带两位序号；封包前复扫**同名覆盖 0**。**一处例外已如实登记**：16 件直接落盘之件于封包前**原地补 D-3 包装**（`_probe_wrapped_post_hoc: true`，原载荷逐字保留于 `_probe_payload`）——此为**补标记**非覆盖新载荷，显式留痕不静默。 |
| **乙-6**（不建出口机械检测；声明句入模板） | 声明句入交付包 `AGENTS.md` 与记录表首部；**不建机械检测**（照裁定不加码）；打包/外发动作留 KD/KY 裁定指针 | **PASS**——声明句「打包／外发落位须 KD／KY 显式裁定」已置 `p2prime_workdir/AGENTS.md` 首节；**本位未建任何出口机械检测**（照裁定不加码）；本发**零打包外发动作**，交付形态待 KD/KY 裁定。 |
| **丙-3**（交付件任何修改后复跑断言，末次全 PASS 为封包前置） | 封包脚本前置检查：断言日志**末次**时间戳 > 交付树内任一文件 mtime，且末次结果零 FAIL | 见 §10 封包留痕 |

---

## 第十段 · 封包（§5-5，丙-3）

### 10.1 本位纪律偏离自陈（**重要，不因补齐而抹除**）

| # | 偏离 | 事实 | 处置 |
|---|---|---|---|
| DEV-P2-01 | **断言脚本全程未建、未跑** | 作业指令 §5-4 明文「**每步后**：跑 `verify/probe_assert.py`（A1'–A12'）」。本位自 §4 起至 α/X-1 全部执行完毕，**一次未跑**，亦未建该脚本。封包前方补建并首跑。 | 已补建并跑至零 FAIL。**但「每步后跑」之纪律本发未执行**——该纪律之设计目的（早发现、早止损）本发未兑现。如实登记，归 KD。 |
| DEV-P2-02 | **`data_manifest.json` 迟至封包前方生成** | §8 交付件清单与 D-2 均要求该件；乙-5 要求「载荷落原件**并入 manifest**」。实际为**先落 94 件原件、封包前一次性生成 manifest**，非随落随入。 | 已生成（94 条目／374,375 B）。**「随落随入」之形态本发未执行**，如实登记。 |
| DEV-P2-03 | **16 件 raw 缺 D-3 标记，封包前补包装** | 该 16 件为直接落盘之外部响应原件，未带 `_data_source`。 | 封包前原地补包装，`_probe_wrapped_post_hoc: true`，原载荷逐字保留于 `_probe_payload`。**补标记非覆盖新载荷**，显式留痕。 |
| DEV-P2-04 | **四件放置面订正** | `backtest_probe_reassembled.{json,log}`／`x1_jia_partial{,_salvage}.{txt,log}` 原落 `verify/`，实为**外部环境响应之重组/救出产物**，按 D-1 属 `probe/` 面。 | 已移至 `probe/derived/` 并入 manifest。**订正理由＝放置面本身错，非为使断言通过**——移动后 A2' 之主判据（黑名单哈希渗透）本就为 0，未变。 |

### 10.2 FR-P2-16 · A2' 字面口径与 D-3 强制取值之条款冲突

| 栏 | 内容 |
|---|---|
| **撞到/未撞到** | **撞到（F1，条款面）** |
| **形态描述** | A2' 要求「`_pro`+`be_` 于 `entity/`∪`verify/` 命中数 **==0**」；D-3 强制 `_data_source` 取值 `external` + `_probe_` + `NOT_FOR_ENTITY`，**该值本身含该前缀**。任何于 `verify/` 实现 D-3 检测之脚本**必然含此字面** ⇒ **原条款在 verify/ 面不可满足**。<br>同型第二面：记录件须描述该标记机制本身（字段名），断言日志须回显命中内容——若禁含，则**记录件无法自述其机制、断言无法自证其判定**。 |
| **本位处置（保守，非裁定）** | 判定基面显式声明、非空、逐条给理由、**射程严限**（形态照 F-1 检测器自排除条款）：①排除 D-3 **唯一**强制字面；②排除**记录件与断言日志**之描述性提及（仅此二类件）；③一切 `.json`／`.log` **数据产物照计**；④**黑名单哈希检查（渗透之主判据）不排除任何件**——实测 89 条零渗透。基面全文入 `verify/assert_log.txt` 首节。 |
| **是否需KD裁定** | **是（KD-P2-11）** |

### 10.3 A8' 名/值两读之口径（并入 KD-P2-11）

A8' 原文「token／session id／支付卡**字段字面**命中数 ==0」——「字段字面」在**字段名**与**字段值**间两读并存。实测命中全部为**名面**（`AGENTS.md` 之遮蔽清单、脚本之 `sessionId len=%d` 标签、记录件叙述），**零实际凭据值**。本位取**值面**（合乙-4 立法目的＝凭据**值**不落盘），并显式声明基面。**两读孰是归 KD。**

**残留复扫之保守化**：**不采「64 位十六进制即放行」之宽泛豁免**——QC API token 亦为 64 位十六进制。判定位改为「是否命中**本位自产哈希全集**（锚值 ∪ manifest ∪ 基线 ∪ `probe/raw` 内一切 `sha256` 值字段，共 110 个）或其子串」，逐个机械核对。

### 10.4 断言末次执行结果（丙-3 封包前置）

**A1'–A12' 全部执行，PASS = 12 ／ FAIL = 0 ／ INDETERMINATE = 0。**

| 断言 | 判定 | 要点 |
|---|---|---|
| A1' | PASS | 三分目录在场，顶层无第四实体目录 |
| A2' | PASS | 黑名单 89 条零渗透；前缀命中 0（基面见 10.2） |
| A3' | PASS | `probe/raw` 全部 56 件带 D-3 标记 |
| A4' | PASS | `entity/` forbidden 关键字命中 0（剥离注释/docstring 后判定）；检测器自排除清单非空且仅限自身 |
| A5' | PASS | `engine.py` sha256 == 锚值；本发零 `lean cloud pull`，**CRLF 双值面无对象**故未触发 INDETERMINATE |
| A6' | PASS | 链路前后 5 件指纹两次实测相等（零写入可证） |
| A7' | PASS | 破坏性动作**调用面**命中 0；注释内之条款禁语提及 4 件不计入 |
| A8' | PASS | 凭据**值**面命中 0；残留复扫 0（110 个自产哈希入核） |
| A9' | PASS | GL-06 四件命中交付件指纹表 |
| A10' | PASS | 记录表覆盖 M1–M7 ＋ α 六面 ＋ X-1；**空栏残留 0** |
| A11' | PASS | 五句检测位逐条在场且结果入日志 |
| A12' | PASS | 12 条断言逐条带条款指针，双向无孤项 |

**INDETERMINATE 面声明**：作业指令 §6 限该态仅用于 A5' 之 CRLF 双值并列。本发**零 `lean cloud pull` 调用**（上云与回读一律走 file API，往返实测逐字节保真 CR 0→0），故**无云端侧落盘副本**，该态**无对象**，本发 INDETERMINATE 计数为 0 —— **非「未记录」，是「无对象」**。

### 10.5 乙-6 落位声明（封包不等于外发）

> **打包／外发落位须 KD／KY 显式裁定。**

本位**未执行任何打包或外发动作**。§5-5 之「封包」在本发落为：**断言复跑至零 FAIL ＋ 交付树就位 ＋ 本节留痕**。交付形态（是否压包、落于何处、是否外发）**归 KD／KY**，本位不代行、不预设。

---

## 呈 KD 事项登记栏（85-8；不自立撞墙条目）

| # | 事项 | 归属 |
|---|---|---|
| KD-P2-01 | FR-P2-01：乙-5 原件纪律与 A8' 凭据零落盘之冲突消解口径 | KD |
| KD-P2-02 | FR-P2-03①：本位 `research/read` 调用是否已越 A'-12「遗留物不清」射程 | KD |
| KD-P2-03 | FR-P2-03②：research 位是否继续探（继续即再次分配节点，同型不确定性复现） | KD |
| KD-P2-04 | FR-P2-02：`research/read` 错误文案与已证事实矛盾，是否与第一发摩擦-06 同型 | KD／P3' |
| KD-P2-05 | 越界事实自陈（见《P2prime_开工前置核对留痕 v1.2》§5.3） | KD |
| KD-P2-06 | α-6 判据双值口径 | **已裁：采纳**（20260818） |
| KD-P2-07 | research 位推进路径 | **已裁：选路径乙**（网页端起会话，不调 `research/create` 冷启动） |
| KD-P2-08 | X-1 三位素材之解读与 DOC-25 后续处置 | KD |
| KD-P2-09 | `vega` 单量 437 ULP 差异之成因判明是否立项（超本发射程） | KD |
| KD-P2-10 | A'-6 与 A'-9 冲突 | **已裁：采纳候选(甲)**（云端承载面＝entity 面 ＋ probe 之接口面代码子集） |
| **KD-P2-11** | **断言判定基面三项口径**：①A2' 字面口径 vs D-3 强制取值之不可满足（FR-P2-16）；②A8' 「字段字面」名/值两读；③`__pycache__` 排除（house 先例 GL-06 F-7） | **KD** |
| KD-P2-12 | 本位纪律偏离四项（DEV-P2-01～04）是否升为纪律信号、是否编号落账 | KD |
| KD-P2-13 | E1 映射表增补候选四项：FR-P2-06（32,000 字符上限）／FR-P2-12（日志 10 KB 双限）／FR-P2-14（`AddData` 隐式 universe 订阅）／FR-P2-15 及同族（误导性诊断四例） | KD |

**登记纪律**：以上**不自立撞墙条目**，走呈 KD 事项登记通道，材料挂 W-25 明细（85-8）。是否登记、编号、挂靠位均归 KD。

---

## 反面条款自守

本件**不产出**「成功／失败」二值结论，**不作**「环境可用／不可用」总体判定。**零 L3 判据、零 L3 实体、零合格判定。** 总体判定与后续决策归 KD。
