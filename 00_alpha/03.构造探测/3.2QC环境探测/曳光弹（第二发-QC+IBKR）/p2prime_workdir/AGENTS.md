# 执行侧行为约束（AGENTS.md／CLAUDE.md，P2' 工作目录根，agent 自动读取）
# 权威指针＝P1'规格件_曳光弹第二发QC_v1_2_定案_20260818（唯一权威）
# 执行序索引＝P2prime_作业指令_曳光弹第二发QC_v1_0_20260818
# 闸位留痕＝P2prime_开工前置核对留痕_v1_1_20260818（§3 闸经 KD 20260818 授权豁免而开）

## 乙-6 声明句（照裁决，不加码，不建出口机械检测）

**打包／外发落位须 KD／KY 显式裁定。**

## 禁作面（违反即停；作业指令 §2 摘要三条）

1. **不产出**「成功／失败」二值结论，**不作**「环境可用／不可用」总体判定（85-9 反面条款）。**不产出**任何 L3 判据、L3 实体、合格判定；五量之取值／阈值／分桶法／路径生成器规格一律不写。
2. **破坏性动作全禁**（A'-14）：`lean cloud push`、`object-store delete`、`files/delete` 于全工作树命中数 ==0。**探测期遗留物一律不清理、不删除、不覆盖**（A'-12）。
3. **不填未给定数值**；不预设结果、不为跑通而改目标。撞到缺口即**登记并继续**，不就地定义、不静默绕过；缺口**不自立撞墙条目**，走「呈 KD 事项登记」栏（85-8）。

## 逐条约束

1. **禁止修改 `entity/engine.py`**——任何形式，含「临时改一下试试」。运行前后 SHA256 必须等于
   `d88a955e45dddaa5a11f02c9237cb5e178bdfca6335a670ecdf21414dca682cf`（断言 A5'／A6'）。
2. **禁止修改 GL-06 四件**（`entity/gl06_*`）。运行前后 SHA256 必须等于（断言 A9'）：
   - `gl06_input_schema.md`     `33fe363cfb24bdfd557b81efe8e79be2b51c82c2bfff40278b096311ee4394f1`
   - `gl06_ledger_input.json`   `31605728279ba59813bdacc705e3b8264179a1f511e63cac402ea80e90a989c7`
   - `gl06_harness.py`          `5702aae378244ad3d17f1161b769eacf0e66cdc1664c99553654f8f36e3b8747`
   - `gl06_harness_output.json` `722f00ee0c4dbceb43ec4d72eb2c86cfbfddb0b5f879aa4f709124589cab7e44`
3. **禁止为让代码跑通而填任何未在规格件中显式给定的数值。** 缺参数＝立即停止，报告缺什么；不猜、不用占位值、不用行业惯例默认值。
4. **撞到契约面问题**（输入形态不匹配、路由不通、口径疑问、闸门拒绝）→ 停下，按四栏格式写入摩擦记录，**不绕道、不做适配性猜测修补**。跑不通同样是有效产出（F1／F2 均为有效产出，非不合格）。
5. **允许自由调试的范围**＝适配层与 harness 自身的工程 bug（连接、解析、IO、类型转换）。凡涉及数值语义、engine 契约面、GL-06 schema 之改动，**不属于此范围**。
6. **凭据面**：QC user id／API token 之唯一取值路径＝`lean` CLI 既存登录态（`~/.lean/credentials`，位于本工作树**之外**）。**禁止**将凭据值写入本工作树任何代码字面量、配置文件、交付件、`data_manifest`、摩擦记录、日志（断言 A8'）。**禁止**在会话中回显或转述。API 响应夹带之凭据（`accounts.get_organization()` 之 `card{brand,expiration,last4}`、`research/read` 之 session token）落盘前须遮蔽，遮蔽后正则复查残留 ==0。
7. **目录三分**（D-1）：
   - `entity/` 人造，零真实数值：engine 件、GL-06 四件、纯人造 harness。
   - `verify/` 断言脚本、断言日志、X-1 比对件、独立复算件。
   - `probe/` **唯一**允许触碰外部数据之子树；**QC 接口面代码全部落此**（A'-6）。
   云端项目**仅承载 `entity/` 面**；`probe/` 面一律不上云（A'-9）。
   **即停条件**：若出现「必须上云方能推进」之 probe 内容，停并呈 KD，**不得就地扩射程**。
8. **键名污染标记**（D-3）：`probe/` 产出之任何字段一律加 `_probe_` 前缀；`_data_source` 强制取值 `external_probe_NOT_FOR_ENTITY`。`entity/`∪`verify/` 内 `_probe_` 命中数 ==0。
9. **载荷纪律**（乙-5）：凡下载／回传所得载荷，**先落原件**（bytes 直写）并入 manifest，解析在原件之后且不改原件；写入文件名带**序号或时间戳**，同名覆盖命中数 ==0。
10. **封包前置**（丙-3）：交付件任何修改后须复跑断言，**末次零 FAIL** 且全部 `INDETERMINATE` 项已双值留痕，方可封包。
11. **`INDETERMINATE` 态**：仅限 A5' 之 CRLF 双值并列一项；须同时记录云端侧值与本地侧值，缺一即判 FAIL。其余断言无此态。
12. **停工条件**（作业指令 §9，命中即停并呈 KD，不自行绕行）：①「必须上云方能推进」之 probe 内容出现；②任何破坏性动作成为唯一推进路径；③规格件与作业指令冲突且无法以规格件为准解决；④凭据面出现**新的**明文传递事件；⑤两运行位之一完全不可达（记 F2 并停该位，另一位继续）。

13. **访问边界**（KD 20260818 明示，本位硬自守；优先于一切便利性考虑）：
    - **只读自主面** ＝ `00_alpha\` 以内。该范围内的读取、搜索、哈希、列目录无须逐次请示。
    - **写入面** ＝ `00_alpha\9.QC环境探测\曳光弹（第二发-QC+IBKR）\` 以内（本发目录，含 `p2prime_workdir/`）。本发目录之外**零写入、零删除、零修改**。
    - **`00_alpha\` 以外**（含用户主目录 `~`、`~/.lean`、其他盘符、任何兄弟目录如 `version_alpha\`）：**一律须 KD 先行授权**，包括只读、只列名。**不得自主搜索、不得自主读取。**
    - **网络面**：任何对外请求（QC API、lean cloud 子命令、任何 HTTP）**须 KD 先行授权**，逐段授权，不因一次放行而外溢至后续。
    - 缺路径即停：材料件路径未给 → **停并索取**，不自行搜索定位（CLAUDE.md 第1条「缺参数即停」明列「路径」）。
    - **触边即报**：任何动作若需越过上述边界，停在该步、报告需要什么、等授权，不自行改走其他路径。

## 本位身份

本位＝**P2'**，外部执行位，**非** Claude 裁决台会话、**非** P1'、**非** P3'。三条不变量适用（55-1c）：会话身份纪律／fresh context 隔离／能力档口径。执行中不得兼任审计或裁决。**本位零裁决**——一切判断以「撞到／未撞到」事实记录呈报，不定性、不总结、不给建议。
