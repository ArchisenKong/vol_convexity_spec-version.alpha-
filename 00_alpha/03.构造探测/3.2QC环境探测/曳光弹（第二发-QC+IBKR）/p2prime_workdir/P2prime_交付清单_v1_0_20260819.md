# P2' 交付清单 · 曳光弹第二发（QC）· v1.0 · 20260819

> **件性质**：作业指令 §8「交付件清单」之兑现件，随交付树封包。**定稿**。
> **权威关系**：唯一权威＝《P1'规格件_曳光弹第二发QC_v1_2_定案_20260818》；执行序索引＝《P2prime_作业指令_曳光弹第二发QC_v1_0_20260818》。本件与二者冲突时**以规格件为准**。
> **本位声明**：本位＝P2'，外部执行位，非裁决台会话、非 P1'、非 P3'。**本位零裁决**——本件只列交付物与事实，**不作定性、不作总体判定、不给后续建议**。
> **乙-6 落位声明**：**打包／外发落位须 KD／KY 显式裁定。** 本次**打包**经 KD 20260819 显式指示执行；**外发未获裁定，本位零外发、零上传**。

---

## §1 本发射程与完成状态

**目标面＝两件**（规格件 v1.2：由三件收窄）：α（L3 承载环境探查）＋ X-1（QC 运行时浮点素材）。
**甲-3（IB 有效 T 重跑）经 KD 裁 A'-2＝挂起，撤出本发**，退回裁决87 落地去向 #7 之另一择（后续微探针）；本发**零执行、零预备、零材料产出**。规格件 §9 性质句：「v1.2 起单一执行位，**无 IBKR 位**」。

| 作业指令 §5 步 | 状态 |
|---|---|
| §5-1 基线快照（只读） | **完成**（A／B／C 三轮；C 轮为 KD 删除旧云端项目后之新基线） |
| §5-2 α 探查六面（α-6 两位各跑一次） | **完成** |
| §5-3 X-1 两位并跑 | **完成**（甲＝回测运行时／乙＝research 内核，另有第一发本地基准，三位比对） |
| §5-4 每步断言＋记录表 | **部分**——记录表全程在填；**断言脚本封包前方补建首跑**（DEV-P2-01，见 §5） |
| §5-5 封包 | **完成**（断言 12 PASS／0 FAIL；封包前置三项机械检测全 PASS） |
| §5-6 交付 P3' | **未执行**——交付形态归 KD／KY（乙-6） |

### 三态汇总（F0 链路贯通／F1 摩擦命中／F2 硬阻断；**F1／F2 均为有效产出，非不合格**）

| 面 | 三态 |
|---|---|
| α-1 (i) `.py` 内嵌常量注入 | **F0** |
| α-1 (ii) ObjectStore 算法侧读取 | **F2**（答规格件 E1-12 明载之「回测 job 侧读权限未测」） |
| α-1 (iii) `AddData` 自定义数据类型 | **F0** |
| α-2 路径系综规模与遍历 | **F0**（1 次回测承载 3 条指定路径，逐条可分辨） |
| α-3 五量占位与账本接口在场性 | **F0**（三类接口全在场；五式全可写出并产数） |
| α-4 断言开销与回传路径 | **F0**（通道在场）＋ **F2**（日志通道配额面） |
| α-5 证伪器语义可承载性 | **两半分列**：产出面**撞到**／取回面**未撞到**（本位不合并判定） |
| α-6 载体根 GL-06 环境内可运行性（甲乙两位） | **F0**（双值并列，KD-P2-06 口径） |
| X-1 跨环境浮点素材（三位） | **F0**（素材完整） |
| Docker 本地面 | **F2**（与 E1-01 一致；本发两运行位均在云端侧） |

**反面条款自守**：本件**不产出**「成功／失败」二值结论，**不作**「环境可用／不可用」总体判定；**零 L3 判据、零 L3 实体、零合格判定**；**零收益／夏普／回撤类指标采集**。总体判定与后续决策归 KD。

---

## §2 交付树结构与规模

落位＝`00_alpha/9.QC环境探测/曳光弹（第二发-QC+IBKR）/p2prime_workdir/`

```
p2prime_workdir/                     147 件 / 813,161 B
├─ AGENTS.md                         行为约束（乙-6 声明句＋禁作面＋13 条约束）
├─ P2prime_交付清单_v1_0_20260819.md  本件
├─ entity/    10 件 / 104,212 B       人造，零真实数值：被测件 ＋ 载体件 ＋ 纯人造 harness
├─ verify/    24 件 / 176,116 B       断言脚本／断言日志／比对件／构建件／指纹清单
└─ probe/    112 件 / 526,440 B       唯一外部数据接触面
   ├─ (根)     14 件                  QC 接口面代码（A'-6）＋ data_manifest.json
   ├─ raw/     68 件 / 317,339 B      外部环境响应原件（全部带 D-3 标记）
   ├─ logs/    26 件 /  57,036 B      全量 stdout／stderr
   └─ derived/  4 件 /  18,538 B      响应之重组／救出产物（DEV-P2-04 放置面订正）
```

**D-1 三分成立**：顶层无第四实体目录（断言 A1' PASS）。
**云端侧承载面**（KD-P2-10 裁定）＝ `entity/` 面 ＋ `probe/` 之**接口面代码子集**（唯一成员＝`main.py`，件内 `_P2P_FACE="interface"` 标记）。

---

## §3 作业指令 §8 逐项对表（七项，**全部在场**）

| # | §8 要求 | 交付物 | 大小 | SHA256 |
|---|---|---|---|---|
| 1 | 摩擦记录件（§7 全表） | `verify/friction_record.md` | 75,183 B | `abb1c63b37e57b958addc66f9633a8baf6d05237159eaf1de9cbed8b804a9d89` |
| 2 | `probe/data_manifest.json`（含 `_selection_rule`） | `probe/data_manifest.json` | 32,248 B | `b93c9521ad0ff70135803a5055b70180a81ed095d4e9960c9ff1e0ba9f093fc9` |
| 3 | 断言日志 | `verify/assert_log.json`／`.txt` | 见 §4 | 见 §4 |
| 4 | α 六面材料（α-6 两位） | `probe/raw/alpha6_research_*.json`／回测位见 `probe/raw/rt_probe_results_*.json`；α-5 另有 `verify/alpha5_statistics_analysis.log` | — | 见 `verify/SHA256SUMS` |
| 5 | X-1 素材（两位分列） | `probe/raw/x1_research_*.json`（乙）／`probe/raw/rt_probe_results_*.json`（甲）；比对件 `verify/x1_compare_report.txt`（两位）、`verify/x1_compare3_report.txt`（三位） | 4,709 B／4,360 B | `071332a1…5150e263`／`0f618559…cdbb3c26` |
| 6 | 全量 stdout／stderr 日志 | `probe/logs/` 26 件 | 57,036 B | 见 `verify/SHA256SUMS` |
| 7 | 基线快照 | `probe/raw/` 之 S1 系列（A／B／C 三轮） | — | 见 `verify/SHA256SUMS` |

**M7（文件交换三跳开销）已计入** `verify/friction_record.md` 第八段 M7 行。

---

## §4 封包前置留痕（丙-3 ＋ 作业指令 §5-5）

**断言 A1'–A12' 末次执行：PASS = 12 ／ FAIL = 0 ／ INDETERMINATE = 0。**

**`INDETERMINATE = 0` 之性质**：作业指令 §6 限该态仅用于 A5' 之 CRLF 双值并列。本发**零 `lean cloud pull` 调用**（上云与回读一律走 file API，往返实测逐字节保真 CR 0→0），无云端侧落盘副本，**该态无对象**——是「无对象」，**不是「未记录」**。

**封包前置三项机械检测（`verify/seal_check.py`）**：

| # | 检测位 | 结果 |
|---|---|---|
| ① | 末次断言 FAIL == 0 | **PASS** |
| ② | 全部 INDETERMINATE 项已双值留痕 | **PASS**（0 项，无对象） |
| ③ | 断言日志 mtime 晚于交付树内任一文件 | **PASS**（更新者 0 件） |

**指纹清单**：`verify/SHA256SUMS` 覆盖交付树全量（自排除四件自指件：`SHA256SUMS` 自身、`assert_log.json`／`.txt`、`seal_check.log`——四者均于清单生成后再变动，自指必不自洽；排除理由入清单头部）。

**断言判定基面声明**（显式、非空、逐条给理由、射程严限；形态照 F-1 检测器自排除条款）全文入 `verify/assert_log.txt` 首节，共四条：`pycache`／`d3_required_literal`／`cred_field_name`／`descriptive_mention`／`hash_fragment`。**其中三条口径呈 KD-P2-11。**

---

## §5 本位纪律偏离自陈（**四条，不因补齐而抹除**）

| # | 偏离 | 处置 |
|---|---|---|
| **DEV-P2-01** | 作业指令 §5-4 明文「**每步后**跑断言」，本位自 §4 至 α／X-1 全部执行完毕**一次未跑**，封包前方补建首跑 | 已补建并跑至零 FAIL。**但「每步后跑」之设计目的（早发现、早止损）本发未兑现** |
| **DEV-P2-02** | `data_manifest.json` 迟至封包前一次性生成，乙-5「载荷落原件并入 manifest」之**随落随入形态未执行** | 已生成（94 条目） |
| **DEV-P2-03** | 16 件 raw 缺 D-3 标记，封包前原地补包装 | `_probe_wrapped_post_hoc: true`，原载荷逐字保留于 `_probe_payload`；补标记非覆盖载荷 |
| **DEV-P2-04** | 四件放置面订正（`verify/` → `probe/derived/`） | 订正理由＝放置面本身错（外部响应之重组/救出产物属 probe 面）；**非为使断言通过**——移动前后 A2' 主判据（黑名单哈希渗透）本就为 0 |

**另有越界事实自陈**（本位于勘察阶段自主将检索面扩至 `00_alpha/` 之外及用户主目录）：见《P2prime_开工前置核对留痕 v1.2》§5.3。**越界动作全部只读；写入自始至终仅落本发目录。**

**本位工程缺陷（非环境摩擦，已分列）**：BUG-P2-01～07，见 `verify/friction_record.md` 第七段 7.6。其中 **BUG-P2-06 消耗 1 次回测配额**（回测 3 初始化失败）。

---

## §6 配额与遗留物账（不可撤销项显式登记）

| 项 | 事实 |
|---|---|
| 回测配额 | 本发共发 **4 次**（`researchGuide.backtestCount` 1→4）。其中**第 3 次因本位缺陷 BUG-P2-06 失败**，非环境阻断。**配额消耗不可撤销**（E1-17） |
| 云端项目 | `P2PRIME-SHOT2`（id 35315786）**本发新建，未删除**（`projects.delete` 未调用；A'-14 禁破坏性动作） |
| 云端项目文件 | 6 件（`entity/` 面 5 ＋ 接口面 `main.py` 1），**未删除** |
| research 内核 | 本位经 `POST /api/kernels` 创建 1 个，**未调 DELETE**，随会话闲置自回收 |
| ObjectStore | **本位零写入**（`write_attempted: false`）。探测期遗留物 `/tr2probe-e14-marker.txt`（252 B）**仍在**，本位零触碰 |
| 探测期遗留物（本地） | `9.QC环境探测/probe/`、`qc-workspace/` 本位**零触碰**（A'-12） |
| 本机桌面 `probe_E19_download.txt` | **未查**——在 `00_alpha/` 之外，按 KD 20260818 边界裁定本位不碰 |
| 破坏性动作 | `lean cloud push`／`files.delete`／`object-store delete`／`projects.delete`／`backtests.delete` **全域调用面命中 0**（断言 A7' PASS） |

---

## §7 呈 KD 事项登记（85-8；**本位不自立撞墙条目**）

共 **13 项**，其中 **3 项已裁**（KD-P2-06 双值口径采纳／KD-P2-07 research 位选路径乙／KD-P2-10 A'-6 与 A'-9 冲突采候选甲），**10 项待裁**。逐项内容见 `verify/friction_record.md`「呈 KD 事项登记栏」。

**待裁项摘要**：KD-P2-01（乙-5 原件 vs A8' 凭据零落盘之条款冲突）／KD-P2-02（本位 `research/read` 是否越 A'-12 射程）／KD-P2-03（research 位续探）／KD-P2-04（误导性诊断四例是否与第一发同型）／KD-P2-05（越界事实自陈）／KD-P2-08（X-1 素材解读与 DOC-25 后续处置）／KD-P2-09（`vega` 单量 437 ULP 差异成因是否立项）／**KD-P2-11（断言判定基面三项口径）**／KD-P2-12（纪律偏离四项是否升为纪律信号）／KD-P2-13（E1 映射表增补候选四项）。

**登记纪律**：以上**不自立撞墙条目**，走呈 KD 事项登记通道，材料挂 W-25 明细。是否登记、编号、挂靠位均归 KD。

---

## §8 本件封口

本件为 P2' 交付清单**定稿**。交付树 147 件／813,161 B，封包前置三项机械检测全 PASS。

**本位零裁决**：本件所载一切均为事实与三态记录，**无一项定性结论**。摩擦定性、两发合并归纳、29-B 解冻判定、DOC-25 后续处置，**全部归 P3' 与裁决台**（生产者不自审）。

**乙-6**：打包已依 KD 20260819 指示执行；**外发未获裁定，本位零外发**。
