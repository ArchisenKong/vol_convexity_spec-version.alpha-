## 任务3 · 抽取范围定义

**状态**：仅定义，未抽取、未画图、未选定。G2第一项（定义先经KD确认）由本产物+KD确认动作兑现；G2第二、三项本轮不适用。

---

### 1. 对象层位

**定义**：抽取节点锚定于**思想层 + 语义层**两层；bridge层与工程实现层不在抽取范围；治理层不作为节点来源（当前为空，见任务2产出§四治理层注记"当前为空"）。

**依据与对依赖方向注记的回应**（任务0/1/2/4产出§四）：
- 六份一档文档在任务1表中的引用纪律分别为"思想层源头"（QQQAI）与"语义层索引/语义核验基准"（其余五份），故一档候选池内真实存在的对象只可能落在这两层。
- 依赖注记第1条（定义依赖单向向下）：草图内跨层引用边只允许"语义层对象→引用→思想层定义"方向；若在基础资料中发现疑似反向引用，登记为上游冲突异常，不画入正常边、不静默丢弃。
- 依赖注记第2条（接口需求信息流由切片打穿时向上产出）：bridge/工程层对象须由切片逼出，当前无一阶实体存在于该两层，从文档预取该层对象即构成空演绎，故排除。

### 2. 粒度

**定义**：抽取单元 = **可独立套用任务0判据问题集（Q0–Q6两阶段遍历）并落入输出标签集某一标签的最小命名对象**。具体两界：
- 上界：不以整篇文档、整章、整节为节点——章节属组织形式（Q5"他物的组织形式"），无法作为分类单元。
- 下界：不拆到无法独立回答Q1（无独立产出指向或约束指向）的语句碎片；带取值的具名参数为合法最小单元（先例：A-2复测案例"最低保证金阈值=0.15"可完整走通遍历）。

**依据**：任务0/1/2/4产出§二判据基准+两阶段问题集+输出标签集；用户任务定义"抽取颗粒须落在能被此判据问题集分类的对象层级"。

**附加约束（裁决2）**：文档中的具名举例（convexity_supply_vega、三分类等）不预取为候选；仅当其在一档文档正文中作为真实定义对象出现且满足本粒度判据时，方按普通对象同等程序收录，不享举例身份带来的任何优先权。

### 3. 来源档位

**定义**：抽取来源**仅限一档·上游事实档六份**（QQQAI基金介绍、主题树、12篇文档集、操作方法论主文档、Greeks主文档v0.1.2、TC主文档v0.1.2），此为候选池上限，禁止超出。

**排除**（任务0/1/2/4产出§三分层表+查表规则+域外声明）：
- 二档（Taxonomy_Bridge构建手册、Greeks_与_TC Bridge）：仅方法参照，不作抽取来源，不引用其结论；
- 三档（B-2 v1.3、系统工程总纲v0.3.1）：禁用（与裁决3一致）;
- 域外四份方法论载体文档（全局.md、session整理、任务全集、承接md）：流程/血缘类，不作抽取来源（裁决A）。

### 4. 停止判据

**定义**：满足以下四条即终止抽取，进入草图绘制：

1. **覆盖终止**：六份一档文档各完成一遍全量遍历，遍历单元为§2定义的命名对象，逐份勾销。
2. **收录闭合**：每个已收录节点具备出处指针（文档名+章节/行位），可溯源——此为G2第二项的抽取时前置保证；无出处者不收录。
3. **禁膨胀**：只收文中真实命名的对象；不收派生对象、不补"应当存在"的推断对象、不因某对象重要而深挖其未命名的隐含子对象（防幽灵对象）。引用边同理：只画基础资料中真实出现的引用关系。
4. **残留登记终止**：疑似对象但无法按§2粒度判据确认者，登记为未闭合残留后即可终止，不阻塞——依据任务全集§五"任务3草图的完整性依赖后续核查、可能存在未闭合残留"。

---

**下一步**：本定义待KD确认。确认后展开全量抽取（收录清单+出处指针）→绘制横向引用草图。本轮到此停止。（KD已确认）