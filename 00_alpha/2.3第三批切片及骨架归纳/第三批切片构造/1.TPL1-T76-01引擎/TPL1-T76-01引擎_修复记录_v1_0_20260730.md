# TPL1-T76-01引擎 · 修复记录 · v1.0 · 20260730

> **会话身份**：**引擎切片修复会话**（fresh context，新session）。与切片构造会话、外审会话、任务包生成会话、裁决段会话全部分离。**修复方不自审终态——本会话不出合格判定**，终态待增量复核会话（另session）＋KD最终收口。
> **授权源**：裁决48-1（总判定(a)）＋裁决48 §3（D-6 前置口径已裁）＋裁决48 §4（修复动作范围已裁）；执行依据＝《引擎切片修复会话开启文本_v1_0_20260730》§3 动作面。
> **零裁决**：全部动作按既裁口径执行；执行中所遇疑义一律登记呈报（见 §4），不就地定义、不扩修复面。
> **形态先例**：GK-31 修复会话（裁决37/38 链）。

---

## §1 · 启动核对项五条（44-D4）逐条留痕

| # | 核对项 | 结果 |
|---|---|---|
| ① | 逐件 `-f` 在场核证＋MISS 计数 | 装载面 9 类共 12 件（含本开启文本）逐件核证，**MISS＝0** |
| ② | 文件名/文内版本一致性 | 逐件读取文内头注版本行与文件名对表，全部一致；同族目录内均为最高版本号（现行版判定，41-9） |
| ③ | 装载面核证＋2级隔离申报 | 装载面＝开启文本 §2 九类，无增无减。**2级隔离维持（44-C11）**：《六桩接口期望一致性扫描记录》**零装载、零检索**，其 §4 之 2 级句本会话零可见。**思想层宪法零装载**：契约对照义务经翻译审计表对照区承载（C4②）。外审侧过程脚本零装载——A9 组断言逻辑据外审报告 §3.1 之变异语义描述**独立重建**，未复用外审锚点常量（32-③ 口径同型） |
| ④ | 本开启文本显式引用 | 《引擎切片修复会话开启文本_v1_0_20260730》，§3 动作面逐项执行，§3.5 边界五项零动作 |
| ⑤ | 会话身份声明 | 见本文件头注 |

**装载面外之上下文申报（如实留痕）**：本会话上下文含跨会话记忆摘要（非装载件）。全部修复动作之依据一律取自 §2 所列装载件之实际文本读取；记忆摘要**未作为任何动作之依据或判据来源**，与"禁记忆断言"纪律一致。

---

## §2 · 修复动作逐项执行留痕（开启文本 §3，逐号对应）

### 2.1 A/B产物变动类

| 号 | 动作 | 执行 | 验证 |
|---|---|---|---|
| ① | **D-6 订正**（48 §3 乙口径） | `engine.py` 之 `UNSTATED_DIMENSIONS`／`COMPUTABILITY` 两表：`spot`／`spot_etf`／`index` 三条线性路由去标 `dividend_carry`、可算性升为 `closed_on_stated_face`；`futures`／`european_option`／`american_option` 维持各自命中。联动：`input_schema.md` §3 `ROUTE_DECLARATION` 块＋标注语义段、`_eval_linear` docstring、`route_table`（重跑产出）、逐笔记录（重跑产出）；闭合口径 **1/6→3/6** 三处文本（schema §3、撞墙清单 #1、切片记录呈KD#1 改写为"已收口（裁决48 §3）"）；TPL1-T7601-01 触发场景改写为经济层全域缺口表述（含线性持仓 bleed 漏分红实况） | 重跑后 `harness_output.json` 对底版差异点 **16 个，全部落在 `computability_status`／`unstated_dimensions_hit` 二字段**（route_table 2＋逐笔 2 lots×3 容器＝6，各 8 处）；**非标注类差异 0 项**，数值面零变动 |
| ② | **Q-3** route_table 值层断言 | `assert_check.py` 新增 **A3-b2**：逐路由 `route_id`／`computability_status`／`unstated_dimensions_hit` 三字段与 `ROUTE_DECLARATION` 对表（非仅键集） | SM6 同型注入（route_table 集体改标）→ exit=1，A3-b2 与 A3-d 双通道击落 |
| ③ | **Q-13** 成分5 改述 | `input_schema.md` §6 表行5 改为**类一（条件性定案）**，条件＝零持有成本占位下之恒等映射；明写 `rho=0`／`theta=0` 系该占位条件之直接后果、非对成分7 之独立处置，层次交叉消除；处置栏补"成分7 闭合时本行随之复核" | 与成分7（类二·缺定义型）登记并存无矛盾；三分判别全表其余 11 行零变动 |

### 2.2 断言扩充类

| 号 | 动作 | 执行 | 验证 |
|---|---|---|---|
| ④ | **48-2 六例声明-实现一致性断言** | `input_schema.md` 新增 **§5.1 `CONVENTION_DECLARATION`** 声明块（day count 分母／side_sign 映射／per_lot 缩放因子集／vega·theta·rho 量纲三项／industry_scaling／futures_stub_carry／恒等式适用路由二行）＋出处与断言形态对照表。`assert_check.py` 新增 **A9 组七项**，基线一律程序化解析自该块，**双层双侧**：<br>·源级：A9-a〔C1〕分母字面量、A9-b〔C4〕`scale` 表达式因子集，对 `engine.py` 与 `independent_recompute.py` 双侧提取对表；<br>·行为级：A9-1〔C1+rho〕`rho＝T·(S·δ−V)` 并由产出**反解 day count 分母**与声明值对表、A9-2〔C7〕`vega＝γ·S²·σ·T`、A9-3〔C8b〕BS-PDE 残差为零、A9-4〔C2〕`per_lot＝per_unit×m×q×side_sign` 逐分量（含 V(t)）、A9-5〔C10〕线性/期货桩 per_unit＝(spot,1,0,0,0,0)；行为级两侧分别施于 `harness_output.json` 与 `independent_expected.json` | 六例协同变异逐例注入（隔离临时目录）→ **6/6 击落**，明细见 §3 |
| — | **范围钉死** | 覆盖面限 48-2 所定六例形态；**C3（情景构造式两侧同形）未纳入**，维持归 35-A 数值比对方法固有上限 | 未新增任何针对 C3 之断言 |
| ⑤ | **D-3** 扫描面扩张 | `SCANNED` 由 4 件扩至 **5 件**（补 `verify/negative_control.py`）；A6 `audited` 由 5 件扩至 **6 件**（同补）。口径＝全部交付脚本（entity/＋verify/） | S5 同型注入（`negative_control.py` 注 `import requests`）→ exit=1，step0(ii) 击落 |
| ⑥ | **D-4** 模式表内容钉定 | 取**指纹断言**形态（二选一之后者）。`input_schema.md` 新增 **§8.1 `DETECTOR_FINGERPRINT_DECLARATION`** 声明块，钉定 `verify/scan_patterns.json` 之 sha256＝`f17de0f5544f377daba514e690aa347a6a37f5c20c12746f507f9a0555ebc74c`；`assert_check.py` 新增 **step0(ii-b)**，指纹基线**程序化解析自声明面**、非脚本内常量 | S7 同型注入（删 `http://`／`https://` 两关键词）→ exit=1，step0(ii-b) 击落 |

### 2.3 纯文本订正类

| 号 | 动作 | 执行 | 验证 |
|---|---|---|---|
| ⑦ | **D-1** 行78→行76 | 订正 **五处**（裁定枚举四处＋本会话检出第五处，见 §4-A）：`input_schema.md` §5 正文、§6 表行4、撞墙清单 #3、切片记录本根裁定登记3、切片记录呈KD事项表第3行。切片记录文末核证陈述随实况写回（明写失实陈述面、明写 v1.1 不作全称核证陈述） | 程序化重核：`grep -n "position_greek" 一阶实体_T-76_input_schema_v2_2_20260714.md` → **行76**；`sed -n '78p'` → 代码块结束符 ` ``` `。修复后全交付面 `grep -rn "行78"` 命中 **0 处**（除订正留痕之显式对照文字） |
| ⑧ | **D-5** 三例→四例 | `negative_control.py` docstring 订正 | 与实现（4 例）及日志（4/4 检出）一致 |
| ⑨ | **Q-9** 锚定区间补锚 | `input_schema.md` §2 补 `valuation_date` 权威源＝**T-76 §3 行46**，锚定面由〔§2 行17–33＋§4 行60–79〕两段改为三段 | 程序化重核：`sed -n '46p'` → `# dte = (expiry - valuation_date).days，整数天` |
| ⑩ | **Q-10** 扩类标注 | 撞墙清单四条模板一「缺口类型」栏补 **"超出定义行三类"** 标注＋前置扩类理由段（定义行三类＝缺命名／缺接口／缺模块，出处＝任务6产出v1.7 行50） | 四条逐条在场；扩类理由明写"不构成对模板一定义行之修订" |

### 2.4 实害项

| 号 | 动作 | 执行 | 验证 |
|---|---|---|---|
| ⑪ | **Q-6** 陈旧绿日志 | `assert_check.py` 日志写出时序重构：进程启动即以 `w` 模式清空日志并写运行头，`check()` 逐断言即时写出并 `flush()`，收尾仅追加汇总。清空动作置于判据基线解析**之前**，故解析失败之 `SystemExit` 亦不残留旧日志 | 实证：先跑绿日志（35 条 PASS 在场）→ 破坏声明块 tag 使基线解析失败 → exit=1，磁盘日志仅余运行头，**PASS 残留计数＝0**（旧形态下将残留 35 条） |

### 2.5 边界（登记不修，零动作）

**D-7 产物新鲜度／SM7 记录序／W3 措辞窗口／Q-12 施工侧注入面／Q-8 剩余写死常量 —— 五项零动作**，兑现点维持既定（模板会话／W-13 素材）。`撞墙清单_T-76` 与 `T3-17 schema`（DOC-11/12 宿主）未触碰；契约六条与 C4② 引用零变动；任务包未触碰、未重生成。

---

## §3 · 构造侧检出力自查（隔离临时目录执行，不入交付面）

**方法**：整树副本至隔离临时目录 → 文本级唯一锚点替换（**命中数≠1 即报注入失败，不静默跳过**）→ 重跑 harness 与独立复算 → 跑 `assert_check.py` → exit≠0 判击落。锚点串依裁决48 §4／外审报告 §3.1 之**变异语义描述独立书写**，未取自外审侧脚本、未取自 `negative_control.py` 常量。

| 例 | 变异（两侧同改者标"协同"） | exit | 结论 | 击落通道 |
|---|---|---|---|---|
| C1 | 协同 · day count 分母同改 360 | 1 | 击落 | A9-a ＋ A9-1 ＋ A9-2 |
| C2 | 协同 · `side_sign` 语义同翻 | 1 | 击落 | A9-4 |
| C4 | 协同 · per_lot 同去 `multiplier` | 1 | 击落 | A9-b ＋ A9-4 |
| C7 | 协同 · vega 同施 `/100` | 1 | 击落 | A9-2 |
| C8b | 协同 · theta 同改按日计 | 1 | 击落 | A9-3 |
| C10 | 协同 · 期货桩同加 1.02 持有成本 | 1 | 击落 | A9-5 |
| S5 | `negative_control.py` 注入 `import requests` | 1 | 击落 | step0(ii) |
| S7 | `scan_patterns.json` 删 `http(s)://` 关键词 | 1 | 击落 | step0(ii-b) |
| SM6 | route_table `computability_status` 集体改标 | 1 | 击落 | A3-b2 ＋ A3-d |
| **E11** | `ROUTE_TABLE` futures 改指欧式闭式 | **0** | **仍逃逸** | — （harness 崩溃退出 rc=1、产物未重写，断言链读陈旧产物全过） |

**自查结论**：已裁可检类 **9/10 击落**；E11 同型仍逃逸，成因＝D-7（产物新鲜度）经裁决48 §4 判**登记不修**，本会话不得自行封堵。详见 §4-B 呈KD。

**性质声明**：本自查为修复侧留痕，**非 G4 外审重放、非合格判定依据**。`verify/negative_control.py` 之交付面案例集**未扩充**（保持四例），以免扩修复面。

---

## §4 · 呈KD事项（修复会话不自裁，逐项可裁）

| # | 事项 | 呈报要点 | 候选 |
|---|---|---|---|
| **A** | **D-1 实际出现处为五处，裁定枚举为四处** | 外审报告 D-1 与裁决48 §4 均枚举四处（`input_schema.md` §5／§6表行4／撞墙清单#3／切片记录本根裁定登记3）。本会话程序化全量检索得**第五处**：《切片记录》呈KD事项表第 3 行（"行78 只写 Greeks"）。同一缺陷ID、同一错指、同一类别。本会话**一并订正五处**，理由＝仅订正四处将使 v1.1 文档内部自相矛盾（同一文件内一处作行76、另一处作行78），且外审报告明载"建议无论判定如何均须订正"。该动作**超出裁定之字面枚举**，属修复面之字面外延 | (a) 追认五处订正；(b) 判为扩修复面，回退第五处并另案登记；(c) 另定 |
| **B** | **裁决48 §4 与 §9 之口径张力（E11）** | §4 将 D-7（产物新鲜度）判**登记不修**（兑现点＝W-13(13) 新可检层次候选）；§9 却将 **E11 同型**列入增量复核之变异重放面。本会话实测：E11 注入后 harness 崩溃退出（rc=1）、产物未重写，`assert_check.py` 读陈旧产物 **35/35 全过、exit=0**——**断言链对 E11 同型无检出力，且本轮修复未改变此点**（Q-6 修复所覆盖者为 `assert_check` 自身之日志陈旧，非 harness 产物陈旧）。若增量复核以"断言脚本 exit 码"为唯一 observable，E11 将必然计为逃逸 | (a) 增量复核之 E11 同型判据改以**流水线级 observable**（harness/复算脚本退出码 ≠0 即计击落）；(b) 解除 D-7 之登记不修、另开修复动作；(c) 接受 E11 于本轮维持逃逸、不计入重放标尺分子分母；(d) 另定 |
| **C** | **A9 行为级恒等式与类三成分6 之耦合形态** | A9-1／A9-2／A9-3 三式为无股息欧式闭式解之解析恒等式，属**模型族相关**之可证伪化手段。为避免将类三（定价模型族选型，TPL3-T7601-01）在判据层固化，本会话之处置＝恒等式适用面由 `CONVENTION_DECLARATION` 之 `option_identity_routes` 声明行界定，阶段B 换族时随声明面同步改写；schema §5.1 已明写"不构成对定价模型族之选定"。呈KD确认该处置是否足以隔离判据层与类三成分 | (a) 追认；(b) 判为判据层污染类三、要求改用模型无关形态（代价：theta 年计之钉定将退化为有限差分近似检验，区分力下降）；(c) 另定 |
| **D** | **D-4 二选一之选择结果与理由**（开启文本 §3.2-6 要求入修复记录） | 取**指纹断言**（后者）。理由：①实现成本显著低——指纹形态约 10 行（声明块 1 行＋脚本 8 行），声明面形态需将 15 个关键词、1 条正则、9 个模式串、6 个否定标记、1 个窗口参数逐项转入 schema 并逐集对表，约 55 行；②规避新增写死常量——指纹值**载于 `input_schema.md` §8.1 声明面而非脚本内常量**，沿用"判据基线一律程序化解析自本包内源文件、零人工转抄常量"之既有通道，故不加剧 Q-8（剩余写死常量，与 W-13(6) 包自足张力）之开放张力。**残留局限如实报出**：指纹与声明面同处 schema，二者仍可被同一手同批改写；指纹形态之保护力在于**使削弱动作落入人读可见面**，非密码学意义之防篡改 | (a) 追认；(b) 改采声明面形态；(c) 另定 |

---

## §5 · 新撞墙申报

- **模板一新增：0 条**。**模板二新增：0 条**。§6.3 缺陷回传新增：**0 条**。类三 A→B 衔接新增：**0 条**。
- 既有登记 5 条（TPL1-T7601-01～04＋TPL3-T7601-01）**全部维持 open，零闭合、零状态翻转**；TPL1-T7601-01 之触发场景措辞随 D-6 订正，**缺口本体与阻断性判定零变动**。
- 本会话所遇疑义三项（§4-A／B／C）**均属既裁事项之口径边界与形态确认，非新缺口**，故走呈KD而非模板一/二登记。若KD判其中任一项应转登记，本会话待令另行登记（不自裁）。

---

## §6 · 产出物清单

| 件 | 说明 |
|---|---|
| `TPL1-T76-01引擎_切片交付包_v1_1_20260730.zip` | 修复版交付包；`entity/`＋`verify/` 结构维持；断言 35/35 全过、exit=0 留痕于包内 `verify/assert_check_log.txt` |
| `TPL1-T76-01引擎_修复变动面清单_v1_0_20260730.md` | 逐件三列指纹＋动作号映射；**增量复核之等效对照件** |
| `切片记录_TPL1-T76-01引擎_v1_1_20260730.md` | cp-then-edit 再版；合格判定改为判定链留痕，**不出终态判定** |
| `撞墙清单_TPL1-T76-01引擎_v1_1_20260730.md` | cp-then-edit 再版；缺口条目本体零变动 |
| `TPL1-T76-01引擎_修复记录_v1_0_20260730.md` | 本文件 |

**下游**（既裁引用，裁决48 §9）：→ **增量复核会话**（新session；输入含修复前底版＋本清单）→ **KD最终收口**（含 #8 桩替换重验口径"须裁"义务显式呈裁＋治理开销观察行登记）→ 第三批对象选定（44-C6 串行）。
