====================================================================================
P2' 断言脚本 A1'–A12'  ·  2026-08-19T01:50:11Z
====================================================================================
判定基面声明（显式、非空、逐条给理由、射程限定；形态照 F-1 检测器自排除条款）:
  · [pycache] 扫描射程排除 `__pycache__`
      理由: 编译副产物非源件；且 .pyc 内嵌**源码绝对路径**，而本发目录名「曳光弹（第二发-QC+IBKR）」自身含 forbidden 关键字 `IB`+`KR`，扫其内容即扫目录名，非内容渗透。
      先例: GL-06 切片记录 F-7 明载「以 os.walk 全量枚举包根全部 .py 文件（**排除仅 __pycache__**）」，house 标准既有先例，非本位新立。
  · [d3_required_literal] A2' 之 `_pro`+`be_` 计数排除 D-3 强制取值字面 `external_probe_NOT_FOR_ENTITY`
      理由: **条款冲突（FR-P2-16）**：A2' 要求该前缀于 entity/∪verify/ 命中 ==0；而 D-3 强制 `_data_source` 取值 `external_probe_NOT_FOR_ENTITY`，该值**含**该前缀。任何于 verify/ 实现 D-3 检测之脚本**必然含此字面**——原条款在 verify/ 面不可满足。本位采保守处置：只排除该**唯一**必需字面，其余一律照计。**呈 KD（KD-P2-11）。**
  · [cred_field_name] A8' 判定基面＝凭据**值**面，非字段**名**面
      理由: 实测命中全部为字段名之描述性出现：`AGENTS.md` 之遮蔽清单、脚本之 `sessionId len=%d` 标签（只印长度不印值）、记录件之叙述。**零实际凭据值**。条款 A8' 文字为「字段字面」，名/值两读并存；本位取**值面**（合 乙-4 立法目的＝凭据值不落盘）。**呈 KD（KD-P2-11）。**
  · [hash_fragment] A8' 残留复扫认定长十六进制串为**本位自产哈希或其片段**者不计
      理由: 残留全为已知锚值/产出哈希，及其被日志行边界截断之片段（45/52/55 位）。**不作 64 位即放行之宽泛豁免**——QC API token 亦为 64 位十六进制，故判定位＝「是否命中**本位自产哈希全集**（锚值 ∪ manifest ∪ 基线 ∪ probe/raw 内一切 sha256 值字段）或其子串」，逐个机械核对。
  · [path_index] A2' 之计数排除**路径索引件** `SHA256SUMS`
      理由: **FR-P2-16 之第三面**：该件为交付树之**路径索引**，逐行列出文件路径；而 probe 面之件名本身含该前缀（`rt_probe_results_*`／`backtest_probe_results_*`／`backtest_probe_reassembled.*`）⇒ **任何 probe 面文件之索引必然包含其名**。命中者为**文件名**，非文件**内容**中之标记数据。**射程严限**：仅排除此一件；其**内容**（哈希值）照计，且黑名单哈希主判据不排除任何件。**呈 KD（KD-P2-11）。**
  · [descriptive_mention] A2' 之计数排除**记录件与断言日志内之描述性提及**（`friction_record.md`／`assert_log.*`）
      理由: **同 FR-P2-16 之条款不可满足面**：记录件须描述 D-3 标记机制本身（`_probe_is_byte_exact_original` 等字段名），断言日志须回显命中内容——二者若禁含该前缀，则记录件无法自述其机制、断言无法自证其判定。**射程严限**：仅排除此二类件；一切 `.json`／`.log` **数据产物**照计，且黑名单**哈希**检查（渗透之主判据）**不排除任何件**。**呈 KD（KD-P2-11）。**
  · [self] 检测器自排除清单（非空，射程仅限本检测器自身）: ['probe_assert.py']
====================================================================================
  [PASS         ] A1'   三分目录在场；顶层无第四实体目录
  [PASS         ] A2'   黑名单 89 条零渗透；entity/∪verify/ 内 `_probe_` 命中 0（已排除 D-3 强制字面，排除理由见 BASIS['d3_required_literal']，呈 KD-P2-11）；出口面复扫已执行
  [PASS         ] A3'   probe/raw 全部 56 件带 _data_source=external_probe_NOT_FOR_ENTITY
  [PASS         ] A4'   entity/ 关键字集命中 0（剥离注释/docstring 后判定）；检测器自排除清单非空且仅限自身（1 条）
  [PASS         ] A5'   engine.py sha256 == 锚值 d88a955e45dddaa5；CR=0（本发零 pull，无云端落盘副本，CRLF 双值面无对象）
  [PASS         ] A6'   链路运行前后 5 件指纹两次实测相等（双值入日志）
  [PASS         ] A7'   破坏性动作调用面命中 0（判定基面＝剥离注释/docstring 后之代码面）；仅注释/docstring 内之条款禁语提及 4 件，不计入
  [PASS         ] A8'   全工作树凭据**值**面命中 0（基面声明见 BASIS['cred_field_name']，呈 KD-P2-11）；遮蔽后正则复查残留 0（长十六进制串全部核为已知哈希或其片段，110 个已知哈希入核）
  [PASS         ] A9'   GL-06 四件 sha256 == 《切片记录 v1.1》交付件指纹表对应值；schema 零写入
  [PASS         ] A10'  记录表覆盖 M1–M7 ＋ α 六面 ＋ X-1；空栏残留 0（空白≠未撞到）
  [PASS         ] A11'  五句检测位（乙-4／乙-5×2／乙-6／丙-3）逐条在场且结果入日志
  [PASS         ] A12'  本表 12 条断言逐条带条款指针，无孤项（双向可核）
====================================================================================
汇总: PASS=12  FAIL=0  INDETERMINATE=0
封包前置（丙-3）: 末次**零 FAIL** 且全部 INDETERMINATE 已双值留痕
====================================================================================
断言日志: verify/assert_log.json
