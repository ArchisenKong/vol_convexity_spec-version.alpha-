# 修复变动面清单 · T3-22 · v1.0 · 20260802

> **性质**：呈报态，随T3-22修复交付包同批产出，供增量复核会话分层核验（GK-31先例
> ＝《GK-31修复变动面清单_v1_0》形态；裁决48-1同型条款："修复变动面清单须分列，
> A/B产物变动类与纯文本类分列，增量复核据此分层核验"）。
> **总口径**：本轮**数值产物已变动**（修复会话开启文本§五纪律约束已预告：项4/5/6项
> 扩input与钉点致数值产物预期变动，须全链重跑重出）——底版81/81数值比对项因Q4
> 纳入比对面扩为90/90；put wing t7-t9三期premium_bleed字段改值。**非算法返工**：
> 核心计算实质（band位置/四因诊断读数/convexity/bleed联合检验）零改动，全部变动
> 限于判据链观测能力扩容（F-1/F-3/D-3/F-4/Q4/Q5五类新增机制）＋登记形态订正
> （D-6）＋申报口径订正（D-4/O-1/O-2）＋归因机制重构（D-5）。

---

## A/B产物变动类（数值/字段随之改变，须增量复核重新核验全量数值）

| 变动 | 内容 | 触发项 | 影响面 |
|---|---|---|---|
| 输入域扩充 | `entity/input_data.json`：put wing t7/t8/t9三期`premium_bleed_booked`/`premium_bleed_downgrade_flag`改值（t7与t6相等·等号边界；t8较t7下降·触发non_decreasing=False；t9降级·触发not_downgraded=False）。call wing与两翼其余全部字段（moneyness/dte/skew/liquidity/raw_vega/raw_gamma）**未动** | 项5（D-7） | `entity/harness_output.json`／`verify/independent_output.json`／`verify/comparison_table.tsv`／`verify/assert_log.txt`全部重新生成；put wing`premium_bleed_integrity_check`序列由v1.0全True变为`[T,T,T,T,T,T,T,F,F]`（t8/t9翻转） |
| 比对面扩容 | `verify/independent_recompute.py::recompute_call_periods`补入`wing_presence_status`字段（passthrough，原缺失）；`verify/assert_check.py::numeric_comparison`之`call_fields`新增`wing_presence_status` | 项6（D-8/M14） | 数值比对项由81/81扩为**90/90**（新增9项：call wing 9期×1字段） |

## 断言扩充类（新增判据机制，不改变现有数值本身，仅扩大判据链检出面）

| 变动 | 内容 | 触发项 |
|---|---|---|
| F-1封闭规则动态化 | `verify/assert_check.py`新增`discover_py_scripts()`，step0(ii)扫描面与F-1完备性自检共用该动态发现结果，取代原硬编码四项扫描清单；`EXPECTED_SCRIPTS`降级为完备性对表基线 | 项1（D-1） |
| F-3产物新鲜度observable | `verify/assert_check.py`新增`f3_product_freshness_check()`：本进程内当场重跑`entity/harness.py::run()`与`verify/independent_recompute.py::run()`，与磁盘产物bit-exact比对；下游全部核验改用其返回之新鲜值 | 项2（D-2） |
| D-3钉表扩全量 | `verify/assert_check.py`新增`EXPECTED_PUT_MONEYNESS_DRIFT`/`EXPECTED_PUT_TIME_DECAY_FRACTION`/`EXPECTED_PUT_SKEW_CHANGE`/`EXPECTED_PUT_LIQUIDITY_DETERIORATION`/`EXPECTED_PUT_BLEED_INTEGRITY`/`EXPECTED_CALL_CONVEXITY_READING`/`EXPECTED_CALL_BLEED_INTEGRITY`共63项静态钉表+`full_pin_table_check()`核验函数；`EXPECTED_CALL_PRESENCE_STATUS`（9项，并入`enum_and_pinned_band_check`） | 项3（D-3）／项6（D-8） |
| F-4扩展entry基准 | `verify/assert_check.py`新增`f4b_entry_basis_consistency()`：核证put t1自身四因分量恒为零，识破C11型entry基准协同变异 | 项4（D-8/C11） |
| Q5第三路径订正 | `verify/assert_check.py::bleed_third_path_independent_check`（原`bleed_monotonic_independent_sanity`）由"断言输入自身单调未降级"（判据-实现错层）改为"独立推导期望值与harness输出逐期比对" | 项5（D-7配套修复，非独立新项，为容纳项5输入扩充之必要连带修复——原函数对新输入会产生假性硬失败） |

## 纯文本订正类（不改动任何一侧实现代码本体，仅订正申报/指针/文档表述）

| 变动 | 内容 | 触发项 |
|---|---|---|
| F-2分叉清单重述 | `entity/input_schema.md §7`：独立分叉计数由声明的4订正为按F-2判别据核定的1（bleed单调检验），披露两处零重写复制（skew_change/liquidity_deterioration共用`signed_delta()`）。不改动`verify/independent_recompute.py`任何函数实现 | 项7（D-4） |
| TPL1-T322-01指针改指 | `entity/input_schema.md §3#3／§4／§13#3`：由"引用TPL1-T303-02不重登"改为"新立TPL1-T322-01登记候选"，随交付《TPL1-T322-01登记候选文本》 | 项9（D-6，裁决57-2） |
| O-1订正 | `entity/input_schema.md §3#4`：宪-CV-05锚定行位由"审计表T3·1326行"订正为"审计表T3·1318-1324行" | 项10 |
| O-2订正 | `entity/input_schema.md §12`：补入宪-ST-02（账本记录），v1.0原文漏列 | 项10 |

## 交付形态变动类（脚本行为重构+新增交付文件，不改变其测试对象/测试意图本身）

| 变动 | 内容 | 触发项 |
|---|---|---|
| 负向对照归因动态化 | `verify/negative_control.py`重写：不再自建比对逻辑+写死归因文案，改为在隔离目录内对变异版调用真实`verify/assert_check.py`（未修改一字）之全套判据链，动态捕获`assert_log.txt`首条FAIL行作为归因 | 项8（D-5） |
| 输出持久化新增 | 新增交付文件`verify/negative_control_log.txt`（v1.0版本运行结果仅存在于切片记录文字转述，未落盘，违B19/W-9(2)） | 项8（D-5） |
| 底版隔离纪律强化 | `verify/negative_control.py`之变异临时目录由v1.0的`verify/_mutation_tmp`（嵌套于verify/内，存在目录自嵌套复制隐患，本次修复设计阶段自查发现并订正）改为包根同级`_mutation_tmp_t322`，逐文件白名单复制取代整树copytree | 项8（D-5，修复设计过程自查订正，非外审原文指出，构造纪律留痕） |

## 未变动确认（零改动项，供增量复核比对基线）

- `entity/harness.py`：**零改动**（band_position/process_put_period/process_call_period/bleed_integrity_check/run 全部函数体逐字节与v1.0一致）。
- `entity/input_schema.md §1-§6`除本清单所列具体订正段落外，其余对象定义、五问映射表、三分判别表其余行、仪表身份标注、raw_vega/raw_gamma取值形态裁定，**内容不变**。
- `entity/input_data.json`除put wing t7/t8/t9三期`premium_bleed_booked`/`premium_bleed_downgrade_flag`外，两翼其余全部字段值**不变**。
- band边界数值（put 17/20,19/20；call 9/10,21/20）、band位置序列（put/call）**不变**。
- Q1/Q3 band位置机制、Q2四因诊断读数计算式、Q3 convexity passthrough、Q5布尔联合检验逻辑本体**不变**（仅输入域与判据观测面变动，算法本身零改动）。

---

## 实测记录（修复会话施工侧自查，供增量复核参照，非G4外审重放替代）

修复版entity/+verify/在隔离沙箱内完整跑通（`python3 entity/harness.py && python3 verify/independent_recompute.py && python3 verify/assert_check.py`），90/90数值比对通过、63项钉表通过、全部附加断言PASS，exit 0。`verify/negative_control.py`独立运行，exit 0，归因动态捕获且已持久化。

外审报告v1.0所列攻击/协同变异，修复会话在临时测试副本内逐项复测（测试完毕后已丢弃，不随交付）：

| 攻击/变异 | v1.0结果 | 修复版结果（实测） | 击落断言 |
|---|---|---|---|
| A1（改harness不重跑） | 逃逸exit=0 | **击落exit=1** | `f3_product_freshness_check` |
| A2（伪造independent_output.json+空壳化独立复算） | 逃逸exit=0 | **击落exit=1** | `f3_product_freshness_check` |
| A5（新增entity/extra_helper.py含import socket） | 逃逸exit=0 | **击落exit=1** | step0(ii)动态扫描 |
| C11（Q2四因entry基准t1→t4，两侧协同+重跑） | 逃逸（数值比对90/90仍全通过） | **击落exit=1** | `full_pin_table_check`（钉表先命中；`f4b_entry_basis_consistency`为第二道防线） |
| M14（wing_presence_status覆写为present，单侧） | 逃逸（v1.0未入比对面） | **击落exit=1** | `numeric_comparison`（比对面扩容后直接命中） |
| C03（moneyness_drift去abs，两侧协同+重跑） | 归D-3钉表承接（48 Q-4口径，未成文面） | **击落exit=1** | `full_pin_table_check` |

上述实测为修复会话自查（施工侧），**不构成、不替代**增量复核会话之独立三层验证（生产者不能自审纪律）；增量复核会话须以新session、fresh context独立重放，重放集下限见修复会话开启文本§七（外审变异全集含M14、C03/C04/C05b/C07/C09/C10/C11、攻击A1/A2/A5，及本轮项4/5新增可判面之对应变异）。
