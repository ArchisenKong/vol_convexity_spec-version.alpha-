# DOC-18 评估报告与 KD 裁定落地 · v1.0 · 20260805

> **性质**：DOC-18（引擎代码产物跨会话持久化/挂载之环境层缺口）评估会话之实测记录与 KD 裁定落地合一载体。形态先例＝《T-24外审报告与KD裁定落地_v1_0》《GK11核验与KD裁定落地_v1_0》。
> **会话身份**：DOC-18 评估会话（裁决68-2立项，环境层探查呈报会话）。呈报方零裁决、零自评终局效力；本件 §2 全部裁定由 KD 于本会话内以 **"均confirm"**（20260805）作出，呈报方仅执行落写。
> **编号候选**：**裁决69**（末号68，顺延）。编号确认归 KD；未确认前本件为权威载体，台账指针挂下次自然再版（50-3过站滞后读法）。
> **执行环境**：chat ＋ bash_tool（手册v1.7 §3.4，阶段A chat全程；权威载体＝project现行版）。
> **代写申报（53-6条款）**：裁定记录类，本件即权威载体。
> **兑现点核证**：DOC-18 兑现点＝"W-16 #8批前评估"（待修登记v1.46头注②/裁决65-20）。本会话先于 #8 批任务包生成会话执行，**时序合规**。

---

## §1 实测记录（程序化执行，非目测；全部命令可复现）

### 1.1 会话基线环境探查（KD 提供输入前）

| 项 | 实测结果 |
|---|---|
| project 挂载面构成 | 159 件，**159/159 为 `.md`**；`ls \| grep -v '\.md$'` 空 |
| 全域 Python 源码 | **零**（全域 `^\s*def ` 与 `^import/from…import` 命中数＝0） |
| `M1_VERBATIM_EMBED` 标记 | 5 件命中，**全为标记名被引述**，无内嵌段实体 |
| uploads 通道 | 空 |
| `/mnt/user-data/{uploads,outputs}` 挂载源 | `rclone-filestore:claude_chat_<会话ID>:` |
| `/mnt/project` 性质 | 容器根盘上的**可写本地副本**（写入不回写；探针 `touch` 成功后即删） |

**基线结论两条**：
1. **跨会话不持久之机制层根因＝产出面挂载点绑定会话ID**，非配置疏漏、非可由会话内动作绕开。T3-22 schema §6 所述"环境事实"于 20260805 复现，且根因下探一层（此前登记止于"可及面零 `.py`"之现象层）。
2. **`engine.py` 本体在 project 挂载面完全不在场**——不仅无 `.py` 件，且无任何 md 件承载其源码。

### 1.2 交付包装载核证（KD 上传 zip 后）

装载对象：`TPL1-T76-01引擎_续2求值口径增强_交付包_v1_2_20260802.zip`（20 件全树）。

| 核证项 | 基准 | 结果 |
|---|---|---|
| 逐件 SHA256 | 《切片记录_TPL1-T76-01引擎_续2求值口径增强 v1.2》指纹表（20件） | **20/20 一致**，不符 0，未匹配 0 |
| `entity/engine.py` | 声明值 | `e31fbcfeacebeb196f7e1d63f41089e7e0a7deb3cc9a9a8b44941af31e194e99` ≡ 声明值 |

### 1.3 真实调用路径端到端实测（DOC-18 核心待验项）

| 项 | 结果 |
|---|---|
| 全链重跑 | `harness.py` → `independent_recompute.py` → `regression_check.py` → `build_provenance.py` 全部 **exit=0** |
| 断言 | `assert_check.py` **44/44 PASS，exit=0** |
| 重跑后产物 | 与声明指纹**逐字节零变动**（20/20 维持一致） |
| **单件自足性** | `engine.py` 唯一外部依赖＝ `import math`（标准库）。隔离目录内仅置该单件，构造 `european_option` lot 调用成功 |
| 调用面 | 单入口 `evaluate_lot(lot)`；lot ＝ 14 字段 dict（`instrument_class`／`option_type`／`side`／`strike`／`expiry`／`spot`／`risk_free_rate`／`implied_vol`／`multiplier`／`quantity`／`valuation_date`／`dividend_yield` 等，见 `STATED_FACE_FIELDS`） |
| 体量 | **293 行／9,622 字符／14,173 字节**；CJK 1,905 字；粗估 ≈4.1k token |

**结论**：真实调用路径之技术障碍＝**零**。"七根桩替换需真实调用路径"之硬需求（DOC-18 立项理由）已被证明可满足。

### 1.4 候选(b-1) project 直读实测（KD 上传 `engine.py` 原件后）

| 核证项 | 结果 |
|---|---|
| 在场性 | `/mnt/project/engine.py`，14,173 字节，件数 159→160 |
| 扩展名保真 | `.py`，未被转换 |
| 内容保真 | SHA256 ≡ `e31fbcf…e99`；与交付包原件 `diff` **空**（逐字节一致） |
| **直读可执行** | 不复制、直接以 `/mnt/project/engine.py` 路径 `spec_from_file_location` 加载并调用 `evaluate_lot`，成功 |
| 数值一致性 | 与隔离环境实测**逐位相同**：value 321.911035／delta 0.614358／gamma 0.000611／vega 1176.506724／theta −383.546708／rho 1039.800552；`route_id`＝`bs_dividend_yield_closed_form`；`computability_status`＝`computed_with_open_dimensions`；`unstated_dimensions_hit`＝`["day_count_convention"]` |

**(b-1) 四项全通过**。

**残余未实证项（如实标注）**：**跨会话在场性**。本轮仅证明"会话内刷新＋当前在场"，未证明"新会话装载时仍在场"。按 project 机制该项为高置信推定，**实证点＝#8批开场装载核证**（该核证本为必执行动作，零额外成本、零额外会话位）。在此之前不作已实证宣称。

### 1.5 zip 通道边界（KD 告知之事实）

project context **不接受 zip 包上传**。该限制**不覆盖 `.py` 单件**——§1.4 实测证伪了"project 只收 md"之推定。两个限制面须分列，不可混同。

---

## §2 KD 裁定全文（20260805，"均confirm"）

### 2-1｜DOC-18 定性更正
由"环境层**技术缺口**"更正为"**装载通道选择＋纪律成文**问题"。依据＝§1.3 端到端实测（单件自足／单入口／44-44 PASS）。真实调用路径可满足性已证，原立项所指之硬障碍不成立。

### 2-2｜候选处置（收窄序）

| 候选 | 裁定 | 依据 |
|---|---|---|
| **(b-1) project 直读** | **采为主通道** | §1.4 四项实测全通过；零重复人工动作；自动进每会话可及面 |
| **(d) uploads 会话内上传**〔68-2 三候选外，本会话实测浮现〕 | **补入候选集并保留为兜底通道** | §1.2/§1.3 实证；覆盖 (b-1) 跨会话残余不确定性与引擎再版过渡窗口 |
| (a) 人工抄写整文件内嵌 | **不采** | 主通道已成立，无启动理由 |
| (b-2) md 程序化包裹 | **不启** | 为 (b-1) 被拒时之备案，前提已消失 |
| (c) 任务包内嵌重建 | **不采**；且**裁决20 射程问题本轮不裁** | 无需求即不开裁决位（避免为不会走的分支开裁决面） |

**60-A2 射程维持**：裁决60-A2 所裁＝FW-03-甲**消费 M1 之形态**（契约内嵌合法），**不扩张**至跨会话持久化机制。二者为不同问题，先例不挪用。

### 2-3｜#8 批执行形态落定（68-3 明文要求随本评估结果呈裁）
**单会话执行，不分组**。依据：七根桩替换所需仅 `engine.py` 单件（≈4.1k token）＋各根市场坐标构造 lot，为纯计算动作；分组只增加装载核证次数与跨会话一致性风险，无收益。

**增强依据（§4 呈报D）**：#8 批开场装载核证**可完全程序化闭合、无需 KD 任何手动输入**——所需三项现已全部在 project 可及面：①`engine.py` 本体；②指纹声明源《切片记录_续2 v1.2》；③七根各自桩值声明（各根 schema／切片记录）。

### 2-4｜文件名规范化（呈报A）
现行裸名 `engine.py` 无对象ID／版本号／日期，与既有命名纪律不一致。**具体风险＝引擎再版（v1.3）上传时同名覆盖将使旧版无声消失且无留痕，与 cp-then-edit 纪律正面冲突。**
**裁定＝改名**，式样候选 `TPL1-T76-01引擎_engine_v1_2_20260802.py`（import 侧零影响——`spec_from_file_location` 不要求特定文件名）。执行动作归 KD（project 侧重传/改名）。

### 2-5｜装载纪律与引擎再版落档纪律成文（呈报B ＋ 原建议7）
挂 **W-21 凑批面**，两项并批成文：
- **(i) 装载核证纪律**：无论采 (b-1) 或 (d)，装载后**必须执行 SHA256 对照《切片记录_续2 v1.2》指纹列**方可使用（本会话 §1.2/§1.4 形态即模板）；**禁止在 `/mnt/project` 或工作区内修改引擎源码**——引擎再版只能走引擎自身切片通道。
- **(ii) 引擎再版之 project 落档纪律**：旧版 `.py` 之处置（退场／保留／并存）与命名规则；与撞墙清单 `TPL-FW03A-02`（内嵌段触发式重验，触发条件含"引擎再版"）联动。

### 2-6｜归纳输入排除条款之机械维持义务（呈报C）
`engine.py` 已进 project 即进归纳输入面。《归纳输入封闭清单 v3.0》§三**已有**"entity/input_data.json、harness.py、harness_output.json、engine.py 等交付包代码件"显式排除条款（理由＝harness/实现域，非归纳消费物），故本轮污染风险**已被覆盖**。
**惟该条之事实前提自本次上传起已变更**（由"该件不在 project"变为"在 project"），须在每轮清单再版时**机械维持**，防止后续再版因旧前提而删条。**挂账＝W-21 同批**（清单再版时同步核对）。

### 2-7｜DOC-18 销项
兑现条件满足（评估执行完毕＋路径择定＋纪律挂账落位），**DOC-18 状态 open → 已兑现·闭合**。条目文本保留（裁决22(e)判例）。

---

## §3 前案更正留痕（呈报方主动，两项）

**更正1｜"挂载副本快照"推定不成立**。呈报方前案称"本会话 `ls` 若未现则属挂载副本快照时点问题、需挂下一会话核证"。实测证伪：project 挂载面**在会话内动态刷新**——本会话容器基线 `Aug 4 15:35`（159件全md），KD 上传后同一会话内 `ls` 即得 `Aug 5 01:05` 之 `engine.py`，件数 160。上传→可及**无需换会话**。

**更正2｜(a) 与 md 载体之绑定属呈报缺陷**。呈报方前案以"FW-03-甲 v1.0 逐字内嵌零改动 claim 经核证不成立"为由否定 (a)，并将该否定隐含覆盖至一切 md 载体形态。**该绑定不成立**：FW-03-甲 漂移之成因＝**人工抄写**（删两处注释、docstring 被改写致类三登记丢失），而程序化包裹＋程序化抽取＋SHA256 对表三步全机械，抄写漂移面为零。**(a) 与 (b-2) 之分野不在载体是否 md，在复制通道是人工还是机械**——前案未拆开，属呈报缺陷，更正留痕。（本轮 (b-1) 成立后 (b-2) 不启，该更正不改变裁定结果，仅防止错误理由沉淀为先例。）

---

## §4 台账联动

| 条目 | 动作 |
|---|---|
| **DOC-18** | **销项**（2-7）；状态 open → 已兑现·闭合 |
| **W-16** | 条目内容扩充：**#8 批执行形态落定＝单会话不分组**（2-3）；装载通道＝(b-1) 主／(d) 兜底；开场装载核证可程序化闭合。兑现点不变 |
| **W-21** | 扩项三条：装载核证纪律（2-5-i）／引擎再版 project 落档纪律（2-5-ii）／归纳输入排除条款机械维持核对（2-6）。兑现点不变（#8批收口后之 W-21 凑批∪第四批任务包生成，68-1） |
| 执行期裁决记录 | 本批裁定指针挂下次自然再版（50-3） |
| 治理开销观察行（43-E4） | 第四批周期计数 **+1**（本批裁定，编号候选69），收口时随最终登记 |
| 其余 W／DOC／G／TPL 族 | 零变动 |

---

## §5 清点点②执行（裁决15；对照基准含 55-4a 扩容面）

对照基准：《待修登记_执行期 v1.46》＋《治理缺口与开放张力登记 v1.15》＋《升格候选登记册 v1.1》＋《自由度治理册 v1.4》。逐字对照触发/闭合条件原文：

| 项 | 对照 | 结论 |
|---|---|---|
| DOC-03 | "schema 下次自然再版" | 未命中（本轮不触碰 T3-18 schema） |
| DOC-17 (a)/(c) | (a) 挂 T3-18 schema 自然再版；(c) 挂模板三/手册下次触碰 | 未命中（(b) 已兑现于手册v1.7） |
| **DOC-18** | "兑现点＝W-16 #8批前评估" | **命中·本轮兑现销项** |
| **W-16** | "执行形态随 DOC-18 评估结果呈裁"（68-3） | **命中·执行形态落定，条目扩充** |
| W-18 | v1.44 已全销项（E1–E4 经 65-35 定性） | 未命中 |
| **W-21** | "挂手册/受控文本下次凑批面" | **命中·扩项三条**（兑现点不变） |
| W-22 | 兑现点＝(a)(b) 第四批收口／(c) 第四轮归纳定案 | 未命中 |
| G-5／G-7 | 阶段B 远期 | 未命中 |
| G-8 | 持续观察态（55-1d 形态改写非闭合） | 未命中 |
| TPL1-T7601-02／03 | "零消费方注记，触发＝**首个消费方切片**出现"（65-23） | 未命中。**邻近提示留痕**：#8 批为对照批非切片构造，不构成"消费方切片"；首个真正触发点仍在第四批及以后之消费根 |
| TPL1-T7601-01／04 | 已闭合 | 未命中 |
| 升格候选登记册 REG-UP-01/04/05/06 | 强制时点逐字对照：01（Phase A→B 衔接实装参数治理时）未到；04（块五外审设计或 G 门控修订时，按窄读续挂，重核点挂清点点④）未到；05（实盘准入设计时）／06（Phase B 运行治理设计时）未到 | **命中 0** |
| 自由度治理册信号 #8／#14／#15 | #8（实盘稳定后 KD 启动）未到；#14／#15 触发条件未满足 | 未命中 |

**命中 3 项**（DOC-18 兑现销项／W-16 扩充／W-21 扩项），全部已在 §4 落处置。**新事实一条**：归纳输入封闭清单排除条款之事实前提变更（2-6），已挂 W-21。

---

## §6 落档清单

| 件 | 去向 |
|---|---|
| 本件《DOC-18评估报告与KD裁定落地_v1_0_20260805.md》 | 裁定载体（新立） |
| 《待修登记_执行期_v1_47_20260805.md》 | 台账再版（v1.46 随落档退场） |

**KD 侧执行动作两项**：①落档上述两件；②project 侧 `engine.py` 改名（2-4），式样候选 `TPL1-T76-01引擎_engine_v1_2_20260802.py`。

---

## §7 下游动作序（绝对序，68-1 总序不变）

**本会话收口** → **#8 批链**（任务包另会话生成，裁决20/56纪律；单会话执行，2-3）**∥ 第四批选定会话**（并行，含曳光弹立项菜单 62-12）→ **#8 批收口** → **W-21 凑批 ∪ 第四批任务包生成**（W-21 原两项＋本批扩三项＋O-A 模板条款于该点兑现）。

---

> **G-4 合规声明**：本文档不含骨架/模块产出结构预描述。
> **裁决20 同型纪律**：本文档不含技术路线提示；§1 所载 `evaluate_lot` 调用面为**已收口产物之既有事实转录**（源＝`engine.py` 现行版本体），非对下游构造之路线指定。
> **G-3 口径**：本件所载残余未实证项（跨会话在场性）为登记项，不构成评估不合格。
