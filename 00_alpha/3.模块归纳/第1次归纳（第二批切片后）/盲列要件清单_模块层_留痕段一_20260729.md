# 盲列要件清单 · 留痕段一 · 模块层归纳外审会话（M-2）

> **协议**：本清单在装载任何审计对象（模块划分_v0_1_candidate、归纳会话撞墙记录_模块层归纳）**之前**产出，仅装载开启文本§3类别3/4件（归纳程序纪律v1.2＋任务包§3.1一类全件）。形态照骨架第二轮先例（盲列要件清单_留痕段一_20260729，仅结构参照，内容独立自源件提取）。
> **装载顺序自申报**：本会话在段一读取了模块层归纳任务包§3（一类枚举表）——该读取为解析类别4装载面之必要前置（开启文本§3类别4以"任务包§3.1一类全件"定义），非违序；模块划分candidate与撞墙记录在本清单产出前零读取。
> **产出**：M-2会话；零裁决、零落档。

## 一、九根实体算法要件（正向覆盖比对基准）

各根要件均自该根切片记录"对象语义/实体算法要件"段独立转抄，附行位指针。

### 1. T-76（3要件）〔切片记录v2.2 对象语义段，行8〕
- bucket派生（tenor_bucket／moneyness_bucket自原始expiry/strike派生）
- 六维坐标归属（leg_type×side×option_type×tenor_bucket×moneyness_bucket×scenario身份键映射）
- node级聚合（per-lot raw Greeks求和）
- 上游边界：per-lot Greeks＝上游输入（裁决14）；供给桩TPL1-T76-01

### 2. TC-33（2要件）〔切片记录v1.2 对象语义段，行8〕
- 逐路径P/L聚合（hedge P&L累加＋期末intrinsic payoff）
- worst确界（路径集合上P/L最差取值）
- 上游边界：对冲政策/hedge仓位序列与估值约定＝上游输入（裁决16）；TPL1-TC33-01

### 3. T3-03（3要件）〔切片记录v1.1 对象语义段，行7〕
- 对上游输入/规则的应用与聚合机制（对具体转换规则内容中立，裁决17）
- 三字段标注机制（source_phase／aggregation_scope／allowed_usage）
- 六slice在场性与跨slice一致性校验
- 上游边界：risk-equivalence规则结构与diagnostic解释规则结构＝上游（TPL1-T303-01/02）；情景三段拆分（幅度＝数据／作用规则形态＝重估类一／重估实现＝上游，引TPL1-T76-01）

### 4. GK-14（六形态规则机制，5类动作）〔切片记录v1.1 对象语义段，行7-9〕
- tenor分桶加权求和／forward分段聚合／surface·grid坐标归类／scenario线性重估近似／双轴标注附着（`方法论状态/转译强度`×`运行权限Phase`，两轴不得合并）
- 上游边界：node级raw Vega账本（不重算T-76 per-lot→node聚合）；forward分段规则＝类二（TPL1-GK14-01）；tenor权重＝类三；scenario重估实现引TPL1-T76-01

### 5. T-24（1公式要件）〔切片记录v1.0 对象语义段，行7〕
- 两估值时点定价重估取差：`bleed = V(t0) − V(t1)`（公式结构类一定案）
- 上游边界：定价模型＝类二缺实现（供给方TPL1-T76-01，裁决14模式引用不重登）；t0/t1、S/K/σ/r、持仓明细＝类三数据层
- 语义随行：分账约束（T-24↔T-23）作语义引用随切片携带

### 6. T3-17（4步机制）〔切片记录v2.2 对象语义段"实体算法要件（本根裁定登记#1，C1）"，行11〕
- band位置判定（below/within/above相对target_activation_band）
- 逐Greek（vega/gamma）情景压力响应比计算
- 双Greek合成（TPL1-T317-01桩，min取小）
- 槽位比较与持续窗口判定（wing_responsiveness_floor／wing_dullness_window，严格大于、非粘滞重置）
- 上游边界：T3-22代理时序／建仓声明基准／情景重估内容／槽位数值＝上游输入

### 7. T3-18（3层）〔切片记录v1.2 对象语义段，行8〕
- 逐期funding gap计算（income − bleed − roll_cost）
- 滚动窗口累计gap聚合
- 破坏形态三态判定（不可判/未触发/触发三元组）

### 8. T-33（生成＋度量＋分类）〔切片记录v1.2 §1对象语义，行26-27〕
- 路径确定性生成（按参数生成六族：P/Q/O/Z/N/B）
- 度量（crossing_count／rv_proxy；六读候选空间，TPL3-T33-02类三）
- 说明性分类（"低IV+震荡"三元组判定，候选口径非冻结判据）

### 9. GK-31可算部分（基线聚合＋分列＋旗标＋坐标扩展）〔切片记录v1.2 对象语义段，行9〕
- leg_type单键分组求和：5腿×5分量（delta/gamma/vega/theta/rho）基线聚合
- 各腿specific观察量分列（复用既有坐标维度：vega_by_tenor、by_option_type左右尾分列等）
- 联合旗标（positive_theta_with_short_gamma）
- 坐标扩展（leg_type扩5值＋新增instrument_type维度）
- 上游边界：node级raw Greeks账本（不重算per-lot→node，同GK-14先例）；五"边界："句＝域边界硬约束零消费

## 二、骨架v2.0区四 · X1–X9跨根消费边（含X9零边双端）〔骨架v2.0行246-258〕

| 边 | 双端 | 类型 |
|---|---|---|
| X1 坐标继承 | T-76→GK-14 | 实体层消费边（A9档一） |
| X2 缺口汇聚 | TPL1-T76-01→{T-76,T3-03,GK-14,T-24}＋TC-33条件 | 上游依赖边 |
| X3 拆分模式复用 | 裁决17→GK-14/T-24 | 裁决血缘边 |
| X4 锚定值转移失败 | T-76→T-24→裁决19 | 校准失效实证边（C3命题证据） |
| X5 坐标继承第二例 | T-76→GK-31（半继承半自建，O-5削弱注记随行） | 实体层消费边 |
| X6 缺口第二消费方 | T3-03→GK-31（TPL1-T303-02 `gamma_center`） | 缺口共享边（B2计数放宽实证） |
| X7 升格入口兑现 | TC-33→T3-17（A6） | 骨架自身通道兑现边 |
| X8 桩系数不继承 | T3-03／T3-17v1.0→T3-17v2.0 | 反向边（C5不外推正面执行） |
| X9 缺口供给方零依赖声明 | TPL1-T76-01↛{T3-18,T-33}；双端＝〔T3-18 schema §2第2条＋撞墙清单_T3-18说明节（裁决43 D-6并正）；T-33 schema §6〕↔〔撞墙清单_T-76 TPL1-T76-01〕 | 零边显式声明（B1第三态） |

## 三、外审方独立预期之模块划分消费义务面（比对用，非结构预设）

1. 九根每根之算法要件在模块划分内有归属，或显式"不归属"说明（正向覆盖）。
2. X1–X9每条边在模块划分内被消费（作模块间关系依据／模块内聚依据）或显式说明不消费；X9零边双端不得被当作正向依赖消费。
3. 模块划分不得预设自本清单结构（本清单为外审比对基准，非归纳输入；G-4）。
4. 每条模块划分陈述带一阶指针；共性类陈述列全部支持根与不支持根。
5. 判断位（模块归属争议、合组/拆分信号）呈报形态而非终裁形态。

〔段一完，段二起装载审计对象〕
