# 切片记录 · T3-18 funding relation failure · v1.1 · 20260726

> **v1.1（20260726，T3-18缺陷回传修复会话产出，裁决31-C(d)授权，cp-then-edit自v1.0）**
> 会话身份＝**缺陷回传修复会话**（§6.3单一修复动作，新会话身份，非原构造会话延续、非外审会话、非裁决会话）。本版不自评合格、不自判修复达成——合格判定权在增量复核会话（外审侧）与KD。
> 本版变更限裁决31-C(d)封闭三项：①O3订正（断言项数分解式补列 `harness_real_run`／`independent_recompute_run`，项数按KD裁定"丙"式分列）；②修复过程留痕（新增"修复过程记录（v1.1）"节）；③编号书写形态订正（`TPL3-T3-18-01`→`TPL3-T318-01`，裁决31-G）。
> **本版不改写v1.0各条正文措辞**（避免超出(d)封闭范围）。由此产生两处滞后，列为呈报项、不就地处置：
> - **呈报项①**：v1.0"合格判定"栏之"通过"系构造会话自记，已被外审判定覆盖（裁决31-C＝(a)不通过·当前交付形态）。本版于该栏加注状态指针，正文措辞未改写。
> - **呈报项②**：v1.0"本根裁定登记"#8 含与D1同源之失实陈述（"A2条目现有四实例均为静态/横截面聚合"）。裁决31-D之D1订正落地面为《撞墙清单_T3-18_v1_1》，**未覆盖本记录#8**；而本记录同属归纳输入集（手册§4：运行态实体包＋撞墙清单＋切片记录）。裁决31-C末明文"D1订正不入修复范围"，故本会话**不就地订正**，登记呈KD裁定订正容器与时点。
> v1.0（20260725）：初版，切片构造会话产出（会话身份＝切片构造会话，块五执行序步②，裁决28末；不做自评、不做外审、不生成任务包，遵手册§8三问自检如实呈报，不代做外审判定）。

## 对象语义（必填）

T3-18 是对累计 funding gap（短端 premium income − 长端 premium bleed − roll cost）之滚动窗口聚合形态、及其"持续性负值且斜率不收敛"破坏判据进行 Q4 可算性验证的一阶实体：输入为人造账本时间序列（逐期 income/bleed/roll_cost 账本值），输出为逐期 funding gap、滚动窗口累计 gap、以及各评估点破坏形态三态判定（不可判/未触发/触发）之三元组。

## 合格判定

> **v1.1状态指针（呈报项①）**：下段"通过"为v1.0构造会话自记，**已被外审判定覆盖**——裁决31-C＝外审判定(a)**不通过**（当前交付形态），缺陷D2成立，走§6.3缺陷回传。修复后之合格判定**待增量复核会话（外审侧，重放M3/M7/C1/C3/C4/C5/C6）与KD确认**，本记录v1.1不作判定语句。v1.0正文措辞按(d)封闭范围保留不改写。

**（v1.0原文）通过**（G-3口径：撞缺口≠不合格；本根撞墙清单一条信号性登记，未阻断A→D跑完）。

verify/assert_check.py 全部 11 项断言通过，exit code = 0（裁决28-B(ii)/裁决30-A：非零退出码纪律以负例验证——构造期一处自身实现缺陷曾使脚本以exit code=1结束，订正后复跑exit code=0，详见"检验通道记录"）。

**断言项数分解式（O3订正，裁决31-C(d)①；项数按KD裁定"丙"式分列＝原始项与修复新增项分列，总数＝11＋16＝27）**：

| 分组 | 断言项 | v1.0原始 | v1.1新增 |
|---|---|---|---|
| step0前置合规 | `step0(i)_input_synthetic`／`step0(ii)_no_data_source_static_scan`／`step0(iii)_independent_recompute_no_harness_reuse` | 3 | — |
| 断言基线程序化解析源文 | `source_anchor_T3_line609_programmatic_parse`（关键词在场） | 1 | — |
| 源文公式结构可判（31-C(c)） | `source_formula_structure_vs_impl_symbol_vector` | — | 1 |
| B段真跑（**O3漏列项，本版补列**） | `harness_real_run` | 1 | — |
| D段②复算真跑（**O3漏列项，本版补列**） | `independent_recompute_run` | 1 | — |
| D段②独立数值比对bit-exact | `numeric_compare_gap_series`／`_cum_gap`／`_shape_evaluation` | 3 | — |
| D段③附加断言·分支覆盖与制品标注 | `branch_coverage_insufficient_history_trigger_nontrigger`／`entity_carries_C6_PB_reference_and_data_source_field` | 2 | — |
| D段③附加断言·逐评估点期望三元组（31-C(b)） | `expected_evaluation_point_set` 1 ＋ `expected_triple_t04..t17` 14 | — | 15 |
| **合计** | | **11** | **16** |

O3之v1.0分解式"3+1+3+2"＝9，与实际11项不符（漏列 `harness_real_run`／`independent_recompute_run`）；总数"11项"原本正确。本表为该分解式之订正，并按"丙"式分列v1.1新增项。

## 本根裁定登记（必填，汇总全部裁定语句＋指针，呈KD逐项裁定，非终裁）

1. **公式结构类一定案**：`funding_gap_t = income_t − bleed_t − roll_cost_t`。判别测试：T3§5.5行609原文直给线性减法结构，无候选歧义空间。裁A，写入 entity/harness.py `compute_gap_series`，非桩，引T3§5.5行609。骨架归属：A2加权线性聚合（权重[+1,−1,−1]），比照A2先例（T-76/TC-33/T3-03/GK-14），本根裁定登记权重取值，不外推跨根（C5纪律）。
2. **滚动窗口累计聚合形式类一定案**：`cum_gap_t = Σ_{s=t-W+1}^{t} funding_gap_s`。判别测试：原文"滚动窗口内累计"文本本身即指定求和形式，无候选歧义。裁A，写入 entity/harness.py `compute_cum_gap`；verify/independent_recompute.py 以前缀和技术独立复算同一聚合结果（路径独立，见检验通道记录）。**窗口长度数值（funding_gap_window槽位）不属此类一裁定范围**——原文明示`parameter_slot_candidate`，本根取桩值4，非校准值。
3. **破坏形态判据操作化＝类三**（计算成分三分判别受控文本§4程序核实）：候选实现方式不止一种且均与原文"持续性负值且斜率不收敛超过槽位窗口"语义相容（持续负值可"全部<基准"或"多数<基准"；斜率可两点割线/最小二乘/差分符号计数；基准可账本零轴/历史分布），无权威文本可排除其余候选。**呈KD**：建议登记为 **TPL3-T318-01**（挂A→B衔接清单，非模板一/二，比照TPL3-T24-01先例形态），是否登记、编号、是否并入既有槽位#7/TPL3-T24-01指针体系，均由KD裁定；本根不自行裁定登记与否（包§3明文："TPL3登记与否由切片会话依三分判别程序判定后呈KD"）。本根实现候选详见entity/input_schema.md §4③（persistence_periods=3、baseline=账本口径零轴／0、slope_threshold=0、割线斜率经移项消去除法）。
4. **C-6标记（M-1）随行携带，不否决**：原文"基准为账本口径的零轴与历史分布，基准选择本身为parameter_slot_candidate"——历史分布＝样本族，判据取值合法性依赖该样本族选择，按T2/02§3行446支命中，登记为证伪触发器。标记语义已见entity/input_schema.md §1，随实体制品携带，不构成本根合格判定否决理由。
5. **随行指针两条（翻译审计表v1.1 T3·609行，非漂移）已随行携带，本根未就地裁口径**：①premium口径与宪-ST-06时间价值成分口径之同源性→治理册槽位候选信号#7（Phase B定）；②bleed端口径→TPL3-T24-01（open）。本根消费bleed为账本数值（数据/机制刀口，值消费不登记），不重新推导其定价来源，引用不重登（一阶实体_T-24_input_schema.md §4-§7先例）。
6. **identity drift / manual review 路由条款＋P_B禁止误用条款＝A7语义标注随行携带**：均以文本形式随entity/input_schema.md §5携带，未实现为可执行逻辑（非本根Q4判据"累计gap形态计算+槽位判据"范围内容），harness/verify不消费该文本参与数值计算。
7. **C3容差命题（锚定值情境依赖）不适用本根**：本根全程整数精确运算（cents），无超越函数（无erf/exp/log/sqrt），C3分派判据（超越函数在场性）判定属离散/精确有理计算，分派子口径(a)＝0（bit-exact，裁决13/19）。未触发跨计算复合度锚定值搬运风险，无需另行实测锚定（详见entity/input_schema.md §6）。
8. **骨架撞墙信号（TPL1-T318-01）**：A2条目现有四实例均为静态/横截面聚合，未显式覆盖滚动时间窗口聚合子形态；T3-18为首个触及后者对象，登记撞墙清单_T3-18供骨架归纳/生长阶段消费，不阻断本根。

## 一阶实体文件清单（运行态，归纳消费物）

- `entity/input_schema.md` —— 对象语义、判据锚定、C-6标记、随行指针、三分判别落地、A7标注、容差声明（**v1.1**：仅编号书写形态订正，无语义/公式/字段变动）
- `entity/ledger_input.json` —— 人造账本时间序列（`_data_source: synthetic_hand_constructed`），**v1.1扩充至17期**（v1.0为12期；t=1..12账本值一字未改，追加t=13..17），槽位桩值未动
- `entity/harness.py` —— 一次性harness（真跑，不建常设pipeline）
- `entity/harness_output.json` —— harness真跑捕获输出

## 撞墙清单条目

TPL1-T318-01（见 `撞墙清单_T3-18_v1_1_20260726.md`〔裁决31-D出版之D1订正版，本会话只读不改〕，open，可继续，不阻断；容器归属维持模板一＋G-6(b)交叉指针，裁决31-H）。

## 检验通道记录（核验夹具，仅供KD外审复核，不进归纳消费）

- `verify/independent_recompute.py` —— 独立路径复算：前缀和(prefix sum)技术替代harness滑动窗口切片求和；持久性判据以`max(window)<baseline`替代`all(v<baseline)`；斜率判据以`not(delta>threshold)`替代`delta<=threshold`（逻辑等价、表达式独立重写）；不import harness、不读取harness_output.json——step0(iii)静态核验通过。
- `verify/assert_check.py` —— step0前置三项断言＋断言基线程序化解析源文（独立读取3_波动率凸性操作方法论主文档第609行并正则核验必需关键词，不复用harness转录）＋D段②独立数值比对（逐期gap/累计gap/形态判定三层bit-exact）＋D段③附加断言（判据覆盖三分支齐备：历史不足/触发/未触发；实体制品携带C-6/P_B/`_data_source`字段）。共11项，全部通过；exit code=0（非零退出码纪律，裁决28-B(ii)/30-A）。
- `verify/assert_log.txt` —— 断言执行完整日志（逐条[PASS]输出，随附；**v1.1为27项重跑日志**）。
- `verify/negative_control.py`＋`verify/negative_control_log.txt` —— **v1.1新增**，修复过程核验留痕（七例变异注入之退出码记录；构造侧留痕，非外审重放、非合格判定——W-9(2)留痕纪律，凡进入合格判定叙述链之核验，脚本与输出须随verify/交付，不得仅以文字声明）。
- **构造期自查订正一处**：assert_check.py 初版 step0(iii) 检测函数以朴素子串匹配`"harness_output.json"`是否出现于independent_recompute.py源码，误将该脚本文档字符串内"不读取harness_output.json"之说明性文字判为真实读取行为（假阳性），初次真跑exit code=1。订正：改为剥离注释/docstring后再匹配（同W-8(c)口径），复跑全部11项通过、exit code=0。此为断言脚本自身实现缺陷之构造期自查自纠，非T3-18对象本身缺陷，过程留痕于assert_check.py函数注释与本记录（如实记录，非隐藏往返）。

## 修复过程记录（v1.1，裁决31-C(d)②）

**授权源**：裁决31-C（外审判定(a)不通过·缺陷D2成立，§6.3缺陷回传单一修复动作）。修复范围封闭四项 (a)(b)(c)(d)，未扩项。

### 改动文件清单（cp-then-edit，自v1.0交付件整树复制后编辑；v1.0七件sha256与外审报告§7附录逐件比对一致，7/7）

| 文件 | 改动性质 | 内容 |
|---|---|---|
| `entity/ledger_input.json` | **内容扩充**（(a)） | periods 由12期扩至17期。t=1..12 账本三元组一字未改；追加 t=13..17（income/bleed/roll_cost 均非负，由目标 gap 值反解）。`slots` 未动。`_note` 字段追加v1.1扩充说明（沿用既有字段，未新增字段） |
| `entity/harness_output.json` | **重跑再生**（(a)派生） | harness 真跑重新写出。既有评估点 t=4..12 之 `gap_series`前12项／`cum_gap`／`shape_evaluation` 三层输出**逐键机械核验零变动**；新增键仅 t=13..17 |
| `verify/assert_check.py` | **断言升级**（(b)(c)） | ①新增 `EXPECTED_TRIPLES` 静态钉表＋`check_expected_evaluation_point_set`＋`check_expected_triples_per_point`（逐评估点14项）；②新增 `check_source_formula_structure_vs_impl`（源文减法项序/符号向量 vs 实现符号向量，顺序敏感），并纳入 gate；③收尾输出增"丙"式项数分解行 |
| `verify/assert_log.txt` | **重跑再生** | 27项逐条[PASS]，exit code=0 |
| `verify/negative_control.py` | **新增**（四-2 W-9(2)留痕纪律／五-1产出要求） | 修复过程核验留痕：七例变异注入脚本 |
| `verify/negative_control_log.txt` | **新增** | 上述脚本输出 |
| `entity/input_schema.md` | **编号书写形态订正**（(d)③／裁决31-G） | `TPL3-T3-18-01`→`TPL3-T318-01`；出v1.1版头。§4③候选枚举未扩充、对象语义未动、类一定案公式未动、§3字段定义未动 |
| `entity/harness.py`／`verify/independent_recompute.py` | **零改动** | 实现侧未动——(a)(b)(c) 三项均不触及计算实现 |

### (a) input 扩充构造依据（逐评估点边界态覆盖）

外审 D2 根因①＝人造 input 状态覆盖不足：`persistent_negative` 在 v1.0 全部评估点恒为 True（单侧饱和），`cum_gap == baseline` 边界零触及。v1.1 追加五期后：

| 新增覆盖 | 落点 | 对应外审逃逸例 |
|---|---|---|
| `cum_gap == baseline`（cum_gap[13]＝0） | t=13 | M7（基准比较 `<`→`<=`） |
| `persistent_negative = False`（窗口含非负累计值） | t=13/14/15 | M3、C6 |
| `delta == slope_threshold`（v1.0既有，t=10） | t=10 | C3、C5 |
| 割线窗口取 `persistence_periods` 与取 `funding_gap_window` 两读产出不同布尔 | t=15/17 | C4 |

### (b) 期望值独立推导路径声明

`EXPECTED_TRIPLES` 为**静态字面量**，非运行期计算产物：不 import harness、不读 `harness_output.json` 生成，亦不由 assert_check.py 内任何第三实现路径推算。理由——若由第三实现推算，该实现与 harness/复算共享同一操作化选择时协同变异仍可同步逃逸（裁决31-F 已指认此为协同逃逸根因）。推导链为人工算术（gap→cum_gap→三元组逐点求值），全过程留痕于 assert_check.py 之 `EXPECTED_TRIPLES` 上方注释块，可离机复核。**推导先于运行**：钉表写定后方执行 harness，两者逐点一致，未回改钉表。

### (c) 断言基线解析段升级读法声明

31-C(c) 之"由'关键词在场'**升为**'公式结构可判'"，本会话取**增补**读法：新增 `source_formula_structure_vs_impl_symbol_vector`，保留原 `source_anchor_T3_line609_programmatic_parse`（后者锚定槽位名与聚合语义关键词，前者不覆盖该面），不减损既有覆盖面。若 KD 认取"替换"读法（删去关键词在场项），本项待裁——见修复会话收尾呈报。

### 断言重跑结果（事实陈述，非判定）

- `verify/assert_check.py`：**27/27 项通过，exit code = 0**。项数分解（KD裁定"丙"式）：v1.0原始项 11 ＋ v1.1修复新增项 16 ＝ 27。
- `verify/negative_control.py`（构造侧留痕，**非外审重放、非合格判定**）：七例（M3/M7 单侧；C1/C3/C4/C5/C6 协同）注入后 assert_check.py 退出码均为 1，注入失败 0 例。逐例首个失败断言：M3／M7＝`numeric_compare_shape_evaluation_bit_exact_a0`；C1＝`source_formula_structure_vs_impl_symbol_vector`；C3／C5＝`expected_triple_t10`；C4＝`expected_triple_t15`；C6＝`expected_triple_t13`。注入锚点串与替换串明文列于该脚本 `MUTATIONS` 常量，供增量复核会话比对——**本会话不主张与外审侧注入等价，不作击落率判定**。

### A/B 产物零变动核验（四-4）

机械逐键比对 v1.0 与 v1.1 之 `harness_output.json`／`ledger_input.json`：`gap_series_cents` 前12项一致、`cum_gap_cents` t=4..12 全键一致、`shape_evaluation` t=4..12 全键一致、`periods` t=1..12 逐字段一致、`slots` 一致。对象语义未动、类一定案公式未动、schema 实质内容未动。**未触发四-4停并登记条件。**

### 摩擦续记（四-7）

本会话为 chat 侧构造，无设备挂载目录 git 写操作，未复现摩擦记录_T3-18 之 F7 故障模式；F1–F6 亦未复现。**新增一条 W 候选行**（形态比照摩擦记录_T3-18，呈凑批会话，不单独立号、不自裁登记）：

- **W候选-F8 · 修复会话之 git 落档面与会话物理位置错配**：手册 v1.4§3.4 定本地目录为权威载体、改动经 git 落档；本修复会话在 chat 侧沙箱产出，容器内**未建平行 git 仓**（建之则制造第二个权威载体，与§3.4冲突），git 落档为设备侧下游动作。留痕供凑批会话评估：修复类会话之"产出会话／落档会话"分离是否需成文，或修复会话是否应一律置于 Cowork 侧执行。非阻断。

## KD登记项

- **容差实测值**：子口径(a)＝0（bit-exact）。逐期funding gap、滚动窗口累计gap、破坏形态判定三层输出，harness与独立复算（前缀和技术）结果完全一致——整数精确运算天然保证bit-exact，非浮点舍入意义下的"实测收敛值"，无需另行登记容差数值。
- **TPL3-T318-01候选登记提案** —— **已裁（裁决31-G，20260726）**：登记生效（类三·阶段B校准项，独立条目，以指针关联治理册槽位候选信号#7与TPL3-T24-01，三者域不同不并入）；编号规范＝对象ID去连字符，旧书写形态废止。登记文本另纳外审两条补充（量纲留痕：移项消去除法之等价性仅在 `slope_threshold_cents=0` 时成立；C4读法列为候选：斜率窗口取 `persistence_periods` vs 取 `funding_gap_window`）。另见裁决31-M新登 TPL3-T318-02（`roll_cost_t` 实现/预估两读）。本栏为状态指针，v1.0提案措辞保留于"本根裁定登记"第3条不改写。
- **TPL1-T318-01骨架撞墙信号**：呈骨架归纳/生长阶段（阶段2/3）消费，比照A6处置模式（见"本根裁定登记"第8条）。
