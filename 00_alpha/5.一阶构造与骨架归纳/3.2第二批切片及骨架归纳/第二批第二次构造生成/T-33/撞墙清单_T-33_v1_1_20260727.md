# 撞墙清单 · T-33 · v1.1 · 20260727

> 会话身份＝修复会话（新会话身份，非原构造会话、非外审会话）。本次修订＝D-5订正（施工过程发现事项#2扫描面对象集陈述订正为实测值）；TPL1-T33-01内容**不变**（裁决34-B2边界6："不得触动已裁判定"，TPL1-T33-01非本轮修复对象）。

模板一登记：1 条（TPL1-T33-01，内容与v1.0一致）。
模板二登记：0 条。
§6.3 缺陷回传：0 条（本文档为缺陷回传之修复产出，非新回传对象）。

---

## 模板一 · TPL1-T33-01（内容不变，随v1.0）

| 字段 | 内容 |
|---|---|
| 条目ID | TPL1-T33-01 |
| 缺口类型 | 缺定义（类二·语义约束也无） |
| 缺口描述（仅功能需求） | 短端接货/出货循环之成本摊薄P&L规则的权威计算定义：给定路径与短端持仓，各次义务区穿越触发的接货（以strike-premium折扣成本获得底层资产）/出货（covered call形成轮空收入或等效出货价）循环，其现金流账本如何逐次计算、累计成本基础如何随循环更新。00A行101（T-33权威定义源）与00B §1原型说明仅有边界叙事，无逐次循环的计算规则（触发条件、成本基础更新公式、循环状态机）定义 |
| 触发切片 | T-33（低IV震荡磨损路径） |
| 触发场景 | entity/input_schema.md §1对象语义与§3权威定义引用须完整转录00A行101定义文本；该分句所指循环规则本身无权威计算定义，构成本对象文档必然触及但Q4可算范围明确排除计算之相邻缺口 |
| 出处 | 00A§5行101（权威定义源）；00B§1（原型叙事边界）；同型相邻先例＝TPL1-TC33-01——**不合并**：机制主体（对冲方 vs 策略自身持仓）与消费场景不同，比照TPL1-T303-02"相邻但非同一缺口"注记处置（裁决31-G O7） |
| 当前占位处理 | 无——本根Q4判据明确不要求计算该循环之现金流，仅在语义文档层如实携带定义引用 |
| 阻断性 | 可继续（本根A→D已跑完，判定不翻转；该缺口不进入本根crossing_count/rv_proxy之计算路径） |
| 建议归属（仅指针） | 与TPL1-TC33-01同域相邻，是否合并归属同一未来对象或分立，待骨架归纳消费缺口信号后反推 |
| 状态 | open |

## 施工过程发现事项（如实记录，供外审/增量复核知悉；均为核验脚本自身实现问题或本会话订正记录，非上游产物错误、非施工对象缺口）

1. **三禁形核查脚本首版假阳性**（v1.0既有，内容不变）：entity/input_schema.md §5初稿在引用块外的说明性文字中以引号复述"该路径即P_B唯一失效路径"（否定语境），触发`verify/three_forbidden_forms_check.py`对"唯一失效路径"字面短语的启发式扫描命中。改述为不复述该字面短语，复跑清零。核验脚本设计未改（保持启发式，不引入语境判别以防被绕过）。

2. **step0静态扫描脚本自扫描假阳性，含扫描面对象集陈述（v1.1订正，外审D-5缺陷）**：`verify/step0_static_scan.py`源码内联W-8三部分forbidden关键字模式定义，首次真跑对自身文件扫描命中假阳性，修正为扫描目标集排除脚本自身文件。**本根扫描面对象集实测＝六份脚本**：`entity/harness.py`＋`verify/independent_recompute.py`＋`verify/source_anchor_parse.py`＋`verify/three_forbidden_forms_check.py`＋`verify/mutation_test.py`＋`verify/assert_check.py`（`step0_static_scan.py`自身排除）。**[v1.1订正]**：v1.0本条原文表述"排除范围内（entity/harness.py、verify/独立复算与三禁形核查脚本）"仅列3份（遗漏`source_anchor_parse.py`/`mutation_test.py`/`assert_check.py`），与`step0_static_scan.py`之`TARGET_DIRS=["../entity","."]`实测扫描六份（外审报告§1第15项、切片记录§6同步核实）不符，构成外审D-5缺陷（陈述与实现不符，方向为实现强于陈述）。**本条订正为实测六份**，人工复核交叉确认六份脚本零实际forbidden import/调用命中。

3. **三禁形核查脚本第二例假阳性（v1.1新增）**：修复动作(d)在entity/input_schema.md §6新增六读警示随注初稿含"不得写成硬阈值、不得写成自动触发规则"字样，触发`check_auto_trigger()`对"自动触发"字面短语之朴素扫描命中（否定语境假阳性，与本清单事项#1同型——**第三次同型再犯**）。处置比照#1先例：不改核验脚本语境判别逻辑，改述警示随注为不逐字复述禁止短语、转指回§3引用条款，复跑清零。

4. **harness.py头部docstring族数陈述连带订正（v1.1新增，本会话主动一致性订正）**：v1.0 docstring"生成族P...与族Q...两族路径数据"与v1.0实际生成五族不符——此表述失实早于v1.1即存在，非外审D-6原点名条目（D-6专指input_schema.md）。本会话新增族B之机会一并订正，如实记载，不涉及计算逻辑变动。

## 治理联动纪律六.6留痕（v1.0记载，本会话未新增此类事项）

本会话执行环境＝claude.ai chat+bash_tool沙箱，与手册v1.4§3所设定之Cowork本地目录不符——沿用v1.0/原构造会话已挂之同一环境差异事项处置（裁决34-E3并入构造会话既有挂号，不重复登记），本次修复会话不新增此类摩擦记录。
