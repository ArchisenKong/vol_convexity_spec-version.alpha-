# 切片记录 · GK-14 Vega六形态（GK版）· v1.1 · 20260712

> **v1.1修订（裁决18落地）**：①B1边界句补"经裁决18修正后追认"（v1.0施工方直接宣告措辞越任务包§4纪律，宣告权归KD，记录在案不翻转判定，R1）；②surface/grid坐标定性依据指针修正——实为任务5 Step1产出（v1.3）行264，v1.0误标任务3（D-T6-01同类：依据真实存在、指针错，R4）；同一指针错误存在于entity/input_schema.md §2.5，同源同修（schema升v1.1）；③KD登记项新增scenario_set候选枚举定性句（R5）；④本根裁定登记补裁决18指针与R5条目。A/B产物（input_data/harness/harness_output）零变动，合格判定维持通过。外审夹具观察项O1（M2变异检出方式为KeyError崩溃非干净FAIL）留痕于撞墙清单v1.1核验脚本自查记录段。

## 对象语义（必填，B1）

GK-14 ＝ Vega六形态（GK版）：与T-74同族同路径（Step2 §2.1行83；Step4 §4.2第5条"转译强度轴为标注不改判定"），一阶实体。Q4判据（经T-74继承）＝"喂node级raw Vega账本+形态规则/情景，出对应形态Vega值，判据＝与raw复算一致"；GK-14增量＝输出形态为多分量度量+双轴标注，附加断言＝复算一致、轴分离标注齐备。六形态定义源＝GK§6.2（行482-493）：raw Vega / tenor Vega / forward-segmented Vega / surface Vega / grid Vega / scenario-stressed Vega，每形态携带两条独立轴线（`方法论状态/转译强度` × `运行权限Phase`，行484明文"两者不得合并为一个'所属层'"）。

**GK-14实体算法要件**＝对node级raw Vega账本按六条形态规则执行的聚合/映射/标注机制（tenor分桶加权求和、forward分段聚合、surface/grid坐标归类、scenario线性重估近似、双轴标注附着），对具体权重取值/分段边界/情景幅度中立（同型于裁决14/16/17：per-lot Greeks/对冲政策/情景重估实现均为上游输入，GK-14不重算T-76已完成的per-lot→node聚合，直接消费node级raw Vega账本）。**本边界经裁决18修正后追认**——v1.0施工方直接宣告、未用"候选/待外审裁定"措辞，越任务包§4纪律（宣告权归KD），记录在案不翻转判定。三处机制成分经三分判别自查：forward-segmented分段规则＝类二缺定义（登记TPL1-GK14-01）；tenor权重数值＝类三阶段B校准项（w_τ=1候选，不登记）；grid/surface坐标schema＝本根工程裁定（复用T-76已有坐标维度，任务3裁定其"属切片逼出物"，不登记）；scenario-stressed重估实现＝缺实现型，引用TPL1-T76-01不重登（同裁决17对T3-03模式）。

**实跑印证**：以7个node构造premium income（30D/90D短仓）+wing protection（90D/180D长仓）结构的node级raw Vega账本（继承T-76六维坐标系之tenor_bucket×moneyness_bucket子集）。tenor Vega在90D桶将N3(+75)/N4(+70)/N5(-40)/N7(-20)四node等权求和为85，与GK§6.1.3"跨期限Vega未调整前不可比较"边界一致——桶内聚合先于跨桶比较,未做跨tenor直接净额裁决。surface/grid Vega在90D×put_wing cell将N3/N7聚合为55（唯一发生真实多node聚合的cell），体现"scalar/单node观察不足以代表cell风险"（呼应GK§6.1.2"ATM Vega不能代表wing Vega"）。scenario-stressed Vega线性近似（k=1/20）对全部7node及组合汇总（-15→-63/4）保持线性关系，量级与方向可从raw账本+声明独立复算得出，无黑箱成分。语义与实跑行为一致。

## 合格判定（必带，B2）

**通过（合格）**。

判定依据：D段三步全过——step0前置合规通过（input `_data_source=synthetic_hand_constructed`确认；harness.py静态扫描剥离注释/docstring后0命中forbidden模式，含W-5 bare import模式；independent_recompute.py剥离注释/docstring后0处harness耦合痕迹，人工grep交叉核验含"harness"字样3处命中，均落在文件头/行内独立性声明的docstring或`#`注释内，非实际代码耦合，见撞墙清单"核验脚本自查记录"段）；数值比对33项全部diff=0（裁决13子口径(a)：离散/精确有理计算，bit-exact，全程`fractions.Fraction`）；附加断言3项全部通过（六形态计数=6且集合与GK§6.2一致；双轴标注齐备、两轴独立字段不合并、取值经**独立程序化解析GK§6.2源文**（非复用harness/独立复算两侧手工转录）比对逐字一致；每形态均为≥2分量的多分量度量，分量均携带`lineage`可追溯指针）。判定口径出处＝裁决13（任务6 v1.6 D段①）+任务包§2 D段附加断言口径。

**完成态口径**（G-3口径）：本根撞到1处缺定义（TPL1-GK14-01），已登记＋占位继续，未阻断A→D跑完，属正常副产物，不影响"通过"判定。

## 一阶实体文件清单

| 文件 | 说明 |
|---|---|
| entity/input_schema.md | **v1.1**：坐标继承说明（T-76六维坐标系子集）、六形态算法declaration（含权重/分段/坐标/情景四类机制成分三分判别结论）、T-19轴分离承接说明、容差口径声明（§2.5指针依裁决18/R4修正） |
| entity/input_data.json | A段人造input：7 node级raw Vega账本（premium income短仓+wing protection长仓结构）+ 形态规则/情景声明，`_data_source: synthetic_hand_constructed` |
| entity/harness.py | B段一次性harness：命令式for循环+dict累加风格，`fractions.Fraction`精确有理数运算，六形态全部实跑 |
| entity/harness_output.json | C段实跑真数：六形态×（多分量值+双轴标注+lineage） |

## 撞墙清单条目

非空。模板一1条（TPL1-GK14-01，open，类二缺定义型：forward-segmented Vega真实分段规则）；另引用TPL1-T76-01（缺实现型，scenario-stressed Vega重估实现供给方，裁决17模式，引用不重登）；模板二0条；§6.3回传0条。详见`撞墙清单_GK-14_v1_0_20260712.md`。

## 检验通道记录

**step0结论**：通过。(i) input `_data_source=synthetic_hand_constructed`确认；(ii) harness.py静态扫描（剥离注释/docstring后关键词匹配，含W-5 bare import模式`^\s*import\s+(requests|urllib|socket|yfinance|ibapi|ib_insync|pandas_datareader)\b`）0命中；(iii) independent_recompute.py未import harness模块、未读取harness_output.json，剥离注释/docstring后二次扫描0命中，人工grep交叉核验剥离前3处"harness"字样（行4/5/20）确认均落在模块docstring或`#`注释内，非实际耦合。核验脚本自身曾对该独立性自述文本产生假阳性（在剥离前原始文本上匹配），已定位为核验脚本实现问题并修正，详见撞墙清单核验脚本自查记录段。

**独立复算实现方式说明（何以路径独立，何以核心分叉）**：
- 数据源独立：`verify/independent_recompute.py`直读`entity/input_data.json`，不触碰harness产物。
- 计算核心分叉（非取字段/循环外壳分叉）：
  - tenor/forward-segmented分桶——harness用手工dict累加（`if tau not in tenor_sum: ...; tenor_sum[tau]+=...`逐node遍历累加），独立复算用`itertools.groupby(sorted(...))`分组后生成器求和（`bucket_by`函数）；
  - surface/grid cell聚合——harness用手工dict累加（同上模式，单一`cell_sum`循环内完成），独立复算用`collections.defaultdict`预分组+独立`cell_value()`函数按需生成器求和（分组与取值两阶段分离，非同一遍历内累加）；
  - scenario-stressed逐node——harness用for循环显式构造字典并原地累加`total`，独立复算用列表推导式`[(nid, F(...)*multiplier) for n in NODES]`构造元组列表后对其做内置`sum()`生成器求和；
  - 六形态双轴标注——harness与独立复算两侧`AXIS_ANNOTATION`字典各自独立手工转录GK§6.2表（非共享同一份数据结构或互相复制），断言基准另由`assert_check.py`第三次独立**程序化解析**源文件`### 6.2`至`### 6.3`区间表格取得（正则抽取，非手工转录），三路互不复用，满足任务包"断言基准须独立锚定源文，不复用harness侧转录"强制要求（T3-03外审教训订正项）。
  四处核心表达式均分叉，非共用同一遍历/累加结构；均使用`fractions.Fraction`精确有理数（平凡primitive，允许共用）。

**手工第三方三角化**（本根全有理数运算，人工笔算对撞墙关联成分——tenor Vega权重聚合、surface/grid真实多node聚合cell、scenario-stressed线性近似——做三方核验）：

```
tenor Vega 90D桶（w_τ=1等权，撞墙关联：GK§6.1.3权重待校准）：
  N3+N4+N5+N7 = 75+70+(-40)+(-20) = 85 ✓ 与harness/独立复算一致

surface/grid cell (90D, put_wing)（唯一真实多node聚合cell）：
  N3+N7 = 75+(-20) = 55 ✓ 一致

scenario-stressed Vega（k=1/20，multiplier=21/20，撞墙关联：GK§2.3.1重估类一定案）：
  逐node：N1: -80×21/20=-84 ✓
          N2: -70×21/20=-1470/20=-147/2 ✓
          N3: 75×21/20=1575/20=315/4 ✓
  raw总额（未加权）：-80-70+75+70-40+50-20
    = (-80-70)+(75+70)+(-40+50)+(-20) = -150+145+10-20 = -15
  情景总额（线性性质核验，非重复累加）：-15×21/20 = -315/20 = -63/4 ✓ 与harness/独立复算aggregate_total一致
```

三方（harness / 独立复算脚本 / 人工笔算）结果一致，无分歧。

**逐项比对结果表**（裁决13子口径(a)：0，bit-exact；全部非零diff无论限内外如实记录——本根全部为0，无非零项；33项全量列出，见`verify/comparison_table.tsv`，摘要如下）：

| form | 分量数 | 全部diff=0 |
|---|---|---|
| raw Vega | 7 | 是 |
| tenor Vega | 3 | 是 |
| forward-segmented Vega | 3 | 是 |
| surface Vega | 6 | 是 |
| grid Vega | 6 | 是 |
| scenario-stressed Vega | 8（7 node + 1 aggregate） | 是 |
| **合计** | **33** | **33/33限内，0项越限** |

**附加断言结论**：通过。①六形态计数(6)与预期集合一致（raw/tenor/forward-segmented/surface/grid/scenario-stressed Vega）；②双轴标注（`methodology_status_translation_strength`、`operating_phase`）全部在场，两轴以独立字段承载未合并，取值与GK§6.2表原文（行486-493，经`assert_check.py`独立程序化解析取得，非复用手工转录）逐字一致；③形态齐备（每形态均为≥2分量的多分量度量，每分量携带`lineage`可追溯指针，指向`node_ledger`内具体node_id，无缺失）。

## KD登记项

- **适用容差子口径**：裁决13子口径(a)——离散/精确有理计算，0（bit-exact）。理由：node级raw_vega全部人造为整数，六形态全部转换（等权求和、坐标归类、线性乘法近似）均为`fractions.Fraction`有理数`+ - ×`运算，无sqrt/exp/log等超越函数，无浮点舍入风险，天然满足bit-exact。实测值：33项全部diff=0。
- **B段计算路线选择**：全形态离散有理数算术路线（无定价引擎重估、无连续解析函数）。理由：Q4判据"喂node级raw Vega账本+形态规则/情景，出对应形态Vega值，判据＝与raw复算一致"未要求任一形态经BS/定价模型重估；scenario-stressed Vega的重估路线（真实定价模型co-move重估）依裁决17模式引用TPL1-T76-01（缺实现型），本根不实现重估、以线性近似桩占位，不重复登记。
- **撞墙登记判定（依裁决14/16/17同构教训，本会话施工阶段主动自查，非外审订正）**：
  - forward-segmented Vega分段规则：类一测试（GK§6.1.4仅declare需观察未来时间段分布,未给分段算法,查过GK§6.1.4/GK§6.2/候选池均无可径直裁定文本）不通过→类二测试（候选池检索无唯一明确待建供给方,亦非纯参数值问题——分段边界本身是结构性选择）判定为缺定义，登记TPL1-GK14-01，占位＝复用tenor_bucket作最简确定性桩。
  - tenor Vega权重w_τ：类一测试部分通过（GK§6.1.3裁定加权求和结构+禁止naive净额，但权重数值本身"是模型化或经验性假设,需重估和审计"=明示阶段B性质）→判定为类三阶段B校准项，不登记，声明w_τ=1候选值不外推。
  - surface/grid坐标schema：GK§6.2边界"未冻结"/"需另审"+任务5 Step1产出（v1.3）行264对GK-16定性（"surface实现坐标候选；surface Vega语义由T-74/GK-14承载，坐标属切片逼出物"；v1.0误标任务3，裁决18/R4修指针）→判定为本根工程裁定（复用T-76既有坐标维度，非新造），不登记模板一/二；correlation matrix超出Q4判据范围，范围裁定不登记。
  - scenario-stressed情景：三段拆分沿用裁决17对T3-03的现成结论（幅度值=数据不登记；作用规则形态=重估,类一已定案；重估实现=缺实现,供给方TPL1-T76-01引用不重登）。
  - 〔映〕边界自查：forward-segmented Vega实现未引入TC-12 `forward-vol bucket`语汇/字段，字段名`forward_segment`为GK-14自有声明，合规。
- **scenario_set候选枚举定性（裁决18/R5）**：surface Vega输出中的`scenario_set_candidate`（parallel_shift/skew_tilt/local_deformation）＝数据类元数据，源于人造声明照抄，未对其执行任何计算，未冻结，**非归纳可消费的已验证产出**；保留于实体输出以如实反映GK§6.2行491"scenario set未冻结"状态，骨架归纳时依本定性过滤。
- **T-19承接实现情况**：正交轴声明（形态×权限不得合并计数体系，任务3行353〔约〕）在本根以两独立字段`methodology_status_translation_strength`/`operating_phase`承载，全程未合并入单一"layer"/"tier"字段、未互相派生，GK§6.2行484原文正交要求逐条落实。GK-14不新增角色链内容（T-19本体六角色语义不在本根输出结构中，本根仅在双轴标注结构层面维持正交），无§6.3回传登记项。

## 本根裁定登记〔W-1字段，KD裁定D5授权提前适用〕

| # | 裁定语句 | 指针 |
|---|---|---|
| 1 | 实体边界：六形态聚合/映射/标注机制＝要件，权重取值/分段边界/情景幅度＝上游或阶段B（施工阶段主动适用，**经裁决18修正后追认**——直接宣告措辞越§4纪律记录在案） | 本记录 B1；裁决18 |
| 2 | node级raw Vega账本坐标继承T-76六维坐标系（leg_type×side×option_type×tenor_bucket×moneyness_bucket×scenario）之tenor_bucket/moneyness_bucket子集，tenor_bucket排序30D<90D<180D、moneyness_bucket排序put_wing<atm<call_wing——本根工程裁定，不外推为跨根命名规范 | input_schema §1 |
| 3 | tenor Vega权重w_τ=1（等权），阶段B校准候选之一，待B证伪，不登记模板一/二 | input_schema §2.2；本记录KD登记项 |
| 4 | forward-segmented Vega分段规则占位＝复用tenor_bucket，缺定义类二，登记TPL1-GK14-01，占位不claim语义等价 | 撞墙清单TPL1-GK14-01；input_schema §2.3 |
| 5 | surface/grid Vega cell坐标＝(tenor_bucket, moneyness_bucket)，复用T-76既有坐标维度，本根工程裁定不外推为production grid/schema；correlation matrix出域不计算（范围裁定非能力缺失）；定性依据＝任务5 Step1行264（v1.0误标任务3，裁决18/R4修正） | input_schema §2.4/§2.5（v1.1）；裁决18 |
| 6 | scenario-stressed Vega情景三段拆分：幅度值(S1)=数据；作用规则形态=重估(类一定案，源文同裁决17引用)；重估实现=缺实现，供给方TPL1-T76-01引用不重登；本根线性近似k=1/20为占位桩 | input_schema §2.6；裁决17 |
| 7 | 断言基准（附加断言②）须独立程序化解析GK§6.2源文，不得复用harness/独立复算任一侧手工转录——T3-03外审教训在GK-14本根落地为构造时点强制项，非事后订正 | verify/assert_check.py；任务包§2 D段"T3-03外审教训"条款 |
| 8 | T-19轴分离承接：双轴以独立字段（methodology_status_translation_strength / operating_phase）承载，不合并入单一layer/tier字段 | input_schema §3；本记录KD登记项末条 |
| 9 | scenario_set候选枚举＝数据类元数据，未冻结，非归纳可消费的已验证产出，保留于实体输出、归纳时过滤 | 本记录KD登记项；裁决18/R5 |
