# 执行侧行为约束（AGENTS.md／CLAUDE.md，P2 工作目录根，agent 自动读取）
# 权威指针＝P1规格件 v1.3 §4A/§4B（KD裁定20260811追加-1/追加-2；20260812 X-2凭据补件）

1. 禁止修改 entity/engine.py——任何形式，含「临时改一下试试」。运行前后 SHA256 必须等于 d88a955e45dddaa5a11f02c9237cb5e178bdfca6335a670ecdf21414dca682cf。
2. 禁止为让代码跑通而填任何未在规格件中显式给定的数值。缺参数＝立即停止，报告缺什么；不猜、不用占位值、不用行业惯例默认值。
3. 撞到 engine 契约面问题（输入形态不匹配、路由不通、口径疑问）→ 停下，按四栏格式写入摩擦记录，不绕道、不做适配性猜测修补。
4. 允许自由调试的范围＝适配层与 harness 自身的工程 bug（连接、解析、IO、类型转换）。凡涉及数值语义或 engine 契约面的改动，不属于此范围。
5. dividend_yield 与 risk_free_rate 两字段：本工作目录任何代码中禁止出现其数值字面量赋值；唯一取值路径＝probe/kd_params_probe_.json（KY 手工填写）。文件缺失或字段为空 → 立即停止并向 KY 索取，禁止代填。
6. flex_token／flex_query_id 等账户侧凭据：唯一取值路径＝运行时环境变量 IB_FLEX_TOKEN／IB_FLEX_QUERY_ID。禁止写入任何代码字面量、任何配置文件、任何交付件、data_manifest、摩擦记录原文。缺失 → 立即停止并向 KY 索取，禁止代填、禁止硬编码。
