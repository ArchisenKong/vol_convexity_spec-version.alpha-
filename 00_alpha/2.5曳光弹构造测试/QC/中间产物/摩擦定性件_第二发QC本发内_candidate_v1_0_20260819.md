# 摩擦定性件 · 曳光弹第二发（QC）· 本发内 · candidate · v1.0 · 20260819

> **性质**：《P3' 汇整件_曳光弹第二发QC_candidate_v1_0_20260819》§4 之姊妹件（KD 20260819 裁定分列）。**射程严限于本发内**——不作跨发结论、不作两发合并归纳（后者见合并归纳件）。
> **会话身份**：P3'，汇整复核 session。定性为**呈报候选**，非裁定。
> **三分形态**：照第一发 P3 汇整件 §3「九条摩擦三分」之同一分类轴（①机制按设计生效之正向证据／②真实环境教出之纪律／③己方防线已探明盲区），**保跨发可比性**。采用同一分类轴系形态借用，**不构成两发同型之结论**。
> **反面条款（85-9）自守**：不产出「成功／失败」二值结论、不作「环境可用／不可用」总体判定；F1／F2 均为有效产出，非不合格。
> **禁作面自守**：零 L3 判据、零 L3 实体、零合格判定；不预判 29-B；不变更 DOC-25 兑现点。
> **材料面**：环境摩擦 FR-P2-01～16（16 条）／本位工程缺陷 BUG-P2-01～07（7 条，**非环境摩擦，分列**）／P3' 复核发现 P3-a～m（13 条）。

---

## §1 三目标之三态汇总（事实登记，非判定）

| 面 | 三态 | 关键事实 |
|---|---|---|
| α-1 (i) `.py` 内嵌常量注入 | **F0** | 三载体件 `import: ok`；五件还原后 SHA256 全部命中锚值 |
| α-1 (ii) ObjectStore 算法侧读取 | **F2** | `LocalObjectStore.ContainsKey()` 权限拒；`enumerated_keys` 空数组。**答 E1-12 明载之「回测 job 侧读权限未测」——本发已测，结果为拒** |
| α-1 (iii) `AddData` 自定义数据类型 | **F0** | `add_data.ok=true`，数据源＝自造确定性序列（3 行人造常量），零市场数据、零 vendor 格式 |
| α-2 路径系综规模与遍历 | **F0** | 1 次回测承载 3 条指定路径，逐条按 symbol 可分辨（各 4 点）；「撞到」三分支（只能单路径／须 N 次独立回测／路径不可指定）**均不成立** |
| α-3 五量占位与账本接口在场性 | **F0** | 三类账本接口（持仓账本／现金流水／成交明细）全在场；五式全可写出并产数，零 Greeks、零定价模型量（L3 铁律2 未触碰） |
| α-4 断言开销与回传路径 | **F0＋F2** | 通道在场（三处分工可成立；诊断面可得）；日志通道配额面 F2 |
| α-5 证伪器语义可承载性 | **两半分裂** | 产出面**撞到**（27 键中 22 键为收益/风险类，无关闭开关）；取回面**未撞到**（`SetRuntimeStatistic` 自定义键按前缀机械可分，可只取账本量）。**不合并判定** |
| α-6 载体根 GL-06 环境内可运行性（甲乙两位） | **F0** | 双值并列（KD-P2-06）：两位 `byte_exact` 与 `byte_exact_normalized` 均 True，`newline_profile` 与锚件同 |
| X-1 跨环境浮点素材（三位） | **F0** | 素材完整：libm digest 甲异乙丙同；7 探针中 `erf(0.7)`／`expm1(1e-8)` 甲乙各差 1 ULP、乙丙同；引擎 6 量中 `vega` 甲乙差 437 ULP、乙丙同，余 5 量三位逐位全同 |
| Docker 本地面 | **F2** | `command not found`（exit 127），与 E1-01 一致；本发两运行位均在云端侧 |

---

## §2 第一组 · 机制按设计生效之正向证据（8 条）

**该组之共同性质**：既设条款、闸位、隔离面在真实外部环境内**被触发并按设计动作**，非「未被考验而看似成立」。

| # | 正向证据 | 事实基础 |
|---|---|---|
| **正-1** | **A'-9 即停条件按设计生效** | 回测必须云端在场含 `QCAlgorithm` 子类之 `main.py` ⇒ 按 A'-6 落 `probe/`、按 A'-9 不上云 ⇒ 直接对撞。P2' **停于该点、呈 KD、未就地扩射程**（KD-P2-10）。即停条件之立法目的（防射程蠕变）**实测兑现** |
| **正-2** | **作业指令 §9⑤ 按设计生效** | α-6-乙 于 research 位 4/4 `Connection not found.` ⇒ 记 F2 并停该位、另一位继续。**未以「跑通」为目标绕行** |
| **正-3** | **授权作出后新增实质信息时不以既有授权覆盖** | KD 曾放行 research 位；其后 E27 日志显示 `research/create` 在 free 档实测失败且**失败后将节点置于 busy 态**（即卡死状态之制造动作本身），而卡死态无端点可解（`research/stop` 不存在）＋不得以破坏性动作清理（A'-14）。P2' **停在此处呈 KD（KD-P2-07）**，未以既有授权覆盖新信息。**该形态为本发最强之纪律正向证据** |
| **正-4** | **D-2 黑名单渗透主判据零命中，经 P3' 独立复算证成** | 89 条黑名单 × `entity/`∪`verify/` 全量，P3' 现场重跑命中 0（非采信 P2' 自陈） |
| **正-5** | **指纹链完整：载入即钉、链路前后全等、零写入可证** | `load_fingerprint_baseline.json` 与 `entity/` 五件同刻（21:16:20）落盘，早于首次外部调用（21:43）；前值/后值/P3' 实测三值全等。**核证点另延伸至远端还原点**——载体件内嵌 SHA256 于运行位内自核（`restore()` 不一致即抛），回测位 4/4、research 位 5/5 命中锚值 |
| **正-6** | **双值口径（KD-P2-06）在两位均产出可分辨记录，且预判被实证** | P2' 于本地对照组预先登记「云端为 Linux（LF），逐字节比对很可能 PASS，且该 PASS 之一部分由行尾约定贡献、非纯计算等价之证据」。实测两云端位 `CR=0`、`byte_exact` 直接 True ⇒ **差异源已定量归因于行尾（+149 B ＝ 149 个 CR），非计算面**。若无本地对照组，该 True 将被误读为「GL-06 在 QC 环境内输出一致」 |
| **正-7** | **本位工程缺陷与环境摩擦显式分列** | BUG-P2-01～07 逐条给成因（调用签名错／传参位错／转义层／字典覆盖／解析器误判／`None` 传入／heredoc 转义），**A 轮 FAIL 日志原样保留不删不修饰**。立法目的＝防后续消费方将本位缺陷误读为环境阻断 |
| **正-8** | **A'-14 破坏性动作全域零命中，经 P3' 零剥离原文复核** | `push`／`files.delete`／`object-store delete`／`projects.delete`／`backtests.delete` 之调用面命中 0；8 处原文模式命中全部落在 module docstring／注释内。云端项目、云端文件、ObjectStore 遗留物、探测期本地遗留物**全部未删未改** |

**负向并置（须显式，防单向读）**：**正-5 之「载入即跑首次核证」在实质面兑现，但形式面未走断言脚本**（DEV-P2-01）；正-4／正-8 之判定基面存在声明与实现不等价（P3-d），详见第三组。

---

## §3 第二组 · 真实环境教出之纪律（9 条）

**该组之共同性质**：Phase A 纯函数世界内不存在、只能由真实外部环境反推得出之失败形态。**为一阶实体面材料，其二阶规矩之成文与否归 KD。**

| # | 形态 | 一阶实例 | 反推指向 |
|---|---|---|---|
| **教-1** | **约束叠乘方显形；单独任一不触发** | E1-08 扩展名白名单（拒 `.md`/`.json`）迫使改 base64 载体 → base64 膨胀 ≈+33% → 越 32,000 字符上限（FR-P2-06）。**两约束单独均不阻断，叠乘方阻断** | 规格件 E1 映射表增补候选；探测阶段之约束枚举**不足以预测叠乘面** |
| **教-2** | **失败非原子：调用抛异常 ≠ 服务端无副作用** | `files.create` 超限抛 `RequestFailedError`，但服务端**已创建 1 字符文件条目**（FR-P2-07）。P2' 曾据「抛异常」推断「无部分写入」，该陈述已自陈更正 | **与 A'-14 之交互面**：`files/delete` 全禁之下，若当时另建新项目，该 1 字符件将成为**不可清理之遗留物** |
| **教-3** | **诊断信息不指向真实约束面（本发四例同族）** | ①项目存在却报 `does not exist or you do not have the necessary permissions`（FR-P2-02）；②项目已删却返回 `[OK] list, 0 项`（FR-P2-05）；③行号参数报「It must be a valid timestamp」（FR-P2-13）；④源路径为 null 却报错于 `CloudDataPermissionManager.cs:line 92`（FR-P2-15） | E1-19（HTTP 200＋`success:false`）；**成败与存在性之语义不由状态码/异常承载** |
| **教-4** | **存在性之两条推断路径均被实测推翻** | 「以调用成功推断对象存在」与「以错误文案推断对象不存在」**双向失效**（FR-P2-05）。P2' 后续一切存在性判定改以 `projects.get_all()` 清单**枚举比对**承载 | 断言脚本之成败判定形态；跨环境断言设计 |
| **教-5** | **配额是不可撤销资源，且日配额使重试不解** | 日志通道 free 档 **10 KB/回测且 10 KB/天**（FR-P2-12）：当日耗尽后重发回测**亦无法取得日志**。回测配额本发共耗 4 次，其中 1 次因本位缺陷 BUG-P2-06 失败，**消耗不可撤销**（E1-17） | α-4 回传路径；M7；**「重试」在配额面不是通用救济手段** |
| **教-6** | **通道选择直接决定指纹链是否成立** | file API 往返**逐字节保真**（`CR: 0→0`，三件全 True，FR-P2-09）；而 `lean cloud pull` 在 Windows 上做 **LF→CRLF 转换**（E1-09）。同一内容、两条通道、指纹链一成立一不成立 | E1-07 回传矩阵；A'-16；D-2 指纹链射程 |
| **教-7** | **单值逐字节判据不可分辨「计算不同」与「行尾不同」** | 本地对照组：produced 4,308 B vs expected 4,159 B，差 +149 ＝ 149 个 `\r`；归一化后逐字节相等且 SHA256 精确命中锚值；JSON 语义相等 True（FR-P2-08）。机制＝`harness.py` 以文本模式写盘，行尾由平台约定介入 | α-6 判据形态；**已由 KD-P2-06 落为双值并列口径**。与 E1-09／A'-16 之 pull 通道转换**机制不同、通道不同**，不合并 |
| **教-8** | **隐式实体：使用者注册之键集不闭合** | `AddData` **隐式创建** QC 内部 userdefined-universe 订阅，其 `GetSource` 落在同一自定义数据类上，symbol（`QC-UNIVERSE-USERDEFINED-USA-BASE`）**不在使用者注册之键集内**（FR-P2-14）。凡以「symbol → 源文件」映射实现 `GetSource` 者**必然遇到映射外 symbol**；该分支返回 `None` 即触发 FR-P2-15 | α-1 候选(iii)／α-2；E1-14 行为面未文档化 |
| **教-9** | **遗留状态可由失败调用制造，且无端点可解** | E1-13 所载「research 节点 busy:true 未释放」之成因经 E27 日志回溯**确证＝探测期自身之 `research/create` 失败调用**：创建请求 190.1s 超时未产出可用会话，却把节点置 busy。free 档单节点 ＋ `research/stop` 不存在 ⇒ **无端点可解**，且不得以破坏性动作清理（A'-14）。可靠观测位＝节点 `busy` 状态即会话在场性（本发确证） | A'-12；A'-14；KD-P2-02／03 |

**本组之共同模式候选（限于本发内）**：外部环境之失败面同时具备**叠乘性**（教-1）、**非原子性**（教-2）、**语义不可信性**（教-3/4）、**不可撤销性**（教-5/9）与**通道依赖性**（教-6/7）。**Phase A 纯函数世界内这五维均不存在。**（该模式与第一发结论之关系归合并归纳件，本件不作跨发结论。）

---

## §4 第三组 · 己方防线已探明盲区（8 条）

**该组之共同性质**：缺陷在**己方条款集与检测器**，非环境。**该组为本发最应被 KD 消费之材料。**

| # | 盲区 | 事实 | 归属 |
|---|---|---|---|
| **盲-1** | **条款自不可满足** | A2' 要求 `_pro`+`be_` 于 `entity/`∪`verify/` 命中 ==0；而 D-3 强制 `_data_source` 取值 `external_probe_NOT_FOR_ENTITY`，**该值本身含该前缀** ⇒ 任何于 `verify/` 实现 D-3 检测之脚本**必然含此字面**，原条款在 `verify/` 面**不可满足**（FR-P2-16）。同型第二面：记录件须描述该机制、断言日志须回显命中内容，若禁含则**记录件无法自述、断言无法自证** | KD-P2-11 |
| **盲-2** | **两条款立法时点不同、射程未相交，交叉面未预见** | 乙-5「下载成功即落**原件**」× A8'「凭据零落盘」：对夹带凭据之响应（`research/read` 之 session token、`accounts.get_organization()` 之 `card{brand,expiration,last4}`），**落逐字节原件即违 A8'，遮蔽后落盘即非原件**（FR-P2-01）。乙-5 之归纳基础＝第一发摩擦-07/-08（下载保真面），其一阶实例**不含「响应夹带凭据」形态** | KD-P2-01 |
| **盲-3** | **同批代定间既存不自洽，须至特定运行位方显形** | A'-6（接口面代码全部落 `probe/`）与 A'-9（`probe/` 面一律不上云）：A'-9 之推得依据将 `probe/` 内容枚举为「外部环境响应载荷」，而 **A'-6 同版同批恰将接口面代码置入 `probe/`** ⇒ 二者对「`probe/` 装什么」采用互不相容之模型。**冲突在 A'-6 落定时即已存在**，至回测位方显形 | 已裁（KD-P2-10 采候选甲） |
| **盲-4** | **仪器过度触发吞掉核心证据** | 遮蔽算子兜底正则 `\b[0-9a-f]{40,}\b` 将 `produced_sha256`／`normalized_sha256`／`expected_sha256` **三项全部遮蔽**，`byte_exact=True` 沦为不可独立复核之断言（FR-P2-11）。成因＝算子未区分「来源未知之文本」与「本位自产、schema 已知、零凭据字段之结构化结果块」 | 已自修（两级遮蔽）；登记备查 |
| **盲-5** | **断言射程窄于记录表行集，且二者无对表机制** | A10' 判定射程自陈「M1–M7 ＋ α 六面 ＋ X-1」；作业指令 §7 行集另含「E1 映射表标『需KD裁定』之各行」（13 行）。实测 **5 行零出现**、无 E1 四栏专表，而 A10'=PASS（P3-i）。零覆盖之 E1-15／E1-16 为规格件明列之**乙-4 阻断候选**，且规格件载「E5 强制项：本发内须产生第一个可核证实例」 | KD-P3-04／KD-P3-06／KD-P3-05 |
| **盲-6** | **判定基面之声明与实现不等价** | 声明「剥离注释/docstring」，实现为 tokenize STRING token 全量替换（含代码行内字符串常量）。A2' 面实测差额 2 处（路径字面，与已声明之 `path_index` 同型但未被其射程覆盖）；A4'／A7' 面差额 0（P3-d）。另 A3' 之 json-only 射程收窄未入 BASIS 声明（P3-e） | KD-P3-01／KD-P2-11 |
| **盲-7** | **交付件自陈值面不入丙-3 复核对象** | 丙-3 只检测断言日志 mtime 与 FAIL 数。实测六例失同步：交付清单载 manifest 指纹 `b93c9521…`／实为 `64d92f42…`（P3-a）；件数体积 147/813,161 vs 150/843,569（P3-b）；M3 manifest 体积 359,968 vs 374,375（P3-c）；记录件版头「进行中」（P3-g）；AGENTS.md 闸位指针 v1_1 vs v1.2（P3-l）；回测配额「2 次」vs 4 次（P3-m） | KD-P3-02 |
| **盲-8** | **纪律偏离四项：补齐掩盖设计目的** | DEV-P2-01「每步后跑断言」自 §4 至 α/X-1 全部执行完毕**一次未跑**，封包前补建首跑至零 FAIL——**但该纪律之设计目的（早发现、早止损）本发未兑现**；DEV-P2-02 manifest 非随落随入；DEV-P2-03 16 件 raw 封包前补 D-3 包装；DEV-P2-04 四件放置面订正。**四项均已自陈，不因补齐而抹除** | KD-P2-12 |

**本组之共同模式候选（限于本发内）**：**机械闸之完备感是局部的**——闸位本身可自不可满足（盲-1）、条款间可交叉冲突（盲-2/3）、检测器可吞证据（盲-4）、判定基面可与声明脱节（盲-6）、断言射程可窄于义务面（盲-5）、复核对象面可漏掉自陈值（盲-7）、纪律可被「补齐」置换（盲-8）。**七条中六条由复核方检出而非执行方自陈**，构成「生产者不能自审」之第二个一手实例。

---

## §5 待答项与未测项（显式登记，不以已测覆盖未测）

| 项 | 状态 |
|---|---|
| `vega` 单量 437 ULP 差异成因 | **未探明**。已排除之解释：「差分求法放大 libm 差异」——`rho` 同走中心差分却两位逐位相同，差异只落 `vega` 一量。判明所需动作＝对 vol-bump 路径实际调用之 libm 函数与其具体实参逐点取位比对（本发 7 探针未覆盖该组实参），**不在作业指令射程内**（KD-P2-09） |
| ObjectStore 两处拒绝文案是否同一道闸 | CLI/库层导出面＝「data licensing restrictions... Institutional accounts」；算法侧读取＝「does not have permission to read from the organization Object Store... contact your organization administrator」。**文案不同、效果同为拒，是否同闸未判** |
| FR-P2-03 三选一（遗留会话自然过期／本位调用置换／巧合） | 经 E27 回溯**部分收窄**：卡死状态来源确证非 P2'；是否由 P2' 终结**仍不可分辨**（`research/read` 在卡死态下亦耗时 121s，说明该调用确会介入节点分配面）。KD-P2-02 未消解 |
| airlock 出向回流 | F2-09 载 airlock 写入**不回流**；**本发未复测该面** |
| `research.ipynb` 体积 21,875 字节（E1-17）vs API 读回 19,626 chars | char≠byte（UTF-8），**不足以断定文件有变动**。登记为待核项 |
| B 类端点之文档收录与稳定性 | 三件明载为未完成事项，本发未核（P1' §10 禁作面 #13） |
| E1 映射表五行（E1-10／15／16／21／22） | 记录表内**零四栏落位**（P3-i）。处置归 KD-P3-04 |

---

## §6 本件封口

本件为 **candidate**。**P3' 本位零裁决**：全部定性为呈报候选。条款立法、缺口编号、纪律信号升格、E5 强制项兑现状态判定**全部归 KD**。

**射程重申**：本件**不含任何跨发结论**。第二发形态与第一发之同型性、两发合并归纳、29-B 解冻材料，**全部在《两发合并归纳件》内处理**。
